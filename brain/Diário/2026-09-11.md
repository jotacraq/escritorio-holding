# 2026-09-11

## 10:30 — Fase 10 desenhada: copiloto ao vivo da Sessão de Viabilidade

**Pedido:** Marcio quer um assistente de IA que acompanhe a SV **ao vivo** —
ouve a conversa, sugere a próxima pergunta, avisa o que falta no roteiro e
**adapta o roteiro quando um decisor não está na sala**. Referência: um sistema
que o Juliano Santos (THB) apresentou na palestra Diamante.

**Feito:**
- `docs/ARQUITETURA-FASE-10.md` (1.001 linhas, 15 seções, 12 bloqueios B65–B76).
- `CLAUDE.md:30` — corrigido. Listava `output: 'standalone'` como restrição da
  Hostinger; `next.config.ts:4-8` desmente com medição de 03/09 ("todo request
  virava `config_ausente`"). Seguir o CLAUDE.md à risca **derrubava produção**.
- Branch `fase-10-copiloto-ao-vivo`, commit `0a4984f` (só o desenho + CLAUDE.md;
  o código da Fatia 1 ficou fora, aguardando a trava).

**Medido — sonda real contra a API do Recall.ai (não documentação):**
- `GET /api/v1/bot/` → **HTTP 200** em ~1 s. Auth é `Authorization: Token`,
  **não `Bearer`**.
- `POST /bot/` → **HTTP 201 de primeira** com `recording_config.transcript.
  provider.meeting_captions` + `realtime_endpoints` webhook nos eventos
  `transcript.data`, `participant_events.join`, `participant_events.leave`.
  **Os três eventos que a feature precisa existem.**
- 🔴 A resposta voltou `"retention": {"type": "forever"}` **sem eu pedir** — é o
  default. Por omissão, áudio/vídeo de conversa patrimonial ficaria guardado
  **para sempre**, fora do Brasil. Virou **B76** e requisito duro no desenho.
- Bot contra sala inexistente: `joining_call` → `fatal` (`meeting_not_found`)
  em **200 ms**, `recordings: 0`, **zero consumo**. As 5 h grátis intactas.
- `POST /bot/<id>/leave_call/` devolve **400 `bot_command_error`** se o bot já
  desligou — estado esperado, não falha. Tratar como tal, senão a Fatia 4 enche
  `erros_servidor` de ruído.
- Região **`us-east-1`**: os dados **saem do Brasil**. Entra na base legal do B65.

**Decidido:**
- Entrada de áudio por **bot na sala**, não microfone do navegador. Mantém
  `Permissions-Policy: microphone=()` intacto, reusa o padrão de webhook
  assinado (4 já existem na casa) e é a **única** opção que entrega **lista
  nominal de participantes** — o dado que falta para o 3º SIM, hoje um booleano
  sem lastro. Microfone descartado: captura invisível para o cliente e
  diarização fraca. `n8n/provedor de sala` descartado por fato:
  `sala.provedor='manual'`, workflow nunca escrito, Meet×Zoom pendente da Fase 4.
- **Transporte: polling de 3 s com cursor.** SSE criaria o 1º request de vida
  longa atrás de um proxy Hostinger cujo timeout não é nosso; Supabase Realtime
  estrearia na tabela mais sensível do banco.
- **A IA não vira dona do método:** 3 camadas, só a 3ª usa IA; ela aponta para
  `bloco_id` que já existe e quem navega é a advogada. Precedente aplicado:
  "a IA ordena, não escolhe".
- **5 fatias publicáveis**, nascendo desligado em **4 travas independentes**.

**Análise da referência (Juliano):** ele usa **Claude Sonnet** — o mesmo modelo
que já temos. A entrada dele é **`.txt` de transcrição**, e a URL da demo é
`localhost:4177/index.html?bridge=1` com **Tactiq Bridge V3** na barra de
favoritos: lê a legenda do **Google Meet** por extensão, numa página local. Não
tem bot, não tem servidor, não tem STT contratado. O que ele tem e nós não é o
**catálogo de 11 painéis e os critérios** — método destilado, não tecnologia.

**Pendente com o João/Marcio:**
- `OPENROUTER_API_KEY` sumiu da Hostinger → **toda IA do sistema responde 503**.
  Trava a Fatia 2. É o item mais urgente do projeto, independente desta feature.
- Rotacionar as 3 chaves do Recall (foram coladas em chat) antes de produção.
- B65/B66/B67/B76 com a Dra. Elaine antes da Fatia 4.

**Notas tocadas:** [[sic-hf-decisao-copiloto-ao-vivo-bot-na-sala]] ·
[[recall-ai-bot-reuniao-sic-hf]] · [[sic-hf-copiloto-juliano-referencia]] ·
[[sic-hf-armadilha-trava-lgpd-por-caminho-de-dado]]

## 13:50 — Fatia 1 implementada e REPROVADA na trava do Fable

**Feito:** migration `0091` (3 tabelas, RLS por `app.ve_patrimonio()`, 10 chaves
`copiloto_sessao.*` nascendo `false`), 2 rotas determinísticas, `PainelCopiloto`
em abas com o Briefing na coluna de 320 px. Back e front em paralelo, fronteiras
disjuntas, contrato de tipos fechado sem ajuste.

**Medido:** `npm run verificar` → **887 passed | 1 skipped**, typecheck e lint
limpos. `grep` confirma **zero IA** em qualquer caminho da fatia.

**Veredito: REPROVADO** em Solidificação. Três achados legítimos:

1. 🔴 **O kill-switch não existe.** O plano e a **descrição gravada na própria
   0091** (`:236-237`) prometem que com `copiloto_sessao.ativo=false` "a aba não
   aparece e nenhuma rota grava nada". **Nenhuma linha de código lê a chave.**
   Reversão que não desliga não é reversão — e é "dado inventado" **dentro do
   banco**.
2. 🔴 **`scripts/verificacao-0091.sql` aborta no passo 7:** insere NULL em
   `ok boolean not null` → o roteiro morre antes do resultado. Era o único
   instrumento que sustentava o `A MEDIR`, e ninguém viu porque ninguém pôde
   rodá-lo — exatamente o cenário em que ele tinha de ser à prova de erro.
3. 🔴 **Comentário que mente** em `estado.ts:20-22` (afirma leitura condicional
   que o código não faz) + 3 queries onde 2 bastavam, numa função que a Fatia 3
   vai chamar **a cada 3 s**.

**Aprovado e não volta:** escopo determinístico, RLS/grants, PII, C10/U1/C12/
a11y, faseamento do C2, correção do CLAUDE.md.

**Decidido:** `A MEDIR` é aceitável nesta fatia (sem caminho quente, nenhum
número inventado), com **condição vinculante** — rodar o roteiro completo +
`explain (analyze, buffers)` antes de a 0091 tocar produção.

**Conflito latente achado pelo Fable, devolvido ao arquiteto:** a trigger da
0092, como rascunhada, vai recusar **também o segmento `origem='manual'`** da
Fatia 1 — a Fatia 2 entraria matando uma capacidade que a Fatia 1 entregou.

**Notas tocadas:** [[sic-hf-decisao-copiloto-ao-vivo-bot-na-sala]]

## 15:44 — Fatia 2 do copiloto: desfecho de `desvio_sugerido` ligado (Ir para lá / Ignorar)

**Pedido:** Marcio/coordenador — o §5 do plano promete registrar `desfecho`/`desfecho_em` de `copiloto_sugestoes` quando a advogada aceita ou ignora um desvio sugerido; as colunas existiam (0091), os botões existiam na tela, mas nada gravava. Fable apontou como "dado plausível que não existe". Backend entregou `POST /api/sessoes/[id]/copiloto/sugestoes/[sugestaoId]/desfecho`; front deveria ligar os dois botões.
**Feito:**
- `src/components/sessao/api.ts` — `registrarDesfechoSugestaoCopiloto(sessaoId, sugestaoId, desfecho)`, wrapper fino sobre a rota nova.
- `src/components/sessao/PainelCopiloto.tsx` — `ApresentacaoSugestao`/`DesvioSugerido` agora recebem `sessaoId`+`sugestaoId` (de `resposta.sugestao_id`); `registrarDesfechoSemBloquear()` dispara a gravação em paralelo (`void ... .catch(() => {})`), nunca bloqueia "Ir para lá"/"Ignorar" nem mostra erro — inclusive `desfecho_ja_registrado` (409, duplo-clique) é silencioso por design.
- `src/components/sessao/PainelCopiloto.test.tsx` — 6 testes novos: aceita grava e navega; ignorada grava e dispensa; falha na gravação não impede navegação nem dispensa; `desfecho_ja_registrado` não vira alerta; axe limpo.
**Medido:** `npm run verificar` — typecheck limpo, lint limpo, `59 test files | 958 passed | 1 skipped` (baseline do coordenador era 952 passed | 1 skipped — 6 testes novos, todos verdes).
**Decidido:** desfecho é telemetria, não a ação — UI nunca espera o POST nem sinaliza falha dele, para não interromper a sessão ao vivo com a cliente. Não toquei em `src/server/`, `src/app/api/` nem `supabase/` (fronteira do front).
**Pendente com o João:** nenhuma — Fatia 2 completa no que foi pedido. Fatia 3 (ciclo automático, `expirada`) fica para depois.
**Notas tocadas:** —

## 16:41 — Fatia 3 do copiloto (backend): ciclo automático + polling + consolidação em `transcricoes`

**Pedido:** arquiteto — §8 Fatia 3: ciclo automático (3 gatilhos com claim atômica), polling coalescido, consolidação em `transcricoes` ao encerrar, `desfecho='expirada'`. Sem webhook/bot (Fatia 4, fora de escopo).

**Feito:**
- `supabase/migrations/0096_copiloto_ciclos.sql` — tabela `copiloto_ciclos` (PK `(sessao_id, janela)` = a claim; `bloco_indice` para o gatilho de virada de bloco comparar entre avaliações). RLS `app.ve_patrimonio()` (SELECT), escrita só `service_role` (SELECT+INSERT, sem UPDATE/DELETE — append-only).
- `src/server/copiloto/gatilho.ts` — `decidirGatilho` (função pura) + `avaliarGatilho` (leitura): tempo+fala-nova (≥45s E ≥1 segmento novo) OU virada de bloco (piso 15s anti-martelada). Silêncio nunca dispara.
- `src/server/copiloto/ciclo.ts` — `executarCicloCopiloto`: claim (`insert...on conflict do nothing`) → 🔴 **gate jurídico A CADA CICLO** (não só no primeiro — é a correção central da errata §6.2.2 aplicada ao caminho automático) → orçamento → IA (timeout 8s) → validação → grava `copiloto_sugestoes`.
- `src/app/api/sessoes/[id]/copiloto/route.ts` — GET estendida: `bloco` (já existia) + `desde_segmento`/`desde_sugestao` (cursores), payload com `segmentos_novos`/`sugestoes_novas`/`ciclo`. O ciclo roda a cada chamada; se lançar, NUNCA derruba o polling (erro registrado, resposta 200 com o que já tinha).
- `src/app/api/sessoes/[id]/copiloto/encerrar/route.ts` (novo) — marca `sessoes_copiloto.estado='encerrado'`, consolida segmentos em `transcricoes` (reusa o caminho de `POST /api/sessoes/[id]/transcricao`, idempotente por sha256, piso de 200 caracteres), expira sugestões pendentes (`desfecho='expirada'`). Idempotente: 2ª chamada → 409 `sessao_ja_encerrada`.
- `src/server/copiloto/consolidar.ts` (novo) — `concatenarSegmentos` (pura) + `consolidarTranscricaoDaSessao`, replica a lógica de retentativa de `transcricao/route.ts` (função não exportada de lá — não editei a rota, fora da fronteira).
- `src/types/copiloto.ts` — estendido (não redefinido): `EstadoCopilotoComPolling`, `SugestaoCopilotoPolling`, `InfoCicloCopiloto`, `RespostaEncerrarCopiloto`.
- Testes novos: `gatilho.test.ts` (14), `ciclo.test.ts` (10), `consolidar.test.ts` (7), `route.test.ts` do polling (5), `route.test.ts` do encerrar (5) = 41 testes novos.

**Medido:** `npm run verificar` — typecheck limpo, lint limpo, **999 passed | 1 skipped** (baseline 958 passed | 1 skipped + 41 novos, zero regressão). `explain (analyze)` das 2 queries de polling (`sessoes_copiloto_segmentos`/`copiloto_sugestoes` por `(sessao_id, ordem/ordem_evento)`) e da claim/leitura de `copiloto_ciclos`: **A MEDIR** — sem `.env`/banco nesta máquina, comandos exatos colados em `scripts/verificacao-0096.sql` §8 e no comentário de `contexto.ts`.

**Decidido:** `copiloto_ciclos` só nasce na Fatia 3 (não na 0091), conforme §11 do plano ("DDL sem função é peso morto"). `desde_sugestao`/`desde_segmento` recalculam `visivel` na LEITURA contra a confiança mínima atual (pode ter mudado desde a gravação). O ciclo é avaliado dentro do próprio GET de polling — não há rota separada de "tick".

**Pendente com o João:** rodar `scripts/verificacao-0096.sql` + os 2 `explain (analyze, buffers)` do polling contra o banco real antes de produção (mesma condição vinculante da Fatia 1). `OPENROUTER_API_KEY` continua ausente na Hostinger — bloqueia teste de IA real desta fatia também.

**Notas tocadas:** —

## 17:31 — Fatia 3 do copiloto (backend): correção dos 2 achados do Fable (2ª rodada)

**Pedido:** coordenador — Fable reprovou 2 itens meus (núcleo aprovado e não volta: gate a cada ciclo, claim, consolidação, duracao_maxima_minutos, polling_ms, varredura das 10 chaves confirmada por grep próprio). Achado B: comentário em `gatilho.ts` descrevia predicado (`ordem > $2`) que o código não usa (real é `criado_em > $2`/sem filtro). Achado C: `explain` da query de sugestões do polling e das 2 leituras do gatilho prometidos mas ausentes de todo roteiro; comentários da rota apontavam para `verificacao-0096.sql` onde não estavam.

**Feito:**
- `src/server/copiloto/gatilho.ts` — comentário de `avaliarGatilho` reescrito: predicado real (`where sessao_id=$1 and criado_em>$2 limit 1` / `where sessao_id=$1 limit 1`), índice usado só pela coluna `sessao_id`, filtro residual por `criado_em` justificado pelo mesmo raciocínio de `contexto.ts::buscarJanelaTranscricao` (modelo indicado pelo Fable).
- `scripts/verificacao-0096.sql` — passo 8 ganhou (c) a query de sugestões do polling (a que o §2.2 marca com a armadilha do cursor-uuid) e (d1)/(d2) as duas leituras do gatilho, cada uma com o comando exato e o esperado escrito. Passo 9 (nota não bloqueante) — comando de medição para decidir se coalescer as leituras de `configuracoes` (4 sempre-lidas: ativo/confiança/polling_ms/duracao_maxima) compensa, sem implementar a coalescência às cegas.
- `src/app/api/sessoes/[id]/copiloto/route.ts` — os 2 comentários corrigidos: segmentos aponta para `verificacao-0091.sql §7` (onde o comando realmente está, é a mesma query do manual); sugestões aponta para `verificacao-0096.sql §8 (c)` (onde agora está).

**Medido:** `npx vitest run` → **1038 passed | 1 skipped** (mesmo baseline do coordenador, zero regressão — mudança foi só comentário/SQL). `npx eslint` limpo em `src/server/copiloto/` e nas rotas do copiloto. `npx tsc --noEmit` sem erro fora de `PainelCopiloto.tsx` (frontend corrigindo em paralelo, fronteira disjunta).

**Decidido:** não implementei a coalescência de `configuracoes` — decisão do próprio Fable ("que a medição decida, não eu"). Comando pronto no roteiro para quando houver banco real.

**Pendente com o João:** rodar `scripts/verificacao-0096.sql` completo (agora com os 6 `explain` do passo 8) contra o banco real antes de produção.

**Notas tocadas:** —

## 17:10 — FASE 10 FECHADA (`0d979da`)

**Pedido:** copiloto ao vivo da Sessão de Viabilidade, ponta a ponta.

**Feito:** 8 commits, migrations **0091–0098**, **874 → 1212 testes**, 5 fatias,
1 pentest, **14 rodadas de veredito** do Fable. Branch
`fase-10-copiloto-ao-vivo`, árvore limpa.

**Medido:** `npm run verificar` → 1212 passed | 1 skipped, typecheck e lint
limpos, estável em 9+ execuções completas. **Nenhuma migration aplicada em
banco nenhum** — todos os `explain` `A MEDIR` com comando exato nos 5 roteiros.
Nenhum número de plano foi inventado.

**Decidido (as que mudaram o desenho):**
- entrada de áudio por **bot na sala**, não microfone — única opção que entrega
  lista nominal de participantes, o dado que o 3º SIM nunca teve;
- **polling de 3 s**, não SSE nem Realtime (proxy da Hostinger com timeout que
  não é nosso; Realtime estrearia na tabela mais sensível do banco);
- **o gate de LGPD vive no caminho de saída de dado** (a chamada), não no
  INSERT — errata §6.2.2, depois de a trava reprovar a versão que enviava a
  fala antes de conferir consentimento;
- `webhooks_eventos` **não** ganhou expurgo: 5 origens compartilham a tabela e
  o **B42** é decisão da Dra. Elaine desde a Fase 7.

**Pendente com o João/Marcio:** `OPENROUTER_API_KEY` (toda IA em 503) ·
rotacionar as 3 chaves do Recall · rodar os 5 roteiros ao aplicar as migrations.
**Com a Dra. Elaine:** B65 · **B66 (texto do 1º SIM)** · B67 · B69 · B76.

**Dois padrões que a fase descobriu, e que valem além dela:**
1. **"Promessa sem fiador"** — 8+ ocorrências; a 8ª (`n8n/sala`) prova que
   **precede a Fase 10**. Auditoria das Fases 7-9 recomendada.
2. **"Interações, não erros isolados"** — 6 caminhos para a mesma fila travada,
   fechados por **prova por partição**. Pergunta-padrão que fica:
   *"o que garante que tudo que entra nesta fila eventualmente sai?"*

**Notas tocadas:** [[sic-hf-decisao-copiloto-ao-vivo-bot-na-sala]] ·
[[sic-hf-armadilha-trava-lgpd-por-caminho-de-dado]] ·
[[recall-ai-bot-reuniao-sic-hf]] · [[sic-hf-copiloto-juliano-referencia]]

