## Fatia 1 (backend) — "iniciar a sessão quando quiser, sem burocracia"

**Pedido:** Filosofia do dono: quem opera o sistema não segue roteiro fixo — o sistema se adapta a quando ela quer iniciar a sessão, independente de agendamento, contrato, formulário. Fatia de backend: idempotência no fluxo "Iniciar sessão agora" + endpoint único `POST /api/jornadas/[id]/sessao/iniciar`.

**Feito:**
- `src/server/agenda/iniciar-sessao.ts` (novo) — `iniciarSessaoImediata()`: garante `sessoes_viabilidade` (1:1) + `agendamentos` vivo agora, com idempotência por JANELA (não por advogada). Reusado pelos dois pontos de entrada.
- `src/app/api/jornadas/[id]/agendamentos/route.ts:60-84` — `modo: "imediato"` no `CorpoSchema`, desvia para `iniciarSessaoImediata`. Fluxo normal (sem `modo`) intocado.
- `src/app/api/jornadas/[id]/sessao/iniciar/route.ts` (novo) — `POST` sem corpo, único ponto de "iniciar sessão". Atrás de `exigirVePatrimonio()` + kill-switch `sessao.inicio_direto_do_card` (nasce `true`).
- `src/server/agenda/config.ts` — `lerConfiguracaoBool()` + `CHAVE_INICIO_DIRETO_DO_CARD`.
- `supabase/migrations/0108_sessao_inicio_direto_do_card.sql` — seed `sessao.inicio_direto_do_card = true`, `on conflict do nothing`.
- Testes novos: `src/server/agenda/iniciar-sessao.test.ts` (4 casos, prova reaproveitamento na 2ª chamada) + `src/app/api/jornadas/[id]/sessao/iniciar/route.test.ts` (5 casos: kill-switch, 404, 201/200, EXCLUDE).

**Medido:**
- `npx tsc --noEmit` → limpo.
- `npx eslint src` → 0 erros (4 warnings pré-existentes/de outro agente, nenhum meu).
- `NODE_OPTIONS=--max-old-space-size=6144 npx vitest run --no-file-parallelism` → **91 arquivos, 1380 passed, 1 skipped** (baseline era 1371+1/85 arquivos; +9 testes novos em 2 arquivos, zero regressão).

**Achado confirmado (não corrigido por outro agente, corrigido aqui):** `IniciarSessaoAgora.tsx:140` não envia `advogada_id` → grava `null`. A EXCLUDE `ex_agenda_sem_sobreposicao` (0008:36) é `(advogada_id with =, tstzrange &&)` — NULL nunca conflita com NULL no Postgres, ficava inerte nesse fluxo. Resolvido por idempotência de janela, sem preencher `advogada_id` (decisão de escopo à parte).

**Pendente com o Marcio:** `explain (analyze, buffers)` da query nova de idempotência (`agendamentos` filtrado por `sessao_id + status in (...) + janela contra now()`) não foi rodado — este agente não tem MCP do Supabase nem credencial de produção disponível nesta sessão. Consulta pronta abaixo, para rodar via MCP:

```sql
explain (analyze, buffers)
select * from agendamentos
where sessao_id = '<uuid de uma sessão com agendamento vivo>'
  and status in ('agendado','confirmado')
  and inicio_em <= now()
  and fim_em > now();
```

Não criei índice novo para essa query — não há como provar o predicado sem o `explain` real. A tabela `agendamentos` não tem índice puro em `sessao_id` (só o parcial `uniq_agendamento_confirmado` e a GIST da EXCLUDE); dado o volume por sessão (poucos agendamentos por jornada), a expectativa é Seq Scan seguro, mas isso precisa ser medido, não assumido.

**Notas tocadas:** nenhuma nota de domínio nova — fatia é pontual, achado já estava registrado na tarefa.

## Bot autenticado no Zoom (token ZAK) — 17/09/2026

**Pedido:** a conta Zoom da Dra. Elaine exige "Somente usuários autenticados" (travado pelo admin da conta Zoom, não dá para desligar) — todo bot caía em `meeting_requires_sign_in`, zero segmento gravado. Implementar o fluxo oficial da Recall (OAuth do Zoom → credencial guardada NA RECALL → `zoom.zak_url` no pedido do bot).

**Contrato lido** em `docs.recall.ai/docs/zoom-signed-in-bots` (WebFetch, 3 leituras cruzadas — este agente não teve acesso ao MCP da Recall, só ao WebFetch; reproduzido literalmente no comentário de topo de `server/integracoes/zoom.ts`):
- `POST https://us-east-1.recall.ai/api/v2/zoom-oauth-credentials/` com `{oauth_app, authorization_code: {code, redirect_uri}}` → `{id}` (credential_id opaco, é só o que guardamos).
- `GET .../zoom-oauth-credentials/{id}/access-token/` → `{access_token}`.
- `GET https://api.zoom.us/v2/users/me/zak` com `Authorization: Bearer <access_token>` → `{token}` (o ZAK).
- Ao criar o bot: `zoom.zak_url` no corpo de `POST /api/v2/bot/` — URL NOSSA que devolve o ZAK em **texto puro** (confirmado literalmente 2x: pseudocódigo do handler HTTP da doc e a frase "returns the ZAK token as a plain text HTTP response").
- Escopo Zoom: `user:read:zak` (default).
- Os 4 sub_codes de erro (`meeting_requires_sign_in`, `zoom_local_recording_disabled`, `zoom_bot_in_waiting_room`, `zoom_local_recording_request_disabled_by_host`) **NÃO estão documentados** na página lida — tratados por nome/semântica óbvia no frontend, sem confirmação literal da doc (registrado como tal no comentário do código).

**Feito:**
- `supabase/migrations/0113_integracao_zoom_oauth.sql` (novo) — tabela `integracoes_zoom` (singleton `id=1`, CHECK), guarda só `recall_credential_id` (nunca token do Zoom). RLS `app.eh_admin()` para SELECT (`authenticated`); INSERT/UPDATE só `service_role`. `scripts/verificacao-0113.sql` prova singleton, RLS force, GRANTs exatos e a policy usando `eh_admin()`.
- `src/server/integracoes/zoom.ts` (novo) — `zoomOAuthConfigurado`, `montarUrlAutorizacaoZoom`, `trocarCodigoPorCredencialRecall`, `obterZak`, `montarZakUrlComSegredo`, `zakWebhookConfigurado`, `linkEhZoom`.
- `src/app/api/integracoes/zoom/iniciar/route.ts` (novo) — `GET`, `exigirVePatrimonio()`, redireciona para o Zoom; 503 sem env.
- `src/app/api/integracoes/zoom/callback/route.ts` (novo) — `GET`, responde **200 mesmo sem `?code=`** (o Zoom valida a redirect URL ao cadastrar o app — era o 404 que o dono viu hoje). Com `code`: troca com a Recall, grava `credential_id` em `integracoes_zoom`, tela HTML simples de sucesso/erro.
- `src/app/api/integracoes/zoom/zak/route.ts` (novo) — `GET` chamado pela Recall, sem sessão de usuário. Segredo em `?k=` comparado com `segredosIguais` (`server/sala/assinatura.ts`, reuso pedido). Resposta **texto puro** (`text/plain`), nunca JSON. Fail-closed: sem env → 503; `k` errado → 401; sem credencial → 404 `conta_zoom_nao_autorizada`.
- `src/server/copiloto/recall.ts` — `PedirBotParams.zakUrl?: string`; corpo do bot ganha `zoom: {zak_url}` só quando `zakUrl` está presente (`JSON.stringify` omite `undefined`, então bot de Meet nunca carrega `zoom`).
- `src/app/api/sessoes/[id]/copiloto/bot/route.ts` — detecta Zoom por `linkEhZoom(link_sala)`; sem credencial → 409 `zoom_nao_autorizado` (mensagem específica, não genérica); monta `zakUrl` e passa a `pedirBot()`. Comentário de topo da rota atualizado (passo 9b).
- `src/components/sessao/copiloto/PainelBot.tsx` — `MENSAGENS_RECUSA_BOT.zoom_nao_autorizado` + `SUB_CODIGOS_ZOOM` (os 4 sub_codes do Zoom) plugado em `mensagemSalaInvalida()`.
- `src/types/copiloto.ts` — `CodigoRecusaBotCopiloto` ganha `"zoom_nao_autorizado"`.
- Testes novos: `src/server/integracoes/zoom.test.ts` (17), `src/app/api/integracoes/zoom/{iniciar,callback,zak}/route.test.ts` (3+6+6), +2 em `recall.test.ts` (zakUrl presente/ausente no corpo), +4 em `bot/route.test.ts` (Meet não recebe zakUrl, Zoom sem credencial, Zoom sem env, Zoom com credencial passa zakUrl).

**Medido:**
- `npx tsc --noEmit` → limpo.
- `npx eslint src` → 0 erros (1 warning pré-existente em `ConduzirSessaoApp.tsx:183`, não meu).
- `NODE_OPTIONS=--max-old-space-size=6144 npx vitest run --no-file-parallelism` → **99 arquivos, 1493 passed, 1 skipped, 1 failed**. O único falho é o instável pré-existente e já nomeado (`ConduzirSessaoApp.test.tsx` — "escolher 'Corrigir parte' dispara a fixação manual", família do `waitFor`/"escolher a parte 0" citado no pedido); não toquei `ConduzirSessaoApp.tsx`/`.test.tsx`. Rodada isolada da feature: **6 arquivos, 80 passed**.

**Decidido:**
- Credencial (`recall_credential_id`) mora em tabela própria (`integracoes_zoom`), não em `configuracoes` — aquela tabela nega INSERT/DELETE de propósito (chave nova é migration) e é para parâmetro EDITÁVEL pelo admin; isto é estado técnico gravado pelo callback OAuth (`service_role`), mesma categoria de `sessoes_copiloto.gravacao_externa_id`.
- Segredo do `zak` é env PRÓPRIA (`ZOOM_ZAK_WEBHOOK_SECRET`), nunca reaproveita `COPILOTO_WEBHOOK_SECRET` — mesma regra já aplicada no webhook de transcrição (vazamento num canal não pode comprometer o outro).
- `zoom.zak_url` só entra no corpo do bot para link de Zoom (`linkEhZoom` por host) — bot de Meet nunca carrega o campo.

**Pendente com o Marcio (só ele pode fazer):**
1. Colar na Hostinger: `ZOOM_CLIENT_ID`, `ZOOM_CLIENT_SECRET` (o dono disse que cola direto, este agente não pediu nem viu os valores), `RECALL_ZOOM_OAUTH_APP_ID` (o id do App OAuth do Zoom **dentro da Recall** — cadastro manual no painel da Recall, precede o código), `ZOOM_ZAK_WEBHOOK_SECRET` (gerar um segredo novo, ex. `openssl rand -hex 32`).
2. Aplicar `supabase/migrations/0113_integracao_zoom_oauth.sql` (rodar `scripts/verificacao-0113.sql` antes, como já é praxe).
3. Depois do deploy: logar como admin/advogada → `GET /api/integracoes/zoom/iniciar` (criar botão em Admin → Integrações é passo de frontend, fora deste escopo) → autorizar com a conta `apoio@csmholding.com.br` → confirmar a tela de sucesso.
4. Testar: pedir bot numa sessão com link de Zoom real.
5. `explain (analyze)` não coube aqui — a migration é DDL simples (singleton, sem query de alta cardinalidade); o roteiro prova RLS/GRANT em vez de plano de execução.

**Notas tocadas:** nenhuma nota de domínio nova neste turno (registro completo aqui e no MEMORY.md/vault, conforme protocolo global).

## 13:30 — Sessão de teste (Cláudia): copiloto destravado e latência medida

**Pedido:** "pega toda transcrição e tudo que for possível a gente converter em insight"; depois, ao vivo, "diminuir o timing entre a fala e o insight" + "notificação visual quando surgir um novo insight".

**Feito:**
- `src/server/copiloto/entrada-bot.ts` — `resolverSessaoPorBotId` passou a ler `estado`; nova `ativarSePrimeiraFalaDoBot` faz a transição `aguardando`→`ativo` na PRIMEIRA fala transcrita pelo bot, com `.eq("estado","aguardando")` como trava de corrida.
- `src/server/copiloto/entrada-bot.test.ts` — 3 testes de regressão (ativa / não reescreve sessão já ativa / recusa sessão consolidada) + espião `onUpdateSessao` no mock, que distingue UPDATE de SELECT e acumula os `.eq()`.

**Medido (sessão real 755d87d7, tudo com dado de produção):**
- 🔴 **O bug:** 316 segmentos gravados, **0 ciclos de IA**. `ciclo.ts` exige `estado='ativo'` ESTRITO e só a rota de segmento MANUAL ativava. Numa reunião conduzida pelo bot ninguém digita → copiloto mudo do começo ao fim, **sem erro em log nenhum**.
- Após destravar: 451 segmentos, 16 ciclos, 19 sugestões.
- **Diferenciação de vozes PROVADA ao vivo** (era pendência não verificada): 307 segmentos "João CSM" + 140 "Elaine Montenegro". Antes, 100% dos segmentos vinham com um único falante.
- 🔴 **48% das chamadas de IA falhando**: 12 de 26 com `openrouter_resposta_vazia: corpo nao-JSON ou vazio apos 8s (status 200)`, todas em 7.858–7.965 ms. O provedor RESPONDIA; nós desistíamos antes de ler.
- Sucesso: p50 6.036 ms, **p95 7.284 ms** contra `TIMEOUT_COPILOTO_MS = 8_000`. Folga de 716 ms — o teto foi calibrado ANTES de dossiê (0109) e inventário (0111) engordarem a entrada para 8.408 tokens.
- Latência fala→insight: **p50 30,9 s**, máx 179 s. `intervalo_segundos=20` + ~6 s de IA; cada falha vira buraco de ~60 s.
- 🔴 **Eco do Zoom**: 35 pares duplicados em 451 segmentos (~8%), 34 deles com falante DIFERENTE, a 0,53 s de distância. Áudio em viva-voz volta pelo microfone do outro. Correção é operacional (fone de ouvido), não de código.

**Decidido:**
- Ativar pela PRIMEIRA FALA transcrita, não por "bot pedido" (pode ficar na sala de espera) nem por entrada de participante (pode ser só a advogada montando a sala).
- Não mexer em `TIMEOUT_COPILOTO_MS` sem o veredito do pentester e sem medir de novo — o teto existe por um motivo real (§4.3, CONFLITO C3: o global de 300s é veneno aqui).

**Aprendizado que se paga:**
- **Teste verde não prova integração entre dois caminhos.** 308 testes do copiloto passavam com a feature 100% morta em produção — nenhum cobria "bot grava → IA roda", só cada metade isolada. Escrevi o teste novo e **provei que ele falha sem a correção** antes de aceitá-lo (mesma lição do `retention`, hoje de manhã).
- **`status='falhou'` bem instrumentado é o que torna o bug achável**: a Fase 11 trocou `Promise.race` por `AbortController` justamente para parar de mascarar timeout como sucesso. Sem isso, as 12 falhas seriam invisíveis.

**Pendente com o Marcio:**
- Subir o teto de timeout (o p95 encosta nele) e a notificação visual de novo insight — plano apresentado, aguardando escolha.
- Rotacionar Client Secret e Secret Token do Zoom (colados no chat).
- 4 testes vermelhos PRÉ-EXISTENTES em `ConduzirSessaoApp.test.tsx`: passam isolados (22/22), falham na suíte completa com `aria-busy="true"` — poluição entre testes, determinística nas 2 rodadas. Não tem relação com esta mudança.

**Notas tocadas:** [[Esteira do cliente]] · [[Copiloto de sessão]]

## Fatias A, B, C (frontend) — status permanente, "Consultando…" e realce de não-lido

**Pedido:** painel do copiloto ao vivo — Fatia A (linha de status permanente estendendo `GateBloqueado` para as 5 situações que `paraInfoCiclo` já traduzia e a tela descartava; medido: 12 de 26 chamadas de IA, 48%, sumiam sem aviso), Fatia B ("Consultando…" enquanto o GET do ciclo automático está em voo, timeout subindo para 20s), Fatia C (contador de não-lido no rótulo "Fale agora · N novas" + realce de 150ms de fundo âmbar no card recente não lido).

**Feito:**
- `src/components/sessao/copiloto/usePollingCopiloto.ts` — `EstadoPollingCopiloto.requisicaoEmVoo: boolean`, `true` do disparo do GET até a resposta (sucesso ou falha) voltar. Zero rota nova, zero campo novo no payload — deriva só do ciclo já síncrono dentro do próprio `GET`.
- `src/components/sessao/PainelCopiloto.tsx` — `GateBloqueado` virou `EstadoDoCopiloto`: linha única (`min-h-[3.25rem]`, já existia) com prioridade falha-de-rede-persistente → `bloqueado_pelo_gate` (`role="alert"`, intocado) → `orcamento_estourado` (permanente) → `timeout`/`indisponivel`/`conteudo_recusado` (transiente, "A IA não respondeu desta vez. Continua tentando.", nunca a palavra "erro") → `requisicaoEmVoo` ("Consultando…") → "Ouvindo." (default). Todos os novos estados com `role="status"` + `aria-live="polite"`, nunca `assertive`.
- `BlocoFaleAgora`/`SugestoesDoCiclo` — contador local `naoLidas` por `sugestao_id`: uma sugestão é "não lida" enquanto é a card "recente"; ao ser empurrada para "anteriores" (uma sugestão mais nova chega), é marcada como vista e sai da contagem. Rótulo do `Quadro` vira `"Fale agora · N nova(s)"` só quando `naoLidas > 0`.
- `CardRecente` (novo, dentro de `PainelCopiloto.tsx`) — borda esquerda 2px âmbar como ESTADO (liga/desliga com `naoLida`, nunca anima) + UMA transição de 150ms de `background-color` (âmbar fraco → transparente) via `requestAnimationFrame`, reiniciada só quando `sugestaoId` muda. `globals.css`: `.transicao-realce-insight` (`transition-property: background-color; 150ms; linear`) + `.realce-insight-novo` (fundo âmbar fraco) — reusa a trava GLOBAL de `prefers-reduced-motion` já existente (`transition-duration: 0.001ms !important`), sem duplicar a media query.
- `supabase/migrations/0115_copiloto_indicadores_visuais.sql` (novo, isolado da 0114 que o backend-engineer escreve em paralelo) — `insert` de `copiloto_sessao.realce_insight_novo = true` em `configuracoes`. Documenta a decisão; nenhuma rota lê a chave ainda (aplicado incondicionalmente).
- Testes novos em `PainelCopiloto.test.tsx`: 3 describes (Fatia A, 9 casos; Fatia B, 3 casos; Fatia C, 5 casos) — todos usando o padrão fake-timer da suíte (`vi.advanceTimersByTimeAsync`/`vi.waitFor`, nunca `waitFor` do testing-library sob fake timers).

**Medido:**
- `npx tsc --noEmit` → limpo.
- `npx vitest run src/components/sessao/PainelCopiloto.test.tsx` → **99/99** (82 pré-existentes + 17 novos), 3 rodadas consecutivas, nenhuma flake.
- `npx vitest run src/components/sessao/` (suíte completa) → **164 passed, 1 failed**, sempre o mesmo teste em `ConduzirSessaoApp.test.tsx` (não meu — passa 22/22 isolado, 2 rodadas confirmadas). É a mesma poluição entre suítes já registrada às 13:30 de hoje (`aria-busy="true"`); antes eram 4 vermelhos, agora 1 — não piorei o quadro.

**Decidido:**
- "Não lido" e a transição de fade são estado 100% local por `sugestao_id`, sem coluna nova — a chave `realce_insight_novo` só existe para permitir desligar sem deploy se a advogada achar o efeito indesejado ao vivo, não porque o componente precisa ler configuração hoje.
- Reusei a trava global de `prefers-reduced-motion` de `globals.css` em vez de duplicar a media query no componente novo — já cobre `transition-duration` com `!important`.

**Notas tocadas:** [[Copiloto de sessão]]

## 16:20 — Inventário ao vivo: medido o ruído (31 itens para ~10 bens reais)

**Pedido:** Marcio perguntou pelo "documento que ele gera automático com os insights da reunião".

**Achado 1 — o documento de insights NÃO EXISTE.** A sessão da Nicéas encerrou às
15:58 e gerou `transcricoes.id=aaba5fde` com 51.129 chars — mas é a
**transcrição bruta** (começa com "não sei onde está o mouse"), não análise.
`sessoes_copiloto.resumo_acumulado` está **`{}` vazio**. O "resumo pós-sessão"
está no backlog desde a Fase 2 e nunca foi construído. Matéria-prima toda
disponível: 195 sugestões + 31 itens de inventário com evidência literal.

**Achado 2 — o inventário acumulado tem 3 defeitos, medidos item a item:**

1. **Duplicata por descrição** (~13 itens para ~6 bens). A chave de dedup é o
   texto da descrição, e a IA descreve o mesmo bem de formas diferentes a cada
   ciclo:
   - `outro:carro` e `outro:1 carro` — **mesma evidência literal**
     ("O patrimônio que eu tenho é a casa e 1 carro né?")
   - `casa que sofreu modificações e teve IPTU aumentado` e
     `casa que sofreu modificações e reavaliação de IPTU` — **mesma evidência**
   - 3× "imóveis do inventário do pai" (com/sem "avaliados", com/sem "fazenda
     estadual") — mesma evidência "da avaliação dos imóveis né?"

2. 🔴 **FALSO POSITIVO — fala do EXPERT virou patrimônio do cliente.**
   `imovel:imoveis alugados` tem como evidência *"são famílias empresárias,
   famílias que têm imóveis alugados"* — é a **Dra. Elaine explicando o
   conceito**, não a família declarando bem. Idem
   `imoveis (quantidade nao especificada)`, cuja evidência é a **pergunta**
   ("se tem muitos imóveis, se tem poucos imóveis").
   **O inventário não distingue quem falou.** Só conta menção.

3. **A holding não é patrimônio, é a estrutura proposta** — 6 das 8 entradas de
   `categoria: empresa` são variações de "holding" (a empresa mencionada, a
   estrutura, holding familiar, cotas, holding para transmissão…). Infla a
   contagem de empresas do cliente com o próprio produto que está sendo vendido.

**Consequência:** `posse` está `"incerta"` em **31 de 31** — nenhum item
qualificado como próprio. A contagem que o Marcio pediu em 17/09 ("empresas que
eles consideram como deles DE FATO") tem **zero itens** hoje.

**Decidido:** NÃO gerar o documento de insights antes de corrigir a agregação —
um relatório que infla o patrimônio é pior que nenhum relatório. Reportado ao
Marcio com o número medido.

**Pendente com o Marcio:** aprovar a correção da dedup + filtro de falante
(só conta o que o DECISOR declara, não o que o expert exemplifica) antes do
documento pós-sessão.

## 17:31 — Roteiro v5 e prompt v7 ATIVOS em produção; nome de outra cliente removido da tela

**Pedido:** "resolve isso tudo aí e coloque em prática" (carga do roteiro que estava bloqueada por tamanho).

**Achado antes de aplicar (mais grave que o problema original):** a v4 ATIVA
trazia o nome **"Eliana"** 7 vezes dentro de `falas[]` — cliente ANTERIOR,
colado de um roteiro personalizado. O script mestre tem 0 ocorrências. A Dra.
Elaine vinha lendo "Eliana, antes de falarmos…" na tela ao vivo, em sessão de
outra pessoa. A v5 da rodada 2 preservou `falas[]` byte-idênticas à v4 e por
isso HERDOU o defeito. Removidos os 7 vocativos (parte_02/04/09/10/11),
capitalizando a palavra seguinte; comparação programática provou que só
`falas` desses 5 blocos mudou.

**Como foi aplicada (a tabela é fechada por RLS forçado; a RPC exige o JSON
inteiro; sem psql nem credencial local):** tabela de carga `_carga_0118`,
13 blocos inseridos um a um via MCP, **cada um conferido por md5 contra o
arquivo da migration** (13/13), JSON final montado NO BANCO
(`jsonb_build_object('blocos', jsonb_agg(txt::jsonb order by parte))`) e
publicado por `publicar_roteiro_versao` (autor = 1º admin ativo; o dono não
está em `perfis_equipe`). Tabela de carga removida em seguida.

**Ativação:** `ativar_roteiro_versao` exige `auth.uid()` de admin (recusaria
via MCP). Reproduzida a semântica dela num `DO` atômico com `GET DIAGNOSTICS`
que ABORTA se qualquer UPDATE não afetar exatamente 1 linha — se o RLS
barrasse em silêncio, nada mudaria (nunca ficar sem roteiro ativo). Janela
confirmada antes: 0 sessões ativas, 0 falas em 10 min.

**Medido/estado final:**
- `roteiros_versoes`: v4 inativa · **v5 ativa 17:31:16**, 13 blocos, 30 campos, 5 proibidos, 0 "Eliana"
- `prompts_versoes`: v6 inativa · **v7 ativa** (cobriu_no_bloco / acerto-erro)
- `copiloto_sessao.acerto_erro_ativo = true` (cobre verde E vermelho)

**Aprendizados:**
- "Byte-idêntico ao anterior" preserva a regressão junto com o acerto. Antes de herdar texto que a advogada lê ao vivo, **procurar nome próprio**.
- Carga grande sem credencial: fatiar por unidade semântica (bloco), verificar cada fatia por hash, montar no banco. Nunca retipar 32 KB à mão.

**Pendente:** o `explain` da 0118 fica sem medição direta (é chamada de RPC que insere 1 linha numa tabela de 5; mesmo padrão já medido na 0114/0117). Redesign da tela: arquiteto acionado com a captura do dono (transcrição vazando sobre o rodapé, "Inventário (0)" com 31 captados, herói ocupado por aviso de sistema).

**Notas tocadas:** [[Copiloto de sessão]] · [[Esteira do cliente]]
