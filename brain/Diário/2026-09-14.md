# 2026-09-14

## 12:30 — Fase 10 APLICADA no banco (0091→0098) e Fatia 1 LIGADA

**Pedido:** Marcio perguntou por que eu não tinha feito as migrations.

**Achado que corrige a sessão inteira:** eu assumi, durante as 5 fatias e 14
vereditos, que **sem `.env` não havia caminho até o banco** — e marquei tudo
como `A MEDIR`. **Estava errado.** O MCP do Supabase (`plugin_supabase`)
alcança o projeto `fcfsnqqaphtamhrpuyoh` desta máquina. Deveria ter verificado
na primeira migration, não na décima pergunta do usuário. O código não muda; a
**justificativa** que repeti em 14 vereditos era falsa.

**Feito:** as 8 migrations aplicadas em produção, uma a uma, com verificação
entre elas. Banco saiu de **0090 → 0098**.

**Medido (o que era `A MEDIR` a fase inteira):**
- **Polling de segmentos** (`where sessao_id=$1 and ordem>$2 order by ordem`):
  **Index Scan** usando `idx_copiloto_segmentos_polling`, `Execution Time
  0,060 ms`, `shared hit=2`. A 1.800 requisições/sessão → **~0,1 s de banco por
  sessão inteira**. O teto do §4.1 era a preocupação; não é.
- **Elegíveis do expurgo:** **Index Scan** no índice parcial
  `idx_copiloto_sessoes_pendentes_expurgo`, **0,063 ms**.
- **Nenhum `Seq Scan`** em nenhuma das duas.

**Portão da 0097 (o que o Fable chamou de "a armadilha catalogada virou
portão"):** rodei o diff do `pg_get_viewdef` **do banco** antes de aplicar. A
view real tinha **9 cláusulas**, o repo assumia 10 — investiguei e a
divergência era **minha leitura** (`'anonimizacao'` é um *filtro* dentro da
cláusula `expurgo_storage_pendente`, não um tipo emitido). **Não havia
divergência real.** Mesmo assim, recriei a view **a partir do texto do banco**,
não do repo, inserindo só a cláusula nova antes do `ORDER BY` — com guarda que
aborta se a forma não bater. Prova final: **11/11 cláusulas presentes, nenhuma
faltando**.

**Teste de aceite do gate jurídico, contra o banco real (rollback proposital):**
- (a) segmento `origem='manual'` **SEM** decisão jurídica → **PASSOU** (correto:
  a Fatia 1 sobrevive — era o teste que protege o campo de digitar/colar);
- (b) segmento `origem='bot'` → **RECUSADO pelo banco**;
- (c) `copiloto_sugestoes` → **RECUSADA pelo banco**.

**Estado final:** 4 tabelas, RLS forçada nas 4, 4 triggers, 11 chaves.
`ativo=true` (**Fatia 1 LIGADA**), `audio_ao_vivo=false`,
`provedor_audio="nenhum"`, `expurgo_ativo=false`, prompt `ativo=false`,
0 decisões jurídicas, 0 consentimentos.

**Decidido:** ligar **só** a Fatia 1 — determinística, zero IA, zero custo,
zero subprocessador. É o que não depende de nenhuma decisão pendente.

**Pendente com o João/Marcio:** `OPENROUTER_API_KEY` na Hostinger (trava a
Fatia 2) · rotacionar as 3 chaves do Recall · B65/B66/B67/B69/B76 com a
Dra. Elaine.

**Notas tocadas:** [[sic-hf-decisao-copiloto-ao-vivo-bot-na-sala]]

## 13:20 — Envs na Hostinger: 14 → 17. A IA do sistema VOLTOU.

**Feito:** replace completo das variáveis de ambiente (a API da Hostinger só
faz replace total e devolve os valores existentes **mascarados** — o Marcio
mandou print com os 14 valores reais, o que tornou o replace seguro).

Acrescentadas: `RECALL_API_KEY`, `COPILOTO_WEBHOOK_SECRET`,
**`OPENROUTER_API_KEY`**. As 14 antigas preservadas, conferidas na listagem.

**Medido (prova funcional, não palavra):**
- webhook do copiloto: **503 → 401**. Antes recusava por não ter secret
  configurado; agora confere e rejeita chave errada. O secret está sendo lido.
- `GET /api/v1/key` do OpenRouter: chave **válida**, limite **US$ 10**, uso 0.
- Chamada real a `anthropic/claude-sonnet-5` com
  `provider.order:["anthropic"], allow_fallbacks:false` (o caminho exato do
  `openrouter.ts`): **respondeu "OK"**, sem erro.

**A IA estava 503 desde ~04/09** (a chave sumiu na recriação do site, achado do
diagnóstico de 08/09). Isso travava **briefing, croqui, material pós-sessão e
o copiloto** — não era problema da Fase 10, era anterior a ela.

**🔴 PENDÊNCIA DE SEGURANÇA — 2ª ocorrência:** o print da Hostinger expôs em
chat `SUPABASE_SERVICE_ROLE_KEY` (ignora RLS: lê e escreve patrimônio e
transcrições de todas as famílias), `CRON_SECRET`, `LIGACAO_IA_WEBHOOK_SECRET`
e `LINK_PUBLICO_PEPPER`. Somam-se às 3 do Recall (11/09) e à do OpenRouter.
O índice de memória JÁ registrava `SUPABASE_SERVICE_ROLE_KEY` como "exposta em
print — rotacionar" de um incidente anterior.

Ordem de risco para rotação: (1) `SUPABASE_SERVICE_ROLE_KEY`, (2)
`OPENROUTER_API_KEY` (custa dinheiro direto), (3) `RECALL_API_KEY` +
`COPILOTO_WEBHOOK_SECRET`, (4) `CRON_SECRET` e `LIGACAO_IA_WEBHOOK_SECRET`.
⚠️ `LINK_PUBLICO_PEPPER` **invalida os links públicos já enviados** a clientes
— conferir se há algum em uso antes de trocar.

**Notas tocadas:** [[sic-hf-decisao-copiloto-ao-vivo-bot-na-sala]]

## 13:50 — BANCADA do copiloto rodada. Custo OK, latência REPROVA a ativação.

**Feito:** 5 chamadas reais a `anthropic/claude-sonnet-5` via OpenRouter, com o
**caminho exato** do `openrouter.ts`: `provider.order:["anthropic"]`,
`allow_fallbacks:false`, `reasoning.effort:"low"`, `response_format:
json_schema` com `strict:true`, e um contexto realista (~660 tokens: bloco do
roteiro, briefing, estado factual com 2 decisores esperados × 1 presente, e
janela de 90 s de fala).

**Medido:**
| | |
|---|---|
| p50 | **6.075 ms** |
| p95 | **18.468 ms** (1ª chamada, cold start) |
| média | 8.442 ms |
| custo/chamada | **US$ 0,0073** |
| teto de 30/sessão | US$ 0,22 (~R$ 1,18) |

**Veredito: o prompt NÃO é ativado.** O §4.3 do plano é explícito — *"se o p95
passar de 8 s com `low`, a feature não sobe; não se compensa latência
aumentando o timeout numa reunião ao vivo"*. O p95 de 18,5 s estoura o teto de
8 s em 2,3×. Mesmo o p50 (6,1 s) come 76% do orçamento.

**O que a medição mostra e o plano não previa:** a 1ª chamada é 3× mais lenta
que as seguintes (18,5 s × ~5,9 s) — **cold start**, não regime. Isso muda o
diagnóstico: não é o modelo que é lento, é a primeira chamada da sessão. Um
*warm-up* no "Iniciar sessão" (1 chamada descartável ao abrir a tela) pode
trazer o p95 real para perto do p50. **Hipótese a testar, não conclusão.**

**Custo aprovado com folga:** R$ 1,18 por sessão no teto de 30 chamadas, contra
R$ 7.200 de croqui. Custo nunca foi o risco; latência é.

**Qualidade da saída — aprovada por leitura:** JSON estrito válido (sem
markdown fence), `evidencia: null` quando não havia citação literal (não
inventou), `observacao.tipo='inferencia'` com `confianca=0.6` para uma leitura
sutil e correta (cliente cita filhos de fora mas só beneficiou as filhas),
`desvio_sugerido: null` (não forçou desvio sem motivo), `confianca_geral=0.55`
— **abaixo do mínimo de 0,6, ou seja, o servidor NÃO mostraria essa sugestão
na tela.** A régua de confiança funciona como desenhada.

**Notas tocadas:** [[sic-hf-decisao-copiloto-ao-vivo-bot-na-sala]]

## 14:10 — Bancada refeita com 8 rodadas: o p95 PASSA. Prompt liberado.

**Contexto:** a 1ª bancada (5 rodadas) deu p95 = 18.468 ms e reprovou a
ativação. A 1ª chamada levou 18,5 s e as outras ~5,9 s — levantei a hipótese de
**cold start**, não regime, e marquei como "hipótese a testar, não conclusão".

**Medido agora, 8 rodadas, mesmo caminho (`json_schema` estrito, effort low,
provider pinado na Anthropic):**

| | ms |
|---|---|
| p50 | **6.547** |
| p95 | **6.925** |
| máximo | 7.098 |
| mínimo | 4.280 |

**Nenhuma passou de 8 s** — nem a primeira (6.290 ms). O outlier de 18,5 s não
se reproduziu em 8 tentativas. Era cold start pontual, não o modelo.

**Veredito revisto: o critério do §4.3 ("p95 ≤ 8 s com `low`, senão não sobe")
PASSA.** O prompt pode ser ativado.

**Margem real, sem otimismo:** p95 de 6,9 s contra teto de 8 s é **1,1 s de
folga**. Se o contexto crescer (janela maior, resumo acumulado cheio), a
latência sobe junto. Isto precisa ser remedido com sessão real, não com o
contexto sintético da bancada — fica como próxima medição obrigatória.

**Notas tocadas:** [[sic-hf-decisao-copiloto-ao-vivo-bot-na-sala]]

## 14:00 — 🎉 PRIMEIRA SESSÃO AO VIVO PONTA A PONTA. Esteira fechada.

**Feito:** sessão real com bot no Meet, fala de ~2 min simulando cliente, tela
`/conduzir` aberta. **A esteira inteira rodou pela primeira vez.**

| Etapa | Resultado |
|---|---|
| Bot na sala (Deepgram pt-BR) | ✅ |
| Transcrição ao vivo | ✅ **31 segmentos** |
| Webhook → banco | ✅ |
| Tela dispara o ciclo | ✅ gatilho `intervalo`, janela 3 |
| Claim atômica + gate + orçamento | ✅ |
| IA executou | ✅ `concluida` |
| Sugestão gravada | ✅ |

**🔴 ACHADO 1 — o cache muda tudo, e o plano não previa.** Medido:

| | latência | cache |
|---|---|---|
| 1ª chamada | **9.642 ms** ❌ | escreveu 3.127 |
| 2ª chamada | **3.865 ms** ✅ | **leu 3.127** |

**2,5× mais rápida.** A 1ª paga a escrita do prompt em cache; as seguintes leem.
A 1ª estourou o timeout de 8 s e a sugestão foi **descartada** — comportamento
CORRETO (§4.3: "estourou, nada aparece"), não bug. Numa sessão de 90 min só a
primeira é lenta. **Warm-up ao iniciar a sessão resolveria** — a chamada
descartável paga o cache enquanto a advogada ainda se apresenta.

Testei a hipótese de que a fragmentação do Deepgram (31 segmentos curtos)
inflava o contexto: **NÃO é isso** — 31 fragmentos e 1 bloco compactado deram
os mesmos 1.730 tokens e latências equivalentes.

**🔴 ACHADO 2 — `roteiro_versao_id` nasce NULL e cega o copiloto.** A IA
respondeu, corretamente: *"Não há bloco atual ativo no contexto recebido; sem
roteiro carregado, não é possível apontar campos ou perguntas específicas"* —
`tipo: fato`, `confianca_geral: 0.30`, `evidencia: null`.

**Ela se recusou a inventar**, e com 0,30 < 0,6 o servidor **não mostrou na
tela**. É a regra do CLAUDE.md funcionando ("sem evidência, dizer que não há") e
a régua de confiança filtrando.

Causa: `sessoes_viabilidade.roteiro_versao_id` só é preenchido quando alguém
**registra um SIM** (0030). Sessão que nunca teve SIM marcado fica com o campo
nulo, e `montarContextoCopiloto` monta o bloco A vazio — **o copiloto perde o
método, que é a função dele**. Contornado à mão nesta sessão; precisa de
correção no código (resolver o roteiro ATIVO quando a sessão não tem versão
carimbada, sem inventar versão).

**Notas tocadas:** [[sic-hf-decisao-copiloto-ao-vivo-bot-na-sala]]

## 11:03 — PainelCopiloto: hierarquia visual da sugestão (frontend-engineer)

**Pedido:** "a sugestão ficou boa, mas está muito sorrateira... insights e
sugestões precisam ser mais objetivos" + "separadinha ou pontuada entre a
mensagem e a pergunta, com base no que o cliente falou".

**Feito:** `src/components/sessao/PainelCopiloto.tsx:781-914` —
`ApresentacaoSugestao` redesenhado. `proxima_pergunta.texto` virou o herói:
card próprio (`border-l-4` cor `--latao-cta`, marca — não alarme), mesmo
degrau tipográfico (`text-titulo font-bold sm:text-display`) que
`BlocoRoteiro.tsx` já usa para "o que se lê de relance". `motivo` e
`evidencia` saíram de dentro do mesmo parágrafo e viraram elementos
distintos: `motivo` como linha "Por quê:" menor, `evidencia` como
`blockquote` com rótulo próprio ("O cliente disse"). `falta_no_bloco` virou
chips (`Selo tom="neutro"`) em vez de lista vertical — o prompt v2 já entrega
item curto (3-6 palavras). `observacao` desceu para rodapé, com borda
tracejada acima, depois do desvio sugerido — é risco a considerar, não ação.
Nenhum token novo: reusei `Cartao realce`/`text-titulo`/`text-display` já
existentes no DS.

**Medido:** `npm run typecheck` limpo · `npm run lint` 0 erros (2 warnings
pré-existentes em `expurgo.test.ts`, arquivo não tocado) · suíte completa com
`--poolOptions.forks.maxForks=2`: **1212 passed | 1 skipped** — idêntico à
baseline. Nenhuma asserção de teste (`PainelCopiloto.test.tsx`,
`ConduzirSessaoApp.test.tsx`) foi alterada nem enfraquecida.

**Decidido:** não criar componente novo de "card de pergunta" no DS — o
padrão de `BlocoRoteiro.tsx` (border-left + `text-titulo`/`sm:text-display`)
já resolvia o problema de hierarquia; reaproveitado em vez de inventado.

**Pendente com o Marcio:** revisar visualmente (ele publica, não eu commitei).

**Notas tocadas:** [[sic-hf-decisao-copiloto-ao-vivo-bot-na-sala]]

## 14:40 — Diagnóstico: a latência cresce com o TAMANHO DO BLOCO, não com o cache

**Contexto:** eu ia implementar warm-up do cache (a 1ª chamada de cada sessão
estourava os 8 s escrevendo cache). Antes de codar, medi as **8 execuções reais**
da sessão de hoje — e o warm-up resolveria o sintoma errado.

| # | tokens_entrada | latência | cache |
|---|---|---|---|
| 1 | 3.838 | 9.642 ms | escreveu 3.127 |
| 2 | 3.430 | 3.865 ms | leu |
| 3 | 3.592 | 2.766 ms | leu |
| 4 | 3.712 | 3.516 ms | leu |
| 5 | 3.950 | 5.465 ms | leu |
| **6** | **5.461** | **8.948 ms** ❌ | leu |
| 7 | 5.173 | 7.764 ms | leu |
| 8 | 5.231 | 7.034 ms | leu |

**Correlação tokens × latência (ignorando a 1ª, que escreve cache): r = 0,95.**
Praticamente linear: **+1.000 tokens = +2,5 s**.

A execução 6 estourou os 8 s **mesmo com cache quente** — ou seja, o cache
explica a 1ª chamada, mas NÃO explica o resto.

**Causa real: `bloco_atual` vai INTEIRO no contexto, e os blocos têm tamanhos
muito diferentes.** Medido no roteiro v4 ativo:

| bloco | tokens |
|---|---|
| parte_09 | **1.158** |
| parte_10 | 842 |
| parte_03 | 833 |
| parte_01 | 625 |

Conforme a advogada avança pelos 13 blocos, o contexto cresce com o bloco — e
`parte_09` sozinho custa **~1,3 s a mais** que `parte_01`.

**O que NÃO é a causa** (descartado por medição, não por suposição):
- a janela de transcrição: pico de **31 segmentos / 1.030 chars** em 90 s ≈ 260
  tokens, com teto de 40 segmentos já no código;
- a fragmentação do Deepgram: 31 fragmentos e 1 bloco compactado deram os mesmos
  1.730 tokens em teste anterior;
- o resumo acumulado: vazio nesta fatia.

**Correção certa (a fazer):** enxugar o bloco do roteiro no contexto — mandar
objetivo, campos, observar e proibido, mas NÃO as falas inteiras (que são para a
advogada ler na tela, não para a IA). O warm-up do cache continua válido para a
1ª chamada, mas é o segundo problema, não o primeiro.

**Notas tocadas:** [[sic-hf-decisao-copiloto-ao-vivo-bot-na-sala]]

