# Diário — 15/09/2026

## 11:40 — Fase 11: tela de sessão em 3 frentes (layout · latência · papéis de fala)

**Pedido:** Marcio, a partir de um print da tela ao vivo — (1) organizar a
composição visual, porque *"quando o dinamismo começa e os dados começam a ser
exibidos, as divs quebram"*; (2) mapear e reduzir a latência entre transcrição,
pensamento do modelo e resposta; (3) diferenciar as vozes na reunião — quem
conduz, quem é decisor principal, quem são os participantes.

**Feito:** 3 commits em `main` (`c305356`), 33 arquivos, +3.042/−648.

*Layout* (`e08c38b`) — `src/components/sessao/`:
- `recolhido={!dado}` removido dos quadros 3-6 (`PainelCopiloto.tsx:1422,1552,1581,1616`)
- `items-start` → `items-stretch` + `min-h` nas duas grades (`:420`, `:445`)
- teto de 26rem no quadro 1 (`PainelPerguntaAgora`)
- `ultimoNaoNulo()` + `CarimboHoraSeAntigo`
- `QuadroVoceAcertouOuErrou` → `null` (2 call-sites removidos)
- `HistoricoCoach`: `flex-wrap` → `flex-nowrap` (`:1686`)
- `ApresentacaoComparacaoDecisores` renderizada como quadro 7
- `PainelSims.tsx`: blockquote com `max-h-32 overflow-y-auto` + faixa horizontal
  abaixo de `2xl`; `ConduzirSessaoApp.tsx`: grade A alinhada em `2xl`

*Latência* (`39cedf3`) — migration `0102` + `src/server/copiloto/` + `src/server/ia/`:
- `intervalo_segundos` 45→20 · `teto_ia_sessao` 30→90 · `teto_ia_dia` 150→450
- `AbortController` real em `executar-ia.ts` (era `Promise.race`)
- `warmup.ts` chama com `abortarNoTimeout: false`
- double-hop eliminado em `api/sessoes/[id]/copiloto/route.ts`
- `gatilho.ts`: constante duplicada removida, `intervaloSegundos` obrigatório,
  piso da virada 15→8s
- `server/ia/`: `signal?: AbortSignal` aditivo em `tipos.ts`, `executar.ts`,
  `openrouter.ts`

*Papéis de fala* (`51ee5eb`) — migration `0103` + webhook + contexto:
- `TimestampEventoSchema` aceita objeto/string/número
- `participant.id` e `is_host` declarados; `name` opcional
- `resolverPapelNoJoin` em `participantes.ts`, reusando `compararComDecisores`
- `rotuloFalante` consulta o mapa (`contexto.ts:307`)

**Medido (produção, `fcfsnqqaphtamhrpuyoh`):**

| | |
|---|---|
| Latência do copiloto | p50 **3.786 ms** · p95 **9.191 ms** · máx 9.642 ms |
| Execuções | 15 · 14 concluídas · **0 com `status='falhou'`** |
| Custo real | **US$ 0,0065/chamada** (bancada dizia 0,0073) |
| Timeouts escondidos | **2 de 14** estouraram os 8 s e gravaram `concluida` |
| Desperdício | **US$ 0,0245 = 25% do gasto total** |
| Eventos de participante | 10 join + 5 leave, **100% com erro** |
| Sessões com participantes | **0** |
| Segmentos com falante | **128**, todos `"João CSM"` |

`explain (analyze, buffers)` da 0102 (3 UPDATEs): `Index Scan using
configuracoes_pkey`, `rows=1`, 0,872/0,716/0,893 ms.
Da 0103 (INSERT): `Conflict Arbiter Indexes: configuracoes_pkey`, 0,854 ms.

Suíte: **1294 passando** (77 arquivos, 1 skip) · `tsc` limpo · `eslint` 0 erros
(2 warnings preexistentes em `expurgo.test.ts`) · `next build` completo.

**Decidido:**
- **Papel estável, nunca nome, no prompt.** Descartado mandar nome real: mudaria
  a fronteira de PII do §7 e exigiria decisão jurídica nova. O papel resolve sem
  tocar no sigilo.
- **Intervalo e teto movem juntos.** A 45 s o intervalo era a trava; a 20 s o
  teto passa a ser. Baixar um sem o outro deixaria o copiloto mudo na segunda
  metade da sessão, sem erro nenhum.
- **Papel resolvido na ESCRITA**, não na leitura — é o que garante que
  `decisor_1` não troque de pessoa entre ciclos.
- **Cards 7/7 removidos** em vez de renumerados: sempre vazios, e o critério de
  acerto continua sendo decisão de produto pendente.
- **Faixa de SIMs cortada em `2xl`** (não `xl`): `xl` é inclusivo, e 1280/1440
  caíam na coluna. Descartado breakpoint customizado para não sair da escala
  padrão.
- **Streaming e `transcript.partial_data` ficam fora.** Fases próprias.
- **Diarização da Recall NÃO ligada**: `participant.name` já resolve, e não há
  fala sobreposta medida para justificar 1,8× de custo.

**Armadilhas que custaram tempo (e uma que custou produção):**

🔴 **`explain (analyze)` em UPDATE EXECUTA o UPDATE.** Medindo o plano do
primeiro dos 3 UPDATEs da 0102 — como o protocolo exige — gravei em produção. O
estado intermediário (intervalo 20 com teto 30) era pior que qualquer extremo,
então completei os três. Na 0103 usei `begin; explain (analyze) ...; rollback;`
— plano real, nada gravado. **É o padrão para DML daqui em diante.**

🔴 **`falhou = 0` não significa que nada falhou.** `Promise.race` não cancela o
perdedor: a chamada seguia até 300 s, era cobrada, gravava `concluida` e contava
no teto. Só o resultado era descartado.

🔴 **Abortar de verdade mataria o warm-up** — o cache é escrito no voo que
continua depois do timeout (`warmup.ts:145-151`). Precisou de exceção explícita,
com teste cujo nome explica o porquê.

🔴 **Nome de IA não casa com nome de plataforma.** Briefing traz
`"Rodrigo (marido e sócio)"`; a Recall manda `"Rodrigo"`. Decisor presente seria
marcado como ausente na tela.

🔴 **`participant_events.id` é do STREAM, não do evento** — o mesmo id em joins
de 3 pessoas. Como chave de idempotência, engoliria participantes em silêncio.

🔴 **O bot emite join/leave dele mesmo** e viraria `acompanhante_1`. E
`NOME_BOT` tem em-dash enquanto a Recall devolve hífen simples.

🔴 **`items-start` É a causa do reflow**, não a ausência de `min-h`. E persistir
valor antigo sem carimbo de hora troca "dado some" por "dado mente".

**Pendente com o Marcio:**
- **Nada foi visto renderizado.** As alturas dos SIMs (~436 px na faixa, ~692 px
  na coluna) são cálculo sobre o CSS gerado, não captura de tela.
- **Os papéis nunca rodaram com bot real.** Toda validação é teste com o payload
  real como fixture. A primeira sessão ao vivo é o teste de verdade.
- `scripts/a11y.mjs` continua pendente desde 14/09 (exige `next dev` + credencial).
- **Deploy:** o `brain` diz que o remote `infra` publica, mas esta cópia só tem
  `origin`. O push foi para `origin/main`. **Confirmar se publicou** em
  `/versao.txt`; se não, o build na Hostinger é manual.
- `copiloto_sessao.dispensa_gate_juridico = true` — o bot grava sem registro de
  quem autorizou. Decisão de 14/09, registrada; não é desta fase.
- Critério de acerto/erro da condução continua sem definição (bloqueia os cards
  7/7 de voltarem).

**Notas tocadas:** [[SIC-HF - Fase 11 tela de sessao (layout e latencia)]] ·
[[SIC-HF - webhook descarta 100% dos participantes (bug vivo)]] ·
[[2026-09-15 - SIC-HF plano das tres frentes da tela de sessao]] ·
[[2026-09-15 - timeout que nao aborta some da telemetria]] ·
[[2026-09-15 - forma que muda com o dado quebra o layout]] ·
[[2026-09-15 - explain analyze em UPDATE executa o UPDATE]] ·
[[2026-09-15 - nome gerado por IA nao casa com nome de sistema]] ·
[[Recall.ai - bot de reuniao para o copiloto do SIC-HF]]

## 12:15 — Atalho: conduzir a sessão em 1 clique, da tela que ela abre primeiro

**Pedido:** Marcio — *"quero um botão que me leve direto pra sessão do cara, a
gente tá com processos muito burocráticos dentro do sistema."*

**Medido ANTES de mexer** (mapeamento do `src/` inteiro):

| Caminho | Cliques |
|---|---|
| Agenda → "Conduzir" | **2** |
| Clientes → Ficha → gaveta Sessão → passo Presença → botão | **4-5** |
| Tela **"Hoje"** | ❌ **nenhum link** |
| Ctrl+K (paleta) | ❌ sempre cai na Ficha |

Existiam **exatamente 2 links** para `/conduzir` em todo o `src/`
(`LinhaAgendamento.tsx:180`, `SessaoAba.tsx:263`).

A tela "Hoje" já listava a sessão do dia com hora, nome, presença e selo de
preparo — mas o **CTA laranja era "Abrir sala"**, que leva ao Meet, **fora do
sistema**. A advogada saía do sistema pelo botão mais óbvio, e o copiloto
recém-construído ficava para trás.

**Feito** (commit `c90f48f`, merge `bb58951`):

- 🔑 `lib/pasta/proximo-passo.ts:273-291` — **a correção de maior alavancagem**:
  o verbo *"Conduzir a sessão"* **já existia** e apontava para
  `/jornadas/{id}#sessao` (a gaveta de agendamento, a 2 cliques do destino),
  porque `rotas.ts::ITENS_EM_GAVETA` não contém `"sessao"` e o fallback mandava
  para lá. Agora `hrefDoPasso` roteia `sessao` + `dono:advogada` para
  `/sessoes/{jornadaId}/conduzir`. **Corrige 4 consumidores de uma vez**:
  trilho da Ficha, `BarraAcaoMobile`, chip da Hoje, chip da Agenda.
  `dono:equipe` ("Agendar a sessão") continua indo para a gaveta.
- `painel/SessoesHoje.tsx` — "Conduzir" vira ação principal; "Abrir sala" desce
  para secundária. **Zero backend novo.**
- `vw_sessoes_em_aberto` (migration `0104`, **aplicada**) — sessão agendada
  cujo dia passou e não foi marcada como realizada. Janela de 14 dias.
- `onboarding/TourPrimeiraVez.tsx:39` — o tour **prometia** um botão "Conduzir"
  na linha da sessão da Hoje, que nunca existiu. Agora existe.

**Medido depois:**

`explain (analyze, buffers) select * from vw_sessoes_em_aberto` →
`Sort` + `Nested Loop`, **rows=2**, **Execution Time: 0,388 ms**.
Devolve as 2 sessões reais que estavam invisíveis:

| Cliente | Agendada | Dias atrás |
|---|---|---|
| João Pedro (exemplo) | 14/09 | 1 |
| Cláudia (demonstração) | 07/09 | 8 |

Suíte: **1302 passando** (+8), `tsc` limpo, `eslint` 0 erros, `next build` ok.

**Decidido:**
- **View nova, não ampliar `vw_sessoes_do_dia`** — aquela tem contrato fechado
  (`painel-ui.ts`, `agenda.ts`, cálculo de urgência em `PainelDia.tsx`).
- **Janela de 14 dias fixa no SQL**, sem chave em `configuracoes`: não há outro
  consumidor do valor, e criar chave sem consumidor é chave fantasma.
- **Filtrar só por `status`**, não por `realizada_em` — ver armadilha abaixo.

**Armadilhas:**

🔴 **`agendamentos` NÃO tem `realizada_em`** (é de `sessoes_viabilidade`). E a
cardinalidade difere: sessão é 1:1 com a jornada, agendamento é N (remarcação
preserva histórico). Filtrar por `realizada_em` criaria **falso negativo em
remarcação** — sessão com `realizada_em` de uma tentativa antiga e agendamento
novo `confirmado` **deve** aparecer em aberto. O enum `status_agendamento` já
resolve sozinho. *(Erro meu na instrução, corrigido pelo agente.)*

⚠️ **O índice previsto NÃO está provado.** O plano usa `Seq Scan on
agendamentos`, não `idx_agendamentos_proximos` — correto com 4 linhas, mas
anotado na migration para re-medir acima de ~1.000. Mesma regra do vault:
índice pode deixar mais lento (medido em `etapa1_clientes(fase)`).

**Pendente com o Marcio:**
- 🔴 **`LinhaAgendamento.tsx:167,189`** — "Realizada" fica ao lado de
  "Conduzir", **sem confirmação**. Um clique errado tira o `status` de
  `agendado/confirmado` e a linha some das DUAS views: nem "hoje", nem "em
  aberto". **O caminho curto morre de forma irreversível pela UI.** Não mexi:
  é mudança de fluxo, decisão de produto.
- ⚠️ **`roteiros_versoes` ativo é bloqueio TOTAL sem tela de desbloqueio.** Sem
  versão ativa de `sessao_viabilidade`, **nenhuma** sessão pode ser conduzida no
  sistema inteiro, e o texto de erro admite que o Admin não tem a tela — exige
  dev. Conferido: **v4 está ativa**, risco latente, não atual.
- `SessaoAba.tsx:80,86` — inversão lógica: `feitos.presenca =
  Boolean(realizada_em)`, então o passo "Presença" (que contém o botão de
  conduzir) **só acende sozinho depois** de a sessão já ter sido realizada.
- **Produção continua 47+ commits atrás** — o deploy não é automático.

**Notas tocadas:**
[[SIC-HF - o caminho ate conduzir a sessao (burocracia medida)]] ·
[[SIC-HF - producao esta 47 commits atras (deploy nao automatico)]]

## 14:45 — Sessão imediata: "iniciar agora" ou "agendar", à moda do Meet

**Pedido:** Marcio — *"deixa intuitivo pra iniciar uma sessão imediata, igual no
Google Meet, com opção de agendar e iniciar uma imediata."*

**A barreira que o Meet não tem, medida antes de desenhar:**

```
configuracoes['sala.provedor'] = "manual"
```

**O SIC-HF não cria sala.** O link do Meet/Zoom é colado à mão. O mecanismo
automático (n8n) está desligado e, mesmo ligado, tem 3 limites: roda **por
cron** (não sob demanda), filtra `inicio_em > agora` — **só sessões futuras**
(`server/sala/sincronizar.ts:66`) — e é **assíncrono** (pede ao n8n, espera
webhook).

"Clicar e ter uma sala" é impossível hoje sem construir integração.

**Feito** (commit `5322fdf`):

- `components/agenda/IniciarSessaoAgora.tsx` (novo, 287 linhas) — busca de
  cliente reusando `listarJornadas`/`jornadaParaOpcao` da `PaletaComandos`,
  campo de link reusando `linkSalaValido`/`gravarLinkSala` da Ficha,
  redireciona para `/sessoes/{jornadaId}/conduzir`. **Nenhum endpoint novo.**
- O par de ações aparece nas **duas** telas de entrada: `agenda/page.tsx`
  (onde "Marcar sessão" já era o CTA e recuou para secundário) e
  `painel/PainelDia.tsx` — a Hoje é a tela que ela abre primeiro, e deixar o
  atalho só na Agenda custaria o clique de navegação que o pedido existe para
  eliminar. **Mesma instância do componente** (autocontido, sem props): não há
  duplicação.
- `api/jornadas/[id]/agendamentos/route.ts` — `fim_em` virou **opcional**; o
  servidor calcula com `agenda.duracao_padrao_minutos` (60, chave que **já
  existia** e já era consumida por `POST /api/disponibilidades`). O cliente não
  lê `configuracoes`.

**Decidido:**
- **"Iniciar agora" = colar o link + começar.** Descartado gerar a sala
  automaticamente (exigiria ligar o n8n, criar fluxo sob demanda e tratar
  espera assíncrona — feature própria). O caminho escolhido funciona hoje.
- **A sessão imediata cria agendamento com `inicio_em = agora`.** Descartada a
  sessão avulsa: ela sumiria da agenda, das métricas e da esteira. Reusa o
  endpoint que **já cria** `sessoes_viabilidade` quando não existe. Zero
  conceito novo no banco.
- **Sem link, a sessão começa igual** — só o bot do copiloto não tem onde
  entrar. Dito numa linha de ajuda, sem bloquear e sem virar alerta grande.

**Armadilha esclarecida por medição:** a `EXCLUDE CONSTRAINT`
(`0008_sessoes_agendamentos_relatorios.sql:36`) trava por **advogada × intervalo
de horário**, **não** por jornada/sessão. Então iniciar "agora" para um cliente
que já tem sessão futura **não colide** — cria um 2º agendamento para a mesma
sessão (mesmo mecanismo 1:N da remarcação). **O agendamento futuro não muda de
status sozinho**: quem cancela ou remarca é a equipe, na linha da Agenda. A
colisão real (409 `horario_indisponivel`) só ocorre com outro agendamento da
mesma advogada sobre o instante "agora", e a mensagem diz com o que colidiu.

**Verificado:** 1308 testes (+6) · `tsc` limpo · `eslint` 0 erros ·
`next build` completo. Testes cobrem os 3 requisitos duros: redirecionamento por
`jornada_id` (nunca `sessao_id`), mensagem específica na colisão 409, e iniciar
sem link de sala.

**Pendente com o Marcio:**
- **Nada foi visto renderizado** — o fluxo nunca foi clicado de verdade.
- No **celular**, "Marcar sessão" continua isolado na `BarraAcaoMobile` de
  `ListaSessoes.tsx`; "Iniciar sessão agora" não foi para lá (dois CTAs na zona
  do polegar é decisão de UX não coberta).
- Se colar o link virar o atrito real, a integração de sala (n8n sob demanda)
  vira a próxima feature — com o problema já medido, não suposto.
- **Produção continua 47+ commits atrás**; nada disto está no ar.

**Notas tocadas:**
[[2026-09-15 - SIC-HF sessao imediata (modelo Google Meet)]] ·
[[SIC-HF - o caminho ate conduzir a sessao (burocracia medida)]]

## 16:15 — Correção dos dois riscos declarados (pedido do Marcio)

**Pedido:** *"faça as correções com base nos riscos, me ajuda nisso aí"* — os
dois itens que ficaram como "pendente com o Marcio" nas entradas anteriores.

**Feito** (commit `2722870`):

**1. "Realizada" passou a confirmar** (`agenda/LinhaAgendamento.tsx`)

O efeito é **irreversível pela UI**: saindo de `agendado`/`confirmado`, a linha
perde "Conduzir" e a sessão some das **duas** views (`vw_sessoes_do_dia` e
`vw_sessoes_em_aberto` — nenhuma traz `realizado`). Só volta por SQL. E os dois
botões ficam lado a lado.

Reusa `ui/ConfirmarAcao`, que **já estava importado no arquivo** para o
"Cancelar" — mesma trava, mesmo padrão. A regra dele vale: *efeito por extenso,
nunca "tem certeza?"*. O texto avisa que se perde o atalho de conduzir.

**2. "Conduzir sessão" saiu do passo 4** (`ficha360/SessaoAba.tsx`)

`feitos.presenca = Boolean(realizada_em)` + passo aceso = primeiro não
concluído ⟹ o passo "Presença", dono do **único** botão de conduzir da Ficha,
**só acendia depois de a sessão já ter sido realizada**.

Virou rodapé do cartão, com `proximo !== null` (sem agendamento, `/conduzir`
cairia em "sem-sessão"). **O stepper não mudou** — `etapaAtual` continua sendo
o passo real, e o aviso sobre `ui/Passos` derivar "feito" por índice segue
válido. Só o botão saiu de dentro dele.

**Medido:** 1318 testes (+10) · `tsc` limpo · `eslint` 0 erros · build completo.

Os 10 testes cobrem o que importa, não a implementação: clicar em "Realizada"
**não** chama a API; o diálogo diz o efeito e cita "conduzir"; cancelar não
toca no status; e o botão de conduzir aparece **sem nenhum clique no stepper**,
uma vez só, com o id da **jornada**.

**Verificação extra que rendeu um susto e uma lição**

Fui conferir no CSS gerado se a geometria da Fase 11 (`min-h-[7rem]`,
`min-h-[9rem]`, `max-h-[26rem]`) tinha compilado. Primeira busca: **zero
ocorrências** — o que significaria cards sem piso nem teto, ou seja, a quebra
de volta. Rebuild limpo, mesma coisa.

Era **escape de regex errado no meu grep**, não ausência no CSS. Buscando sem
escape, as três estão lá. ⚠️ **Falso negativo de ferramenta parece bug de
produção** — confirmar o método de busca antes de concluir que o código sumiu.

**O que continua NÃO verificado:**
- **Nada foi visto renderizado.** Não há `.env.local` nesta máquina, e
  `scripts/a11y.mjs` exige credencial de produção — caminho que a regra da casa
  proíbe para agente. O axe dos testes é jsdom, e o próprio helper
  (`ui/a11y-teste.tsx`) declara: *"verde aqui é piso, não prova"* — contraste
  não é medido e ordem de foco depende da página inteira.
- **Produção continua 47+ commits atrás** — nada disto está no ar.
- No celular, "Iniciar sessão agora" ainda não está na `BarraAcaoMobile`.

**Notas tocadas:** [[SIC-HF - o caminho ate conduzir a sessao (burocracia medida)]]

## 17:45 — "Mete marcha, corrige tudo": e metade dos riscos era medição errada

**Pedido:** Marcio — *"mete marcha aí, corrige tudo"*, sobre os riscos abertos.

**O padrão que apareceu:** de 5 riscos investigados, **3 não eram problema do
sistema — eram medição errada minha ou sinalização desatualizada.** O sistema
estava mais são do que os próprios avisos dele diziam.

### 1. 🔴 Corrida em `participantes` — risco REAL, corrigido (`05be09a`)

`registrarEventoParticipante` fazia read-modify-write **sem trava**. Dois
`join` simultâneos = o segundo write apaga o primeiro. A Recall entrega em
rajada (10 joins + 5 leaves numa sessão, vários no mesmo segundo).

RPC `registrar_participantes_copiloto` (migration `0105`, **aplicada**): faz
**só** o lock (`for update`) e o CAS. O merge continua em TypeScript — fazer em
PL/pgSQL duplicaria herança de papel, filtro do bot e casamento com decisores,
lógica que mudou HOJE. Duas cópias divergindo em silêncio é o risco que a nota
da 0093 já registra.

`entrada-bot.ts` ganhou loop de retentativa, mesmo formato do
`MAX_TENTATIVAS_ORDEM` que já existia ali.

**Medido e PROVADO em produção, em `begin; ... rollback;`:**
- `LockRows` sobre `Index Scan using sessoes_copiloto_pkey` — **0,306 ms**
- esperado correto → `aplicado=true` · esperado errado → `aplicado=false` +
  estado atual devolvido, sem sobrescrever
- rollback conferido: `participantes` voltou a `[]`

### 2. ✅ "Bloqueio sem interruptor" do roteiro — NÃO existia (`dadf53f`)

Eu classifiquei como bomba sem interruptor. **A aba já existe e já ativa**:
`admin/abas/RoteirosSecao.tsx` tem "Ativar esta", com `ConfirmarAcao` e trava
extra (recusa versão sem a fala de sigilo, para não travar sessão em
andamento). Nasceu na Fase 7 r3.

O errado era a **sinalização**: `ConduzirSessaoApp.tsx:212` ficou congelado no
mundo anterior à aba, e o botão de escape ia para `/admin#prompts` — **hash
errado**. Corrigido texto e destino. Nada foi construído: não havia o que.

### 3. ✅ "Iniciar sessão agora" no celular (`dadf53f`)

CTA da `BarraAcaoMobile`; "Marcar sessão" recua para o slot `secundaria` que o
componente já reservava. Descoberto de passagem: o cabeçalho da Agenda mostrava
o mesmo botão no mobile — apareceria **duas vezes**. Escondido.

### 4. 🔴 `versao.txt` não media nada (`7c7b040`)

**Era carimbado à mão** (`echo <hash> >` + commit "Carimba X"). Última vez:
06/09. O `CLAUDE.md` manda conferi-lo depois de cada push — e a regra foi
seguida, e ninguém percebeu, **porque o arquivo sempre "confirmava"**.

Agora: `scripts/carimbar-versao.mjs` no `prebuild` (hash do git + timestamp) e
`GET /api/versao`, pública — arquivo em `public/` é servido pelo servidor web;
a rota roda no **processo Node**. Distinção que importa num projeto com
histórico de *"o processo que atende não é o build que foi enviado"*.

### 5. Duas afirmações minhas que estavam ERRADAS

🔴 **"Produção está 47 commits atrás"** — não sei disso. Veio do `versao.txt`,
que não mede nada.

🔴 **"O 404 do `/api/diagnostico` prova build velho"** — não prova. A rota
devolve 404 **de propósito** para quem não manda `x-cron-secret`
(`route.ts:50-55`). Esconder-se não é ausência.

**Notícia boa:** o "processo Node órfão" de 03/09 **foi resolvido**. Medido
hoje: `/login` 200 com HTML real, `/hoje` e `/agenda` 307, `/api/painel` 401.

### 6. 🎯 Pela primeira vez, algo VISTO rodando

Subi `next start` na 3123 e testei de verdade:
- `/api/versao` → `{"commit":"6bff2e0","build":"...","agora":"..."}`
- `/login` 200 · `/hoje` 307 · `/agenda` 307 · `/api/painel` 401 — **idêntico a produção**
- **baixei o CSS servido** e conferi as 5 classes de geometria da Fase 11
  (`min-h-[7rem]`, `min-h-[9rem]`, `max-h-[26rem]`, `items-stretch`,
  `max-h-32`), com `2xl` = 96rem e `min-h-11` preservado

Não é mais inferência sobre arquivo em disco: é o servidor entregando.

### Armadilhas do dia

⚠️ **Falso negativo de grep parece bug de produção.** Busquei as classes de
geometria no CSS com escape de regex errado → **zero ocorrências**. Rebuild
limpo: zero de novo. Estavam lá o tempo todo. Antes de concluir que o código
sumiu, **prove que a busca funciona** (procure algo que você sabe que existe).

⚠️ **Dois agentes na mesma working tree saturam a CPU e viram falha de teste.**
A suíte deu 3 a 27 falhas em rodadas diferentes, sempre timeout de `axe`
(8-13 s onde o normal é 200-600 ms), **nunca as mesmas suítes**, incluindo
arquivos intocados. Isolados: 100% verdes. Sem concorrência: **1320 testes, 81
arquivos, 0 falha.** Falha instável com tempo anômalo = suspeitar do ambiente
antes do código.

**Verificado:** 1320 testes · `tsc` limpo · `eslint` 0 erros · build completo.

**Pendente com o Marcio:**
- 🔴 **`dispensa_gate_juridico=true`** — auditado a fundo. Hoje o bot da Recall
  (subprocessador fora do Brasil) entra em **qualquer** sessão e grava família
  discutindo patrimônio e herança **sem registro, em lugar nenhum do banco, de
  quem autorizou nem de consentimento do titular**. Revogar consentimento
  **não interrompe nada**. O SQL de religamento no cabeçalho da 0099 **foi
  conferido e continua aplicável**. É decisão sua, de 14/09 — mas agora está
  medida, não estimada.
- Os 3 `A MEDIR` pré-existentes (`contexto.ts:344`, `gatilho.ts:110`,
  `0091:132`) têm script de verificação no repo e nunca foram rodados.
- **Nada da tela de sessão foi visto renderizado** — só o CSS e as rotas.
- Qual build está em produção **continua desconhecido** até o próximo deploy
  com o carimbo novo.

**Notas tocadas:**
[[SIC-HF - producao esta 47 commits atras (deploy nao automatico)]] ·
[[SIC-HF - o caminho ate conduzir a sessao (burocracia medida)]] ·
[[2026-09-15 - falso negativo de grep parece bug de producao]]
