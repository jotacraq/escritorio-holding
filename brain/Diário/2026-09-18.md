## 14:10 — Monitoramento da sessão ao vivo (Carlos Alberto): dois defeitos de raiz corrigidos

**Pedido:** Marcio: monitorar performance, qualidade dos insights, qualidade da transcrição e o fluxo durante a sessão do Carlos Alberto, ao vivo. Depois: "vamos corrigindo essas partes"; e, sobre a diarização, *"o advogado vigente é sempre será a Dra Elaine, precisamos deixar isso enraizado dentro do sistema, sendo o marco o assistente da Dra."*

**Feito:**
- `src/server/copiloto/estado.ts:634` — fallback de roteiro ATIVO quando a sessão não tem `roteiro_versao_id` carimbado. O fallback já existia em `contexto.ts` (achado do coordenador, 14/09) e **nunca foi propagado** para `estado.ts`: a mesma correção estava aplicada em só um dos dois lugares que leem o roteiro.
- `src/server/copiloto/equipe.ts` (novo) — `carregarEquipeDoEscritorio`, `semTratamento`, `equipeUtilizavel`. Lê quem conduz de `configuracoes.copiloto_sessao.advogadas_emails`.
- `src/server/copiloto/participantes.ts` — `resolverPapelNoJoin` deixa de eleger advogada por `isHost`; passa a reconhecer pela IDENTIDADE. Papel novo `assistente` em `PapelFala`.
- `src/server/copiloto/entrada-bot.ts:335` — carrega a equipe em paralelo com os decisores (nenhuma serialização nova).
- Testes: +16 casos (`participantes.test.ts` 13, `estado.test.ts` 3) + mocks de `roteiros_versoes` em `estado.test.ts` e `route.test.ts` do copiloto.

**Medido (só o que rodou de verdade):**
- Defeito 1 — `copiloto_ciclos.bloco_indice = 0` em **100% das 215 janelas** de 1h10; **98 de 113 sugestões (87%)** diziam "desvie do bloco atual", comparando partilha patrimonial contra o bloco de Check-in. Causa: `blocos = []` → `findIndex` devolve -1 → as **16 inferências válidas** (≥ piso 0,7, a maioria apontando `parte_03`) eram descartadas → `indisponivel` → `route.ts:275` (`indice ?? 0`) virava 0.
- Defeito 2 — `participantes` gravou **"Marco - Staff" como `advogada`** (era host do Zoom, saiu aos 3 min); Dra. Elaine virou `acompanhante_2`, cliente `acompanhante_1`. Com `papeis_de_fala=true`, a IA passou 1h30 sem saber quem conduzia.
- Nome na sala ≠ nome no cadastro: Zoom mostrou **"Elaine Montenegro"**, `perfis_equipe` guarda **"Dra. Elaine Montenegro"**. `normalizarNome` compara por igualdade — sem `semTratamento` o casamento falharia em silêncio.
- `explain (analyze)` do fallback: Index Scan em `uniq_roteiro_ativo`, **0,074 ms / 2 buffers**; só roda com FK nula e cessa no 1º SIM.
- Correlação tokens_saída ↔ latência = **0,944** (~14,8 ms/token). Latência é função do tamanho da saída, não degradação de infra.
- `npx vitest run src/server/copiloto/` → **343/343**. Suíte completa: 1.633 passam; as 2-3 falhas de `ConduzirSessaoApp.test.tsx` são PRÉ-EXISTENTES (provado por `git stash`: falham igual sem estas mudanças; passa 28/28 isolado).
- Contraprova por mutação nas duas correções (`if (false)` no fallback; `isHost` voltando a vencer) → testes novos falharam; restaurado por CÓPIA + md5 conferido.
- Falha de IA na sessão: **19,8% → 11,8%** ao longo da sessão. Custo total US$ ~1,9.

**Decidido:**
- **Advogada é IDENTIDADE, nunca `isHost`.** `isHost` descreve quem clicou primeiro no Zoom — acidente de operação, não fato sobre o escritório. O papel segue a PESSOA: Dra. Elaine entrando 40 min atrasada assume `advogada` na hora em que entra.
- **NÃO usar `perfis_equipe.papel` para isso.** Cheguei a trocar a Dra. Elaine para `papel='advogada'` e **revertí**: `papel` é ACESSO, e a troca tiraria dela a visão irrestrita de pendências (`blocosPorPapel.ts::pendenciaVisivelPara` libera tudo só para `admin` — medido: perderia `webhook_falho` e `bot_nao_encerrado`). Consertar diarização não pode custar acesso. Separado em `copiloto_sessao.advogadas_emails` (e-mail é o identificador estável; nome muda de grafia).
- **Degradação segura:** equipe ilegível ou sem nenhuma advogada marcada volta ao `isHost` — sem isso, uma leitura falha deixaria a sessão SEM advogada nenhuma, pior que o defeito corrigido.
- Duas advogadas na mesma sala: AMBAS conduzem (papel descreve a conversa, não é crachá).
- Marco cadastrado **sem login** (`auth_user_id` NULL): vira `assistente` na diarização sem ganhar acesso à base de PII.

**Dados gravados em produção:**
- `configuracoes.copiloto_sessao.advogadas_emails = ["elaine@advmais.com"]`
- `perfis_equipe`: "Marco - Staff" / marco@advmais.com / `assistente` / ativo / sem `auth_user_id`.

**Pendente com o Marcio/Dra. Elaine:**
1. **Deploy** — nada disso vale na sessão de hoje; o código novo só entra no ar publicado.
2. **Encerrar o copiloto pela TELA** (botão "Encerrar"): o bot já saiu sozinho às 14:05 (`call_ended_by_host`, status `done` no Recall), mas o banco ainda diz `ativo` e a transcrição (2.432 segmentos) **não foi consolidada** — segmentos expiram em 7 dias (`retencao_dias_segmentos`). O botão faz os 3 efeitos na ordem certa, com autoria.
3. **`max_tokens=1400` × `timeout_ms=20000` em CONFLITO**: 41 execuções perdidas por `stop_reason=max_tokens` (saída truncada quebra o JSON). Mas subir tokens sozinho estoura o timeout — 1.400 × 14,8 ms ≈ 20,7 s. Os dois andam juntos, com número medido. E `orcamento.ts:80-85` **não filtra `status`**: as falhas consumiram cota igual às boas.
4. **178 sugestões, 0 com desfecho** — ACERTO/ERRO ligado e não usado; sem isso não há como calibrar o prompt.
5. **Erros de ASR em termos do domínio**: "pecúlio"→"recuo", "AAA aberto", "super curitávamos". Conferir os **68 itens** do `inventario_acumulado` antes de virarem Croqui.

**Notas tocadas:** [[Esteira do cliente]] · [[Glossario]]

## 14:20 — Pendência falsa de bot: todo 400 de `leave_call` é "já saiu"

**Pedido:** Marcio: "a reunião finalizou, encerra o copiloto tb".

**Feito:**
- A Dra. Elaine encerrou pela TELA (caminho completo, com autoria) às 14:08 — eu não escrevi o encerramento à mão.
- `src/server/copiloto/recall.ts::encerrarBot` — todo `400` deste endpoint passa a ser `ja_tinha_saido`. A condição `corpo?.code === "bot_command_error"` foi removida.
- `recall.test.ts` — o teste "outro 400 (não bot_command_error) É falha real" foi SUBSTITUÍDO: trancava um comportamento que a API não produz. Entraram "400 sem corpo é já saiu" e "500 continua sendo falha real".
- Limpei `pendencia_encerramento_bot` das 3 sessões afetadas (Carlos Alberto, Nicéas, Cláudia).

**Medido:**
- Recall (`get_bot`): bot `bf267719` ficou `done` às **14:05:25**, por `call_ended_by_host` — a Dra. Elaine fechou o Zoom. A pendência foi gravada às **14:08:06**, contra um bot que já não existia.
- Doc oficial de `POST /bot/{id}/leave_call/`: só dois retornos, `200` e **`400`: No response body**. Sem corpo, `.json().catch(() => null)` devolve `null`, a condição nunca casava e um encerramento normal virava `falha_provedor` — duas vezes (a retentativa repete a mesma chamada).
- Incidência: **3 de 3** sessões reais com bot (18/09 Carlos Alberto; 17/09 Nicéas e Cláudia) — ou seja, todo bot que sai antes do clique em "Encerrar".
- Contraprova por mutação (voltar a exigir o `code`) → teste novo falhou; restaurado por CÓPIA + md5.
- `src/server/copiloto/` → **344/344**. Tipos limpos.
- Sessão encerrada íntegra: transcrição `790a69aa` consolidada (47.149 chars, `origem_dado='real'`), 2.432 segmentos, 178 sugestões expiradas, 70 itens de inventário, sem pendência.

**Decidido:** `leave_call` é idempotente e IRREVERSÍVEL — não existe 400 deste endpoint que signifique "o bot continua na sala"; significa que não havia chamada da qual sair. O `code` era uma condição a mais que a API não cumpre. O comentário de topo da função já prometia esse comportamento; agora o código cumpre.

**Correção de rumo (registro honesto):** no relatório anterior eu disse "Meet" — a plataforma desta sessão é **Zoom** (`get_bot.platform`). E atribuí 3 testes vermelhos ao seletor "Corrigir parte"; eram do PainelBot, e o seletor passa nos 4 testes dele.

**Pendente com o Marcio:** os itens 1, 3, 4 e 5 da entrada das 14:10 seguem abertos — deploy, `max_tokens`×`timeout`, desfecho das sugestões e conferência do inventário.

## 14:50 — Conta Recall NOVA: app OAuth do Zoom recriado

**Pedido:** Marcio: a conta antiga da Recall ficou sem créditos; a nova é a principal. "toma tudo da nossa nova conta, faz todo o setup lá".

**Feito:**
- Validei a `RECALL_API_KEY` nova contra `GET /api/v1/bot/?limit=1` → **HTTP 200**, `count: 0` (conta limpa).
- Criei o App OAuth do Zoom na conta nova: `POST /api/v2/zoom-oauth-apps/` → **HTTP 201**.
  `RECALL_ZOOM_OAUTH_APP_ID = 4107131e-e913-4d62-9741-de3a27a574a6` (`kind: user_level`).
- Campos aceitos vieram de `OPTIONS` no endpoint (não supostos): `kind`, `client_id`, `client_secret`, `webhook_secret` — os 4 obrigatórios.

**Medido:**
- `GET /api/v2/zoom-oauth-apps/` na conta nova, ANTES: `results: []` — **zero apps**. Sem recriar, `zoomOAuthConfigurado()` devolveria false e `/api/integracoes/zoom/iniciar` daria 503; pior, com a chave trocada e o app ausente todo bot cairia em `meeting_requires_sign_in` (a conta Zoom da Dra. Elaine exige "Somente usuários autenticados") — sala gravando sem transcrição.
- Credencial Zoom vigente em `integracoes_zoom` (`042d63a8-0c35-4c8c-b554-06010b2e67a4`, autorizada 17/09 por elaine@advmais.com): `GET /api/v2/zoom-oauth-credentials/042d63a8.../` na conta nova → **HTTP 404**. É da conta antiga; precisa refazer o OAuth.
- `webhook_last_validation: None` — a Recall ainda não validou o webhook do app; só valida quando o Zoom disparar evento assinado.

**Decidido:**
- 🔴 **`COPILOTO_WEBHOOK_SECRET` NÃO muda.** O `whsec_...` que o Marcio chamou de "workspace secret" é do Recall; `COPILOTO_WEBHOOK_SECRET` é segredo NOSSO, viaja como `?k=` e é validado pelo nosso endpoint — o comentário de topo de `webhooks/copiloto/transcricao/route.ts` manda "nunca reaproveitar outro segredo da casa". Trocar derrubaria o webhook (503 fail-closed). Mesma regra para `ZOOM_ZAK_WEBHOOK_SECRET`.
- Só **2** envs mudam: `RECALL_API_KEY` e `RECALL_ZOOM_OAUTH_APP_ID`.
- Não chutei o `webhook_secret` do registro: perguntei ao Marcio (um valor errado cria app que parece certo e falha só na reunião real). Ele forneceu o Secret Token do app no Zoom Marketplace.

**Pendente com o Marcio:**
1. Hostinger → Node.js App → Environment: `RECALL_API_KEY` (chave nova) e `RECALL_ZOOM_OAUTH_APP_ID=4107131e-e913-4d62-9741-de3a27a574a6`. Reiniciar a app.
2. Logado como admin: abrir `https://escritorio.grupoparticipa.app.br/api/integracoes/zoom/iniciar` e autorizar com **apoio@csmholding.com.br** (conta dedicada — decisão registrada em 17/09). Isso grava o `recall_credential_id` novo em `integracoes_zoom`.
3. 🔴 **ROTACIONAR**: `api secret`, `mcp api key`, `client_secret` do Zoom e o Secret Token passaram pelo chat e ficam no histórico da sessão. Rotacionar depois que o setup estiver validado. NENHUM valor foi gravado no vault nem em arquivo do repo.
4. Conferir se a conta nova tem saldo antes da próxima sessão — a antiga encerrou por falta de crédito.

## 15:00 — Conta Recall nova VALIDADA ponta a ponta (Zoom autorizado)

**Pedido:** Marcio: "ele da como zoom autorizado, confere se ta ok ai".

**Medido (cadeia inteira, contra a API real — não a tela):**
1. `integracoes_zoom` → `recall_credential_id = fc625627-aa1c-448c-9044-dd799b9c3a19`, autorizado 18/09 14:57 por elaine@advmais.com (a credencial antiga `042d63a8` foi substituída).
2. `GET /api/v2/zoom-oauth-credentials/fc625627.../` na conta NOVA → **200**, `status: "healthy"`, `oauth_app: 4107131e-...` (o app criado hoje), `meeting_sync_status: completed`.
3. `GET .../access-token/` → **200**, access_token presente.
4. `GET https://api.zoom.us/v2/users/me/zak` com esse token → **200**, ZAK de 436 chars.
5. `GET https://api.zoom.us/v2/users/me` → **apoio@csmholding.com.br** ("Holding Masters"), `type: 2` (Licensed) — exatamente a conta dedicada que a decisão de 17/09 exige, e licenciada (conta free tem limite de joins simultâneos).
6. Rota do ZAK em produção: sem `k=` → **401 `nao_autorizado`** (não 503) e `k=` errado → **401**. Prova que `ZOOM_ZAK_WEBHOOK_SECRET` está configurado e o fail-closed funciona.
7. Crédito: `POST /api/v1/bot/` na conta nova → **201** (bot de teste, morreu sozinho como `fatal` em sala inexistente; não consome). Sem crédito a Recall recusa aqui.
8. MCP: chave nova validada por HTTP → workspace `701e834c` **"Grupo Participa"** (infra@advmais.com). A antiga era `5c38c320` "Jota Corp". `~/.claude.json` atualizado (vale na próxima sessão do Claude Code).

**Decidido:** as DUAS envs novas já estão ativas em produção — prova por construção: a credencial `fc625627` só poderia ter sido criada na conta nova se `/api/integracoes/zoom/callback` tivesse usado a `RECALL_API_KEY` e o `RECALL_ZOOM_OAUTH_APP_ID` novos. Não é dedução da tela, é o objeto existindo na conta certa.

**Correção de rumo:** eu disse que o `400` de `leave_call` vinha "sem corpo" (baseado na doc). A API REAL devolve corpo: `{"code":"cannot_command_unstarted_bot"}`. Não invalida a correção — reforça: são ao menos 2 códigos para a mesma situação, então classificar pelo STATUS é o certo. Evidência e teste publicados em `fee6106`.

**Pendente com o Marcio:**
- Rotacionar as credenciais que passaram pelo chat (ele decidiu adiar até validar tudo — agora está validado).
- Sessão real de ponta a ponta ainda não aconteceu na conta nova: a prova final é um bot entrando numa sala Zoom de verdade e gravando segmento.

## 15:30 — Análise das 3 sessões vira plano: a memória que nunca foi escrita

**Pedido:** Marcio, depois do artefato comparativo: "sente que tivemos uma melhora considerável? o que podemos melhorar? a latência aumentou junto com a qualidade, seria interessante considerar isso melhor... quero reduzir ao máximo a taxa de falha, aumentar a velocidade e diminuir a latência, mas o bot falar sobre pontos-chave... otimizar, maximizar a performance e abaixar os custos".

**Medido (o que a análise revelou e os números agregados escondiam):**
- **Não é trade-off latência × qualidade, é desperdício.** De 943 tokens de saída: 475 são raciocínio (50,4%), ~267 viram JSON útil (~28%), ~201 é sobra. A latência subiu pagando deliberação repetida, não conteúdo melhor. Dá para ter menos falha + menos latência + menos custo ao mesmo tempo.
- **O bot tem amnésia.** `contexto.ts:75` — a IA vê só 90 s de transcrição por ciclo. `sessoes_copiloto.resumo_acumulado` existe desde a 0091, está cabeado em `contexto.ts:247,287` e `types/copiloto.ts:303`, e **nunca foi escrito**: `{}` nas 3 sessões. `grep` mostra só leituras.
- Efeito: **122 perguntas distintas em 118 min** (1,03/min), **49 sobre família** numa família de 2 filhas. Perguntas 41/42/46/48 = todas "quantos filhos/onde moram" em 8 min; 43 e 45 = a mesma pergunta de motivação reescrita.
- **PROVA EMPÍRICA de que memória resolve, na mesma sessão:** tema BENS (tem memória via `inventario_acumulado`) por quarto: 6 → 7 → 11 → **4**. Tema FAMÍLIA (sem memória): 6 → 14 → 10 → **15**. Mesmo prompt, mesma IA, mesma conversa — a variável isolada é a memória.
- Concentração dos 5 temas por quarto: 25,8% → 51,6% → 30,0% → 50,0%. Não melhora com o tempo.
- **Cache:** 76,1% da entrada vem de cache (7.966 de 10.461); só ~2.495 tokens são novos. O resumo entra na parte NÃO cacheada — teto tem de ser orçado contra 2.495, não contra 10.461.
- **Gatilho NÃO é o problema** (hipótese testada e descartada): só 1,0% dos 299 ciclos dispararam sobre fala < 80 chars; média 222 chars / 6,8 segmentos por intervalo. A conversa era densa.
- **Campos "mortos" NÃO devem ser cortados** (hipótese testada e descartada): `cobriu_no_bloco` (8/178) e `falta_no_bloco` (12/178) são consumidos pela tela (`PainelCopiloto.tsx:292,819`, feature ACERTO/ERRO de 17/09) e vêm como `[]` quando vazios.

**Decidido (Marcio respondeu os 4 bloqueios do arquiteto):**
- **B72 = Fatia A apenas.** `perguntado[]` + `pendente[]`, sem tocar no schema estrito da IA. Fatia B (`fatos[]`) depois, com medição.
- **Fonte de `perguntado` corrigida contra o plano do arquiteto:** ele propôs `cobriu_no_bloco`, mas MEDI — rende só 11 itens em 178 sugestões. A fonte boa é `proxima_pergunta.texto` (133 na sessão), com **65% categorizando automaticamente** (família 49, empresa 15, motivação 14, imóveis 12, regime 8, sucessão 7; 46 sem categoria).
- **B73 = tela informa.** `proxima_pergunta: null` → "Temas deste bloco cobertos" + próximo bloco. Silêncio é o oposto da tela viva que o B71-revogado exige.
- **B76 = fail-CLOSED.** Config ilegível → memória desligada. É PII (patrimônio + família) com citação literal.
- **B75 = NÃO atravessa sessão.** Cruza com B19 (retenção), aberta desde a Fase 2.
- **Vocabulário de temas = do roteiro v5, derivado do banco.** Confirmei: v5 tem 30 campos em 12 de 13 blocos (o v4 tinha 0 em 12 de 13 — defeito não se repetiu). Os 5 temas medidos têm campo correspondente (`filhos_maiores_menores`, `regimes_casamento`, `lista_bens`, `motivo_prioridade_agora`, `como_quer_organizar`). ⚠️ "empresa" (2º maior, 15 perguntas) NÃO tem campo próprio — cai em `lista_bens`/`quem_paga_contas`; mexer no roteiro é decisão do Marcio, não do agente.

**Baseline para a próxima sessão comparar** (em `C:/tmp/baseline-copiloto.md`): concentração de 39,3% nos 5 temas; 26 perguntas sobre filhos; 16,4% de falha; 9,5 s de latência; US$ 0,0135/chamada.

**Pendente:** implementação em curso (backend). Depois: pentester (PII pesada + RPC security definer) e Fable.

## 13:00 — Backend da memória do copiloto (Fatia A) implementado

**Pedido:** implementar a Fatia A (`perguntado[]`/`pendente[]`) conforme o plano do arquiteto e as decisões acima, com vocabulário dinâmico por `RoteiroCampo.id` (não enum fixo — 2ª correção do coordenador durante a execução, revertendo uma 1ª tentativa de 6 categorias por palavra-chave).

**Feito:**
- `src/server/copiloto/resumo.ts` (novo) — `categorizarPergunta` (casa `proxima_pergunta.texto` contra `campos[].rotulo` do bloco atual, por termo significativo normalizado; sem casamento, `null`, nunca força encaixe), `acumularResumo` (upsert por `t`, `pendente` = `campos[]` do bloco menos `perguntado`, poda por bytes), `resumirParaContexto` (recalcula `pendente` contra o bloco ATUAL da chamada), `normalizarResumoAcumulado` (defesa contra o `'{}'::jsonb` legado — achado ANTES de aplicar migration, ver "Medido" abaixo), `acumularResumoNaSessao` (I/O, fail-CLOSED, CAS com 1 retentativa).
- `src/types/copiloto.ts:302-311` — `resumo_acumulado` trocou de `Record<string, unknown>` para `ResumoAcumuladoParaContexto | null`; novos tipos `ItemResumoAcumulado`/`ResumoAcumulado`.
- `src/server/copiloto/contexto.ts` — `montarContextoCopiloto` mudou de retorno (`{ contexto, camposBlocoAtual }`) porque `RoteiroCampo[]` completo (com `id`) NUNCA pode entrar em `ContextoCopiloto` (é serializado por inteiro via `JSON.stringify` para o provedor de IA em `executar-ia.ts:172` — vazaria `id` técnico como token cobrado); bloco E agora passa por `resumirParaContexto`; comentário de topo (linhas 53-56) reescrito.
- `src/server/copiloto/ciclo.ts` e `sugestao/route.ts` — chamam `acumularResumoNaSessao` depois do INSERT de `copiloto_sugestoes` confirmado, reusando `camposBlocoAtual` da mesma chamada (zero query nova); `warmup.ts`/`scripts/bancada-copiloto.ts` ajustados para o novo retorno.
- `src/server/copiloto/expurgo.ts` — `redigirResumoDaSessao` (NO-OP deliberado nesta fatia — `perguntado[]` não guarda citação literal — mas já cabeada nos 2 pontos de `carimbarSessoesSemPendencia`, para a Fatia B trocar só o corpo).
- `supabase/migrations/0120_copiloto_resumo_acumulado.sql` — RPC `registrar_resumo_copiloto` (CAS, cópia estrutural de `registrar_participantes_copiloto`/0105) + chave `copiloto_sessao.resumo_acumulado` (nasce `false`, fail-CLOSED).
- `supabase/migrations/0121_copiloto_prompt_v9_memoria_acumulada.sql` — prompt novo (deriva de `max(versao)`, nasce `ativo=false`): proibição de repetir tema perguntado, `proxima_pergunta:null` como resposta correta quando o bloco está coberto, proibição de re-derivar perguntado/pendente.
- `scripts/verificacao-0120.sql` — roteiro com rollback (CAS correto/incorreto, igualdade estrutural de jsonb, GRANT por catálogo, teto de bytes medido por `pg_column_size`).
- Testes: `resumo.test.ts` (31 casos, novo), mais 5 arquivos de teste ajustados por causa da mudança de tipo/assinatura (`contexto.test.ts`, `ciclo.test.ts`, `executar-ia.test.ts`, `validar.test.ts`, `warmup.test.ts`, `sugestao/route.test.ts`).

**Medido (real, colado):**
- `npx tsc --noEmit` — limpo, sem saída.
- `npx eslint src scripts` — 0 erros, 7 warnings, todos pré-existentes em arquivos não tocados por mim (`ConduzirSessaoApp.tsx`, `PainelCopiloto.tsx/.test.tsx`, `estado.ts`).
- `npx vitest run --exclude "**/ConduzirSessaoApp.test.tsx"` — **104 arquivos, 1646 testes, 100% verde**. `ConduzirSessaoApp.test.tsx` excluído porque instável em suíte completa (3-5 falhas variáveis) mas 28/28 verde quando isolado — arquivo de frontend em edição paralela por outro agente (`PainelCopiloto.tsx`), não relacionado ao meu código.
- 🔴 Achado ANTES de escrever a migration: `sessoes_copiloto.resumo_acumulado` nasce `'{}'::jsonb` (default da 0091, gravado em TODA sessão já criada) — `data?.resumo_acumulado ?? RESUMO_VAZIO` não pega esse caso (`??` só ativa em `null`/`undefined`; `{}` é truthy). Sem `normalizarResumoAcumulado`, a 1ª leitura de qualquer sessão pré-existente quebraria em runtime lendo `.perguntado` de `{}`. Corrigido e coberto por 6 testes dedicados antes de qualquer aplicação em banco.
- `explain (analyze)` da RPC: **PENDENTE** — este agente não tem credencial de banco de produção nesta máquina (mesma restrição já registrada em 0105/0111/0119). Comando exato colado na migration e em `verificacao-0120.sql §7`, para o responsável rodar contra `fcfsnqqaphtamhrpuyoh` e colar o resultado real antes de aplicar.

**Decidido:** manter `pendente`/`perguntado` na mesma unidade (`campo.id`) para a subtração ser direta, sem tradução — decisão do coordenador durante a execução, resolvendo a ambiguidade entre as duas mensagens anteriores (categoria fixa vs. vocabulário dinâmico).

**Pendente com o Marcio/coordenador:**
- Rodar `explain (analyze, buffers)` real da RPC `registrar_resumo_copiloto` contra produção e colar na migration 0120.
- `security-pentester` (obrigatório: RPC `security definer`, PII de família/patrimônio, ainda que sem citação literal nesta fatia).
- Ativar `copiloto_sessao.resumo_acumulado` (nasce `false`) e a versão nova do prompt (nasce `ativo=false`) só depois da sonda/medição em sessão real.

**Notas tocadas:** nenhuma nova (o registro de decisão já está na entrada de 15:30 acima).

## 13:24 — Fable reprovou em SOLIDIFICAÇÃO: memória do copiloto ficava travada em `{}` para sempre

**Pedido:** corrigir os 3 defeitos confirmados por SQL real contra produção + 2 apontamentos do Fable.

**Medido pelo Fable (real, contra o banco):** `'{}'::jsonb is not distinct from '{"v":1,...}'::jsonb` → `FALSE`. `resumo_acumulado` nasce `'{}'::jsonb` (default da 0091) em TODA sessão, não só nas 3 antigas. `resumo.ts` normalizava o `{}` lido para `RESUMO_VAZIO` e mandava esse valor NORMALIZADO como `p_resumo_esperado` — a RPC (que compara o jsonb REAL) recusava sempre, devolvia `{}` cru, e a retentativa chamava `acumularResumo({}, ...)` sem normalizar → `TypeError` em `for (const item of acumulado.perguntado)`. Sem try/catch em `acumularResumoNaSessao`, o erro subia até o ciclo/rota: `sugestao/route.ts` devolvia HTTP 500 com a sugestão JÁ GRAVADA. Os 382 testes anteriores não pegaram porque mockavam o banco devolvendo `RESUMO_VAZIO` já normalizado, nunca o `{}` real.

**Feito:**
- `resumo.ts` — o token do CAS agora é sempre o jsonb BRUTO (`data?.resumo_acumulado`, nunca normalizado); `ResultadoCas.resumo` virou `unknown` (achado do Fable: o tipo escondia que podia ser `{}` sem `.perguntado`); retentativa usa `primeira.resumo` bruto como esperado da 2ª chamada.
- `acumularResumoNaSessao` — corpo movido para `acumularResumoNaSessaoInterno`, wrapper público inteiro sob try/catch com `registrarErro` (a promessa "nunca derruba o ciclo" agora está no código).
- Leitura duplicada de config corrigida: `ativo` virou PARÂMETRO obrigatório de `acumularResumoNaSessao` (nunca mais lido dentro da função); `montarContextoCopiloto` ganhou 4º parâmetro opcional `resumoAcumuladoAtivo`; `ciclo.ts` lê a chave 1× dentro de uma promise encadeada no MESMO `Promise.all`, repassa para os dois lugares; `sugestao/route.ts` lê 1× antes de montar contexto, reaproveita depois.
- `types/copiloto.ts:249` — comentário de `n` corrigido (só `proxima_pergunta`, `cobriu_no_bloco` não é usado — medido: 11 de 178).
- Testes: `resumo.test.ts` ganhou o teste de integração exigido (banco devolve `{}` cru + RPC recusa a 1ª tentativa com o mesmo `{}` cru → nunca lança), mais um teste de exceção inesperada no `from`; `ciclo.test.ts` ganhou teste dedicado provando 1 leitura só, repassada aos dois consumidores.

**Medido (real, provado por reversão temporária controlada, com `.bak` em `/c/tmp` e md5 conferido antes de restaurar):**
- Revertido temporariamente o item 1 (usar normalizado em vez de bruto) → o novo teste de regressão em `resumo.test.ts` FALHOU como esperado (`expected '{"v":1,...}' to be '{}'`). Restaurado, md5 idêntico ao corrigido.
- Revertido temporariamente o item 4 (reintroduzida a releitura da chave em `ciclo.ts`) → o novo teste em `ciclo.test.ts` FALHOU como esperado (`expected "spy" to be called 1 times, but got 2 times`). Restaurado, md5 idêntico.
- `npx tsc --noEmit` — limpo.
- `npx eslint src scripts` — 0 erros, 7 warnings, todos pré-existentes fora do meu escopo.
- `npx vitest run src/server/copiloto/` — **19 arquivos, 384 testes, 100% verde**.
- `npx vitest run --exclude "**/ConduzirSessaoApp.test.tsx"` (repo inteiro) — **104 arquivos, 1648 testes, 100% verde**.

**Decidido:** não tocar em 0120/0121 (já aplicadas em produção, conforme instrução do coordenador) — a correção do item 4 foi só de código (leitura movida para o chamador), e o texto da migration 0120 ("ZERO round-trip novo") passou a ser verdadeiro de fato, sem precisar de 0122.

**Pendente com o Marcio/coordenador:** mesmas pendências da entrada de 13:00 (explain analyze real, pentester, ativação dos kill-switches).

**Notas tocadas:** nenhuma nova.

## 16:30 — Memória do copiloto APROVADA e publicada (66dc5b3)

**Pedido:** Marcio: "pode prosseguir da forma como preferir... quando finalizar tudo, todas as partes, e tiver a finalização consolidada, me retorna... pode subir direto quando finalizar".

**Feito:**
- `src/server/copiloto/resumo.ts` (novo) + 31 testes — funções puras (`categorizarPergunta`, `acumularResumo`, `podarPorBytes`, `derivarPendente`, `normalizarResumoAcumulado`) + I/O com CAS.
- `0120` (RPC CAS + kill-switch) e `0121` (prompt v9) — **aplicadas em produção por mim**, via MCP.
- `scripts/bancada-copiloto.ts` (commit 42007ef) — replay da sessão real para comparar versões de prompt, teto US$ 3, nunca promove.
- `CONTINUAR-AQUI.md` — bloco no topo com a ORDEM DE ATIVAÇÃO vinculante.

**Medido contra produção (não por analogia):**
- `explain (analyze, buffers)` da RPC: `LockRows → Bitmap Index Scan on sessoes_copiloto_pkey`, **0,324 ms, shared hit=3** (a 0105, de referência, mede 0,306 ms).
- CAS provado nos dois sentidos, em transação com rollback: token bruto → `aplicado=true`; token errado → `aplicado=false` + devolve o atual.
- Teto: resumo CHEIO (16 `perguntado` + 8 `pendente`) = **1.761 bytes** contra CHECK de 4096 (folga 2,3×).
- Grants idênticos à `registrar_participantes_copiloto`: `security definer`, `search_path=public, pg_temp`, só `postgres`+`service_role`.
- `src/server/copiloto/` **384/384** · tipos limpos · `npm run build` compila.

**Decidido / aprendido:**
- 🔴 **REPROVAÇÃO DO FABLE VALEU A RODADA.** O código mandava o resumo NORMALIZADO como token do CAS, e o banco guarda `{}` cru: `'{}' is not distinct from '{"v":1,...}'` = FALSE. A escrita **nunca aplicaria**, e a retentativa estouraria `TypeError` subindo até a rota (HTTP 500 no "Me ajuda agora", com a sugestão já gravada). Os 382 testes verdes não pegaram porque o mock devolvia o objeto já normalizado — **um banco que não existe**. Lição no vault: `06 Memorias/2026-09-18 - Token de CAS e o valor BRUTO do banco, nunca o normalizado.md`.
- O `column_default` da coluna é `'{}'::jsonb` — não eram "as 3 sessões antigas", era **toda sessão nova**.
- Hipóteses minhas testadas e DESCARTADAS antes de virar código: (a) disparar por relevância em vez de relógio — só 1,0% dos ciclos disparam sobre fala < 80 chars; (b) cortar campos pouco usados do prompt — a tela consome os dois (`PainelCopiloto.tsx:292,819`).
- Correção ao plano do arquiteto: ele propôs `cobriu_no_bloco` como fonte de `perguntado`; medi — 11 itens em 178 sugestões. A fonte boa é `proxima_pergunta.texto` (133), 65% categorizando automaticamente.

**Pendente com o Marcio:**
1. 🔴 **ORDEM DE ATIVAÇÃO (vinculante, Fable):** frontend do "bloco coberto" (B73) → kill-switch `copiloto_sessao.resumo_acumulado` → promover v9. Invertida, `PainelCopiloto.tsx:1397-1408` imprime "não teve nada específico a apontar" quando a verdade é "bloco coberto, avance".
2. Rodar a bancada (`npx tsx scripts/bancada-copiloto.ts`) antes de promover v8 ou v9 — custa ~US$ 0,65, teto US$ 3.
3. Baseline para comparar na próxima sessão (`C:/tmp/baseline-copiloto.md`): concentração de **39,3%** nos 5 temas; por quarto 25,8% → 51,6% → 30,0% → **50,0%**; 16,4% de falha; 9,5 s de latência.
4. "empresa" foi o 2º tema mais perguntado (15×) e não tem campo no roteiro v5 — criar é decisão de MÉTODO da Dra. Elaine.
5. Fatia B (`fatos[]` com evidência) — só depois de a Fatia A provar ganho em sessão real.
