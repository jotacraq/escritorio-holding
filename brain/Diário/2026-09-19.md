## 14:10 — Máquina nova sincronizada: o clone estava 12 commits atrás do repo que publica

**Pedido:** "se atualize da versão mais recente do sistema, escritorio-holding".
**Feito:** `git merge --ff-only infra/main` (`60374fb` → `e92d322`, 54 arquivos, migrations 0120–0124).
**Medido:** `origin` (jotacraq) está **79 commits atrás** do que estava publicado; quem publica é `infra`
(infra-grupo-participa). Produção servia `e92d322`, igual ao HEAD depois do merge. Migrations no banco
até `20260918204624` (0124 inclusive).
**Decidido:** trabalhar sempre contra `infra/main`; o `origin` deste clone é histórico pessoal e está
defasado — conferir os dois antes de qualquer conclusão sobre "o que está no ar".
**Pendente com o João:** nada.
**Notas tocadas:** nenhuma.

---

## 17:20 — v10 ATIVO, e o registro dizia "0 truncadas" onde o banco diz 49

**Pedido:** "preciso que você ative o v10 também, com urgência".
**Feito:** `prompts_versoes` copiloto_sessao → v10 ativa (v7 desligada); `ficha_cliente`,
`resumo_acumulado`, `rodape_transcricao` → `true`; `max_tokens` 1400 → **1900**; `timeout_ms`
20000 → **28000**. Depois da 0125, `rodape_transcricao` voltou a `false` (deprecada pela chave
`saude_captura`).
**Medido (é o ponto desta entrada):** a v7 truncou **49 de 299 execuções — 16,4%** com
`stop_reason=max_tokens`, sendo 34/145 e 15/140 nas duas horas da sessão do Carlos Alberto (18/09).
O `CONTINUAR-AQUI.md` afirmava "**0 truncadas em 299**" e sustentava nisso a decisão de não ativar.
Pico real de saída: 1.387 contra teto de 1.400. Raciocínio médio: 461 de 691 tokens.
Causa do erro no registro: derivar as execuções a partir de `copiloto_sugestoes.execucao_ia_id`
conta só o conjunto que, por definição, **não** trunca — execução truncada quebra o JSON e nunca
vira sugestão.
**Decidido:** ativar a v10 **e** subir o teto no mesmo movimento. Ativar sem mexer no teto seria
empilhar um campo novo (`ficha_cliente[]`) num limite que já falhava 1 vez em 6. Latência acompanha
a geração (10,77 ms/token), por isso o timeout subiu junto: 28s cobre ~1.900 tokens contra o máximo
real de 19,4s. A bancada **não rodou** — `ANTHROPIC_API_KEY` está vazia no `.env.local` e não há
`OPENROUTER_API_KEY` nesta máquina; não busco credencial de produção.
**Pendente com o João:** medir o p99 da primeira sessão com a v10. `timeout_ms` (28s) passou a ser
maior que `intervalo_segundos` (20s): `copiloto_ciclos` impede duas chamadas na mesma janela, **não**
duas janelas em voo. Se o p99 passar de 20s, subir `intervalo_segundos` para ≥ o timeout.
**Reversão (1 comando):** `update prompts_versoes set ativo=(versao=7) where chave='copiloto_sessao';`
+ `max_tokens=1400`, `timeout_ms=20000`.
**Notas tocadas:** [[Custo da IA]].

---

## 23:40 — Fase 13: a coluna 3 vira abas, a primeira dobra passa a existir, e o fim da sessão vira documento

**Pedido (literal, 5 itens):** transcrição e inventário "em conflito", os dois embaixo → quer **abas**
para escolher, e a transcrição serve "apenas para captar com clareza se o bot está operante"; **tudo
na primeira dobra**; ao encerrar o copiloto, **pop-up** com score da sessão, observações do cliente e
pontos de melhoria, que dê para **baixar** e **consultar depois** ("por ora algo mais básico, e vamos
aprimorando"); e transcrição com **rolagem própria** que desce conforme a fala chega.

**Feito:**
- **Abas (§A):** `AbasColuna3.tsx` — `Ficha · Transcrição · Inventário`, uma célula, **uma** superfície
  de rolagem, aba inativa **desmontada do DOM**. Fechou um **duplo-scroll vivo** em
  `PainelCopiloto.tsx:405` (a `Coluna rolavel` + a região própria do `PainelTranscricao`) — a causa
  mecânica do "conflito" que o dono viu. `PainelInventario` saiu para arquivo próprio.
- **Saúde da captura:** o sinal "o bot está vivo?" virou **linha permanente de ≤44 px** no rodapé;
  o `OverlayTranscricao` saiu inteiro. Sinal é permanente, conteúdo é aba.
- **Auto-scroll (§B):** gruda no fim só se já estava no fim; botão "▼ novas falas" (`absolute`, custo
  vertical zero) quando o leitor subiu; `prefers-reduced-motion` respeitado. Tolerância derivada de
  `line-height`: **31,7 px** (padrão) / 40,7 px (Grande) — o plano previa 24 px, que é menos de 1,5
  linha, o defeito que a regra existe para corrigir.
- **Retrospecto da Sessão (§D):** `copiloto_retrospectos` (0125), `retrospecto.ts`, rota
  `GET …/retrospecto` (JSON e `?formato=docx`), `PopupRetrospecto` + `CorpoRetrospecto` (um corpo,
  dois containers) e aba `#retrospecto` na Ficha 360. **Zero chamada de IA.**
- **Migrations aplicadas e provadas em produção:** **0125** (retrospecto) · **0126** (evento de
  timeline único por sessão) · **0127** (família do copiloto na anonimização do titular) ·
  **0128** (`tl_ins` barra `tipo='retrospecto'`) · **0129** (10º ramo de `vw_pendencias_sistema`).

**Medido:**
- **A "primeira dobra" nunca funcionou fora do modo tela cheia, e isso é anterior a esta fase.**
  No HEAD `e92d322` limpo, em Chromium: `scrollHeight 970 / clientHeight 826` (padrão) e **1207/826**
  (escala Grande); "Encerrar copiloto" ficava **58 a 344 px abaixo da dobra**. Causa: `main:has(>
  .largura-cheia)` usa combinador de **filho**, mas `.largura-cheia` é **neta** do `<main>` — o
  `AppShell` sempre desenhou um `<div class="mx-auto w-full max-w-7xl">` no meio. As duas regras nunca
  casaram: a tela seguia presa em `max-w-7xl` e a cadeia de altura quebrava, fazendo `minmax(0,1fr)`
  virar `max-content`. Corrigido em 3 linhas + `height:100%` em dois níveis.
- **8 capturas em Chromium:** mosaico **571 px** (normal) / **615 px** (tela cheia); faixa de abas
  28 px em 8/8; `col3.scrollHeight === clientHeight` nas 8 combinações × 3 abas; **1** elemento com
  `overflowY:auto` na COL 3. Números crus em `docs/medicoes/fase-13-primeira-dobra.json`.
- **Cobertura do roteiro** (o "score" da v1), reproduzida pelo código contra as 3 sessões reais:
  **4/13, 9/13, 6/13**.
- **Sinal para uma NOTA não existe:** `cobriu_no_bloco` = **11 itens em 369 sugestões**, em 1 de 3
  sessões; `desfecho` = **366 expirada / 3 aceita / 0 ignorada**. Por isso a v1 mostra fração com
  denominador visível, nunca nota 0-10 — número sem lastro é o padrão do COALESCE já catalogado aqui.
- Suíte: **1988/1988** (era 1939 no início do dia), `tsc` 0 erros em `src/`, `npm run build` ok.

**Decidido (do João, hoje):**
- **3 abas**, Ficha de estreia — recusada a alternativa "Ficha fixa + 2 abas embaixo", que reabriria o
  mecanismo exato do defeito F4-1 medido pelo Fable em 18/09.
- **Score = cobertura**, sem nota, até haver sinal medido de condução.
- **Consentimento revogado NÃO trava** a gravação nem o download do retrospecto (achado F3 do
  pentest). Critério da 0093 é "sai para subprocessador?", e a v1 é `origem='derivado'`, zero IA.
  🔴 Decisão de produto, com risco assumido pelo dono, **sem validação jurídica nesta data**. Se
  `origem` virar `'ia'`, a pergunta reabre e a trava tem de existir ANTES.
- **LGPD: família toda do copiloto**, não só a tabela nova.

**Decidido (orquestrador):** retrospecto gravado em todos os caminhos de encerramento, pop-up só no
manual; `saude_captura` no mesmo deploy.

**Pentest — 1 ALTO, 2 médios, 3 baixos, 3 info, todos fechados:**
- 🔴 **ALTO:** `copiloto_retrospectos` (e a família inteira do copiloto, desde a Fase 10) estava fora
  de `public.anonimizar_titular` e de `INVENTARIO_TITULAR`. Um titular exercia o art. 18 e a citação
  literal da fala da família sobrevivia — na única tabela dessa família com botão de download.
  **O relatório apontou `app.anonimizar_titular`: essa função não existe.** A real é
  `public.anonimizar_titular` (13.968 chars, 36 tabelas, `ilike '%copiloto%'` = false). Recriar em
  `app` teria produzido função nova que ninguém chama, com a migration "aplicada e verde".
- **MÉDIO (squat):** `eventos_timeline` aceita INSERT de qualquer papel interno com `tipo` livre;
  com a 0126 tornando o evento único e o código tratando `23505` como sucesso, alguém podia **plantar**
  a linha antes e o texto plantado viraria registro do sistema no Histórico, numa tabela append-only.
  Fechado no banco (0128) **e** no código (relê a linha conflitante; só é sucesso se for a do sistema).
- **MÉDIO (F3):** consentimento revogado — decisão do dono acima.
- Baixos: docblock afirmava proteção de IDOR que a RLS não dá (`ve_patrimonio()` é gate de **papel**,
  não recorte de **linha**); `descricao` do evento levava métrica de condução da advogada, legível por
  `relacionamento`; 429 sem `Retry-After`.

**Fable — APROVADO** nos 5 critérios, com ressalva em solidificação; as duas dívidas da ressalva foram
fechadas antes do commit em vez de ficarem registradas:
- **D-1:** a própria 0127 plantava um crash — `conteudo='{}'` com a tela fazendo `conteudo.cobertura.x`.
  Fechado no servidor: `lerRetrospectoComEstado` separa `ok` | `ausente` | `lapide`; lápide → 404
  `retrospecto_indisponivel`.
- **D-2:** retrospecto que falha no encerramento ficava 404 para sempre. Agora o `GET` remonta sob
  demanda (`montarConteudoRetrospecto` é pura). 🔴 **A interação que quase virou bug novo:** remontar
  numa linha **anonimizada** reconstruiria o documento das fontes — que a 0127 também zerou —
  produzindo documento **novo, datado de hoje, sobre titular cujo tratamento foi declarado encerrado**.
  É por isso que `lapide` e `ausente` são estados separados e só `ausente` autoriza remontar.
- **D-5:** focus trap no pop-up (12/12 Tab e 8/8 Shift+Tab dentro do diálogo, medido em Chromium);
  `z-[60]` (o "N" por cima era `<nextjs-portal>`, dev-only — não havia problema de `AppShell`);
  rótulo repetido removido. **D-4:** "Ver o retrospecto" na linha do encerramento automático.

**Pendente com o João:**
1. **Medir o p99 da primeira sessão com a v10** (ver entrada das 17:20).
2. **Mosaico abaixo de 330 px** com "O cliente disse" aberto em 1280×720 (161–253 px medidos) — nada
   vaza; é decisão de produto sobre quanto aquele campo vale.
3. **Crash latente em outras telas de titular anonimizado**, medido e registrado, **não** corrigido:
   `PainelPerfilSessao.tsx:55`, `MaterialAba.tsx:207`, `MateriaisModelosAba.tsx:266` fazem acesso de
   2 níveis sobre jsonb que a anonimização zera (`materiais_gerados` é o risco real).
   `CabecalhoFicha.tsx` degrada limpo. Gatilho: a 1ª anonimização real (`titulares_solicitacoes` = 0
   hoje). Escopo certo é varrer as 36 tabelas, não as 3.
4. **`ConduzirSessaoApp.test.tsx`** — 1 caso instável sob contenção de 27 arquivos (`waitFor` de 1s
   esperando tick real do poller); 5/5 isolado, 4 corridas seguintes verdes. É a corrida de 18/09,
   não desta fase.

**Notas tocadas:** [[Schema]], [[Seguranca]], [[Esteira do cliente]].
