## 19:30 — Fase 12, Fatia 1: servidor infere o bloco atual da conversa

**Pedido:** executar a Fatia A/1 do plano da Fase 12 — o servidor passa a saber
"onde a advogada está agora" na Sessão de Viabilidade, em vez de depender só
do índice que a tela guarda em `sessionStorage` (defeito-raiz: sem clique, o
gatilho de "virada de bloco" nunca disparava e a tela afirmava blocos não
percorridos que a conversa já tinha passado).

**Feito:** o escopo mudou duas vezes em tempo real, por correção do
`arquiteto` via coordenador (a primeira era tabela `copiloto_cobertura`, a
segunda jsonb em `sessoes_copiloto`, a final — a que foi ao ar — é só DML,
reusando `copiloto_sugestoes.bloco_id` que já existia desde a 0091). A versão
final:
- `src/server/copiloto/schema.ts` / `validar.ts`: 6º campo `bloco_inferido`
  (bloco_id + confianca + evidencia) na saída da IA — sem enum novo (o risco
  de estourar o teto da gramática estrita era do `veredito` de cobertura, que
  ficou fora). Validação: bloco_id fora do roteiro ativo ou evidência não
  conferida descarta o campo inteiro, nunca corrige.
- `src/server/copiloto/contexto.ts`: `roteiro_ativo_blocos` — `(id, titulo,
  objetivo)` de todos os blocos, sem `falas`/`proibido` (peso medido em
  14/09: `falas` era 94-97% do peso de 1 bloco).
- `src/server/copiloto/ciclo.ts`: grava `bloco_inferido.bloco_id` em
  `copiloto_sugestoes.bloco_id` (reinterpreta a coluna, fallback preserva
  `desvio_sugerido`/bloco de contexto).
- `src/server/copiloto/estado.ts`: `resolverBlocoAtual` — precedência
  fixação manual recente (`?bloco=`+`fixado_em`, janela de 300s) > última
  inferência gravada > `indisponivel` (nunca índice 0 inventado — CLAUDE.md:
  "nada de dado inventado na tela"). Reescrito o comentário que dizia "o
  servidor não tem onde a advogada está agora" — virou mentira depois desta
  entrega.
- `src/server/copiloto/gatilho.ts`: `virada_bloco` só dispara por correção
  **manual** — trava contra o laço (ciclo infere → grava → próxima avaliação
  vê o índice mudar → dispara virada → infere de novo). Teste de aceite
  dedicado: 3 ciclos com bloco inferido diferente não disparam nenhum extra.
- `src/app/api/sessoes/[id]/copiloto/route.ts`: `?bloco=` vira
  opcional/depreciado — só vale como fixação manual quando vier com
  `fixado_em` (nunca ressuscita `sessionStorage` velho sem carimbo de tempo).
- `supabase/migrations/0106_copiloto_inferencia_bloco.sql`: só DML — 2 chaves
  (`copiloto_sessao.inferencia_bloco_ativa=true`,
  `copiloto_sessao.janela_fixacao_manual_segundos=300`) + `update` do corpo
  do prompt v1 `copiloto_sessao` (0094), que nunca foi ativado em produção
  (0 execuções — exceção documentada à regra "prompt nunca editado, só
  versionado").
- `scripts/verificacao-0106.sql`: roteiro de `explain (analyze, buffers)`
  dentro de `begin/rollback`, pronto para quem tiver acesso ao banco rodar.

**Medido:** `npx tsc --noEmit` limpo · `npx eslint .` 0 erros (2 warnings
pré-existentes, arquivo não tocado) · `npm run build` completo · `npx vitest
run` sem concorrência: **1331 passed, 1 skipped, 85 arquivos** (baseline era
1320/81 — subiu porque o `frontend-engineer` também entregou em paralelo).

**Decidido:**
- Cobertura acerto/erro por bloco (a "Fatia 2" do desenho original) ficou de
  fora desta entrega — rascunho preservado em `tmp/squad/` (fora do git),
  não aplicado.
- Migration `0106` **não foi aplicada em produção** — este agente não tem
  acesso ao banco `fcfsnqqaphtamhrpuyoh` nesta máquina (regra dura: nunca
  buscar credencial de produção). O `explain (analyze)` do roteiro de
  verificação é a EXPECTATIVA (mesmos predicados já provados por migrations
  irmãs, 0103/0105), não uma medição real desta rodada — fica pendente para
  quem aplicar.
- Latência do ciclo (p50/p95) **não foi remedida** pelo mesmo motivo de falta
  de acesso a produção — reportado como pendência, não maquiado.
- Dois incidentes de perda de trabalho durante a sessão (edições em
  `estado.ts`/`ciclo.ts`/`gatilho.ts`/`route.ts` revertidas sozinhas para a
  HEAD do git, aparentemente por edição concorrente de outro agente na mesma
  working tree) — reconstruído e comitado logo em seguida; registrado como
  risco operacional de rodar back+front em paralelo na mesma árvore.

**Pendente com o Marcio:**
- Rodar `scripts/verificacao-0106.sql` contra o banco real antes de aplicar a
  migration (ou aplicar direto se o `explain` esperado bater, dado que reusa
  os mesmos índices já provados em 0103/0105).
- Bancada de latência p50/p95 do ciclo com a inferência ligada — se p95 > 8s,
  a fatia não deveria ter subido (ainda não medido).
- Decidir se/quando a Fatia 2 (cobertura acerto/erro por bloco) entra — o
  rascunho existe em `tmp/squad/rascunho-fatia2-cobertura-0106.sql.txt`.

**Notas tocadas:** nenhuma nova no vault desta vez — decisão de escopo já
documentada no histórico de mensagens do coordenador, não repetida aqui.

## 19:52 — Fase 12, Fatia B: ConduzirSessaoApp vira leitura (linha fina do topo)

**Pedido:** terminar a Fatia B em `src/components/sessao/ConduzirSessaoApp.tsx`
(o `PainelCopiloto.tsx` já tinha sido feito por outro agente, 2.091→937 linhas).
Pedido literal do Marcio: "a tela hoje está poluída com diversas informações
que não são úteis [...] os detalhes vemos depois, no resumo da sessão [...]
preciso que essa tela seja intuitiva".

**Feito:**
- `ConduzirSessaoApp.tsx`: removidos da primeira dobra — `BarraProgresso`
  compacta (12 partes), `<nav>` fixa "Anterior/Próxima", `PainelSims` (4 SIMs
  — decisão do dono: consentimento de gravação não é requisito, sai sem
  substituto), `PainelVigilanciaAoVivo`, `BlocoRoteiro`/"Anotação da parte",
  `PainelPerfilConsulta`, `AtalhosTeclado`, `PainelBriefingSessao`,
  `PainelOferta`, e o `sessionStorage` do índice manual. Nenhum componente foi
  apagado — só o call-site saiu; renascem na Ficha 360 (Fatia 2, fora de
  escopo). A navegação por SETA do teclado (`useEffect`) ficou.
- Nova `LinhaFinaRoteiro`: uma linha densa — "Agora: `<título>` · Falta
  `<nome>` na sala [Corrigir parte ▾]". `Agora:` lê `bloco_atual_resolvido`
  (contrato do backend em `src/types/copiloto.ts`) via uma 2ª instância de
  `usePollingCopiloto`; `origem==="indisponivel"`/`bloco_id===null` cai em
  "ainda identificando…", isolado em `rotuloBlocoAtual()` para trocar em 1
  linha. `· Falta X na sala` reusa `resumoAusentesLinhaFina` (já existia).
  `[Corrigir parte]` é `<select>` (não botão), `min-h-11`, chama
  `fixarBlocoManualmente()` do hook — que manda `bloco`+`fixado_em` (ISO,
  agora) na MESMA rota `GET /copiloto` (não existe rota `POST .../bloco`
  separada; fixação manual sempre foi via query string, `estado.ts`).
- `usePollingCopiloto.ts` (ajuste mínimo, permitido pelo escopo): passou a
  expor `blocoAtualResolvido` (snapshot, substitui nunca acumula, mesmo
  padrão de `comparacaoDecisores`) e `fixarBlocoManualmente(indice)` — grava
  numa ref, cancela o timer do ciclo normal e dispara um ciclo imediato (sem
  esperar até 10s parado pelo próximo tick natural).
- `api.ts` (ajuste mínimo): `buscarPollingCopiloto` ganhou `fixadoEm?` opcional
  nos parâmetros — só some no request quando presente.
- `PainelBot` continua montado (fora da vista, `display:none`) só para o
  `aoMudarEstado` alimentar o aviso de `sala_invalida`/`meeting_not_found` na
  linha fina — erro de sala nunca fica mudo mesmo com o quadro fora da tela.
- `ConduzirSessaoApp.test.tsx`: reescrito do zero (o antigo testava
  exatamente os elementos removidos por este pedido — barra de progresso,
  `PainelSims`, `PainelBriefingSessao`). 11 testes novos, incl. a trava do
  plano: bloco resolvido pelo servidor usa o título dele; "ainda
  identificando…" nunca vira "Parte 0"; `sessionStorage` da chave antiga não
  é mais lido; ausentes somem por completo sem ausência; `<select>` nunca
  botão com `min-h-11`; escolha dispara fixação com `bloco`+`fixado_em`
  JUNTOS; erro de sala aparece com `PainelBot` oculto.

**Divergência do plano:** o plano pedia validar `bloco < 0` no handler de
correção — adicionei também `indiceEscolhido >= blocosRoteiro.length` (defesa
extra, o `<select>` só oferece opções válidas mas built-in nunca custa).
Também não implementei sincronização automática "servidor força o índice
local a seguir" — avaliei e descartei: criaria risco de loop (seta do teclado
mudando `indice`, servidor devolvendo inferência antiga, `useEffect` revertendo
o clique da seta). A trava do plano ("nunca derive do sessionStorage") já está
satisfeita porque o `sessionStorage` foi removido e a linha fina lê
`bloco_atual_resolvido` direto do servidor, nunca do índice local.

**Medido:** `npx tsc --noEmit` limpo · `npx eslint src` 0 erros (2 warnings
pré-existentes em `expurgo.test.ts`, arquivo não tocado) · `npx vitest run
--no-file-parallelism`: **1336 passed, 1 skipped, 85 arquivos** (baseline era
1331/1/85 — subiu porque 11 testes novos substituíram 6 antigos obsoletos) ·
`node scripts/contraste.mjs`: nenhuma falha · `npm run build`: completo, todas
as rotas geram, incl. `/sessoes/[id]/conduzir`.

**Decidido:** 2 chamadas de polling na mesma tela (`PainelCopiloto` +
`LinhaFinaRoteiro`) aceito como custo da fatia — é o único jeito de a linha
fina saber o bloco sem virar filho de `PainelCopiloto` (fora do escopo, já
pronto e travado por outro agente).

**Pendente com o Marcio:** nenhum — fatia fechada. Ficha 360 (Fatia 2) é onde
os componentes removidos hoje renascem, fora deste escopo.

**Notas tocadas:** [[Fase 12]]

## 21:40 — T1 da repaginação: tokens de densidade (2 arquivos, 176 componentes)

**Pedido:** aplicar a escala de densidade nova (`Cartao` padding + 5 tokens de
`globals.css`) — maior alavancagem da repaginação de UI pedida pelo Marcio em
16/09 ("clean e convencional", ver 05 Decisoes no vault). Sistemas de
referência: Fiori/Atlassian/Salesforce Lightning, não mais o seminário.

**Feito:**
- `src/components/ui/Cartao.tsx`: `PREENCHIMENTO.normal` `"p-5 sm:p-6"` (20-24px)
  → `"p-4"` (16px, sem salto em `sm:` — o salto fazia o desktop, onde o
  advogado trabalha, ser o mais folgado); `PREENCHIMENTO.compacto` `"p-4"`
  (16px) → `"p-3"` (12px).
- `src/app/globals.css`: `--espaco-secao` 1.625rem→1.25rem (26→20px) ·
  `--espaco-bloco` 1.125rem→0.875rem (18→14px) · `--espaco-cartao`
  1rem→0.75rem (16→12px) · `--espaco-item` **inalterado** (8px, trava de
  `--alvo-gap`) · `--raio-cartao` 0.75rem→0.5rem (12→8px, passa a coincidir
  com `--raio-controle`).
- `docs/DESIGN-SYSTEM.md`: §2.1 (tabela de ritmo com T1/V2/V1), §3 (raio no
  checklist) e nota de topo registrando a mudança de direção Fase 7 → T1.
- `src/components/sessao/**` e `src/app/(app)/sessoes/**` **não tocados**
  (regra dura do pedido — sessão real em produção); confirmado por grep, 0
  diffs ali. A tela `/sessoes/[id]/conduzir` usa `Cartao` em só 1 ponto
  (`PainelOferta.tsx`), o resto do layout é próprio da sessão.

**Medido:**
- `npx tsc --noEmit`: limpo.
- `npx eslint src`: 1 warning pré-existente (`ConduzirSessaoApp.tsx:183`,
  arquivo não tocado), 0 erros.
- `NODE_OPTIONS=--max-old-space-size=6144 npx vitest run
  --no-file-parallelism`: **1357 passed, 1 skipped, 85 arquivos** — baseline
  mantida, nenhuma queda.
- `node scripts/contraste.mjs`: rodado antes e depois — **nenhum número
  mudou** (não tocamos em cor), 0 falhas, 3 exceções documentadas idênticas
  às do DS §6.
- Altura (`document.body.scrollHeight`) e `scrollWidth` a 390px **não foram
  medidos no navegador**: falta `.env.local` nesta máquina, e a regra do
  pedido proíbe buscar credencial de produção. Entreguei aritmética
  declarada como estimativa (contagem de `Cartao`/`gap-*` por tela × delta
  do token) em vez de disfarçar de medição — ordem de grandeza: `/hoje`
  900→~828, `/clientes` 900→~852, `/agenda` 900→~856, Ficha 917→~840-870,
  `/sessoes/.../conduzir` praticamente sem mudança (só 1 `Cartao` na tela).

**Decidido:** manter `--espaco-item`/`--alvo-gap` intocado mesmo sendo o
maior candidato "óbvio" a encolher — é o piso de folga entre alvos de toque
adjacentes (WCAG, público 50+); densidade não pode comer alvo de clique,
regra dura do pedido.

**Pendente com o Marcio:**
- Remedir altura/scrollWidth reais com `next dev` funcionando (falta
  `.env.local` nesta máquina) antes de considerar o número da aritmética
  como fechado.
- Conferir em captura real se `/sessoes/[id]/conduzir` (`PainelOferta`)
  aperta com o padding novo — não editada, só herdada; se apertar, ajuste é
  pontual naquele arquivo, não reversão do token global.

**Notas tocadas:** `05 Decisoes/2026-09-16 - Direcao de UI validada pelo
Marcio (clean e convencional).md` (vault, já existia — referenciada, não
criada agora).

## 21:51 — T2 da repaginação: `/hoje` vira acesso rápido sem rolar

**Pedido:** pedido literal do Marcio — "nunca tenho amostra de acesso rápido
[...] não sei o que preciso fazer [...] o admin tem que ter o controle do
sistema". Fazer os atalhos de `ResumoDoDia` ficarem alcançáveis sem rolar, e
enxugar os títulos de faixa para padrão de separador ERP. Só 3 arquivos
permitidos: `ResumoDoDia.tsx`, `PainelDia.tsx`, `Bloco.tsx`.

**Feito:**
- `ResumoDoDia.tsx`: `<nav>` virou `sticky top-[61px] z-10 lg:top-0`. 61px é a
  altura medida em CSS do header mobile do `AppShell` (`h-11` 44px + `py-2`
  16px + borda 1px) — abaixo de `lg` o header do celular já ocupa o topo,
  então a barra cola logo abaixo dele (`z-10 < z-30` do header, nunca por
  cima). Abaixo de `sm`: `flex-nowrap overflow-x-auto` com `-mx-3 px-3` (compensa
  o padding do `<main>`) e fundo próprio (`bg-papel-fundo/95 backdrop-blur`)
  para não vazar texto por baixo ao rolar; de `sm` para cima volta a
  `flex-wrap`, sem fundo, sem `-mx`. Itens ganharam `shrink-0` — o filho rola,
  a página não.
- `PainelDia.tsx`: os três `<h2 className="text-subtitulo font-bold">`
  ("Precisa de você agora", "Ainda esta semana", "Sistema") viraram
  `text-rotulo font-semibold text-tinta-fraca` (12px) com uma régua horizontal
  (`<span aria-hidden className="h-px flex-1 bg-linha">`) ao lado, mesmo
  padrão já usado em `Cartao`/`Kpi`/`Tabela` do design system — não inventei
  componente novo. `aria-labelledby` das três `<section>` inalterado (mesmo
  id, mesmo `<h2>`, só a classe mudou). `TudoCerto` desceu para depois das
  três seções (inclusive "Sistema"), sempre no rodapé — nunca mais pode cair
  entre duas faixas com trabalho.
- `Bloco.tsx`: **não tocado** — nenhum dos 4 itens do pedido pedia mudança
  nele; ele já tinha o modo "uma linha" da Fase 6 (nada a refazer).

**Divergência do plano — uma linha:** o item 3 pedia `TudoCerto` "colapsado em
uma linha (‘5 blocos sem pendência · ver’)"; o componente `TudoCerto.tsx` já
é uma linha só, mas hoje lista os nomes inline em vez de esconder atrás de um
"ver" — mudar esse comportamento exige editar `TudoCerto.tsx`, fora da lista
de arquivos permitidos (só `ResumoDoDia`/`PainelDia`/`Bloco`). Proposta de
ajuste: liberar `TudoCerto.tsx` numa próxima fatia para trocar a lista inline
por `<details><summary>N blocos sem pendência · ver</summary>…</details>`.

**Medido:**
- `npx tsc --noEmit`: limpo.
- `npx eslint src`: 0 erros, 1 warning pré-existente (`ConduzirSessaoApp.tsx:183`,
  arquivo não tocado).
- `NODE_OPTIONS=--max-old-space-size=6144 npx vitest run --no-file-parallelism`:
  **1357 passed, 1 skipped, 85 arquivos** — baseline mantida, sem regressão.
- `node scripts/contraste.mjs`: nenhuma falha nova, 3 exceções documentadas
  idênticas ao DS §6 (não mexi em cor).
- Altura a 1440×900 e `scrollWidth` a 390px **não medidos no navegador**:
  falta `.env.local` nesta máquina (mesma pendência já registrada em
  21:40) — não procurei credencial, conforme a regra do pedido. Estimativa
  por aritmética declarada: economia de ~11px por faixa (título 28px→17px)
  ×até 3 faixas = ~30-35px de altura de documento; o ganho principal não é
  de altura, é da barra `sticky` ficar disponível durante toda a rolagem —
  efeito que aritmética de altura não capta.

**Pendente com o Marcio:**
- Remedir `document.body.scrollHeight`/`scrollWidth` reais com `next dev`
  funcionando (mesma pendência do `.env.local` de 21:40) antes de fechar o
  número "Precisa de você agora inteira visível sem rolar a 1440×900".
- Decidir se `TudoCerto.tsx` entra numa fatia própria para virar
  `<details>` colapsado (ver divergência acima).

**Notas tocadas:** `05 Decisoes/2026-09-16 - Direcao de UI validada pelo
Marcio (clean e convencional).md` (vault, já existia — referenciada, não
criada agora).
