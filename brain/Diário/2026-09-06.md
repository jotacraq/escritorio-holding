# 2026-09-06

## 00:20 — Fechamento de sessão: .env de produção, decisões delegadas, próxima fase, acesso da Dra. Elaine

**Pedido:** "anota tudo, cria um env com todas as variáveis para eu colocar na host (em Downloads), mantenha as decisões que você tomou, organiza e fim de sessão"; depois: "cria um acesso de admin com elaine@advmais.com".
**Feito:** `C:\Users\João\Downloads\sic-hf-producao.env` com as 30 variáveis lidas pelo código (legenda PRONTO/MANTER/VOCÊ/OPCIONAL; prontos: Supabase, service_role, CRON_SECRET novo, ligação por IA completa; VOCÊ: Resend, Hotmart, OpenRouter; MANTER: pepper). Decisões: A4 mantido (jornada nova após `ganha`), `LIMITE_ARQUIVOS_POR_LINK` 5→10 (`src/server/publico/documento.ts:28`), Ficha avançada fica para a próxima fase. `CONTINUAR-AQUI.md` com bloco "Fechamento de sessão" + próxima fase. Usuário de auth `elaine@advmais.com` criado com senha (e-mail confirmado) e ligado ao perfil `admin` "Dra. Elaine Montenegro" (a linha placeholder `elaine@exemplo.com.br` foi reaproveitada: e-mail, papel, nome e `auth_user_id` atualizados).
**Medido:** `tsc` 0 · `eslint` 0 · produção em `3d6d8b0` conferida (`/painel` → 308 → `/hoje`).
**Decidido:** próxima fase é melhoria contínua sobre o que está — não é feature nova. A senha entregue é a mesma do basic-auth do dashboard (cron do hPanel) — recomendado trocar no primeiro acesso.
**Pendente com o João:** colar o `.env` na Hostinger + Variable no n8n + contas Resend/Hotmart/OpenRouter; apagar os dois arquivos de segredo depois; trocar a senha da Dra. Elaine.
**Notas tocadas:** `CONTINUAR-AQUI.md`, [[Seguranca]].

## 14:30 — Fase 7 "Pronto para apresentar": diagnóstico e rodada 1 lançada

**Pedido:** "revisa o projeto, sugere melhorias, finaliza o agente de ligação (pronto, testado, validado), melhora os processos como um todo; não precisa me retornar — o projeto é tua responsabilidade; o que só eu posso fazer, lista no final."
**Feito:** reconhecimento por 2 agentes Sonnet (ligação ponta a ponta · levantamento de melhorias) + sondas do orquestrador. Brief da squad em `tmp/squad/fase7-brief.md` (fronteiras de arquivo, 3 agentes Opus em paralelo: LIG full-stack da ligação · SEG segurança/pendências de back · UX polimento).
**Medido:** produção em `b49073e` (= HEAD sem o carimbo). n8n LANCADOR `zh5tjDcSoHaPaRRL`: 1 execução (ping de 05/09 22:40); WEBHOOK `OXetB37jgJgmif3d`: 0 execuções — **nenhuma ligação real aconteceu**. `POST /api/cron/regua` com o `CRON_SECRET` do arquivo → 401 (env não colada na Hostinger). Ping assinado no LANCADOR → 401 `variavel_LIGACAO_IA_WEBHOOK_SECRET_ausente` (Variable não criada no n8n). `tsc` 0 · `eslint` 0 (1 aviso em `tmp/mock`). Sem suíte de testes (3 scripts `tsx` manuais). 124 rotas de API. Chatwoot: canal de envio + webhook de entrada JÁ codificados (20 arquivos), só sem env.
**Decidido:** rodada 1 = ligação madura (janela de discagem, E.164, fallback sem revogar link humano, `VAPI_ASSISTENTE_ID` obrigatório, retenção LGPD desligada por padrão, nós n8n versionados no repo + vitest) ‖ pendências da trava da Fase 6 (rate limit de links, `criado_por`, grant anon 0038, gap `links` da Pasta, rotas órfãs, varredura fail-open) ‖ UX (Ficha ≤1080 px, repertório paginado, sr-only do trilho, contraste AA, consistência das 5 telas). Depois: pentester → trava do Fable → migrations 0073/0074 via MCP → build → push nos dois remotes.
**Pendente com o João:** Variable no n8n + envs na Hostinger (sem isso a ligação real não acontece; código fica pronto e o roteiro de 10 minutos vai em `docs/integracoes/n8n-ligacao-ia.md` §8).
**Notas tocadas:** `tmp/squad/fase7-brief.md`, `CONTINUAR-AQUI.md` (ao fechar).

## 11:00 — Fase 7 · rodada 1 executada: ligação por IA madura, pendências da Fase 6, polimento, pentest, trava

**Pedido:** (continuação) "mete marcha, organiza os agentes, deixa 100% pronto".
**Feito:** 3 agentes Opus em paralelo (LIG · SEG · UX), + 0075 (limite de arquivos unificado), + pentest (Fable) + correções do LIG + trava do Fable + agente FIX. Migrations **0073** (janela/retenção/token cifrado), **0073b** (grant de SELECT por coluna em `ligacoes_ia` — a `revoke select (coluna)` da 0073 era no-op com SELECT de tabela), **0074** (`criado_por` em `emitir_link_confirmacao_sistema`, revoga EXECUTE de `anon` em `app.registrar_evento_timeline`), **0075** (`app.limite_arquivos_por_link()` lê `configuracoes['link.limite_arquivos']=10` — o 5→10 de 06/09 era inerte: o banco recusava no 6º) — todas aplicadas via MCP e provadas (7/7 · 10/10 · 6/6). Nós do n8n versionados em `n8n/ligacao/*.js` e **republicados** (WEBHOOK `9361f30d…`, LANCADOR `a4d10e8b…`). Vitest: 0 → 167 testes. Rotas mortas removidas (5 do SEG + 2 GET de `ligacoes-ia`); `/api/admin/decisoes-juridicas` restaurada (caminho documentado da decisão B13).
**Medido:** pentest: 0 crítico · **1 ALTO (A1)**: webhook Vapi→n8n sem autenticação + `callback_url` do metadata = SSRF + oráculo de assinatura → fechado (x-vapi-secret com `timingSafeEqual`, destino fixo `$vars.SICHF_CALLBACK_URL`, LANCADOR recusa discar sem as duas Variables, `id_externo_divergente` ignorado, rate limit por IP+ligação). Sonda pós-publicação: webhook forjado → erro fatal "VAPI_SERVER_SECRET ausente" (nada assinado). Bug real no nó publicado (achado do LIG): sem truncagem, `recordingUrl:""` → 422 e evento perdido, inclusive com horário escolhido — corrigido. Trava do Fable: segurança/escalabilidade/solidificação APROVADO; UX e otimização REPROVADO (botão inexistente citado na tela; `link.limite_arquivos` sem rota de PATCH; item `links` da Pasta era código morto desde a Fase 6) → agente FIX em andamento. `npm run build` exit 0.
**Decidido:** token do link do sistema cifrado (AES-256-GCM v2 com AAD=id da ligação, chave por HKDF do pepper) em vez de 2 links ativos; janela padrão seg–sex 9h–19h SP (rotulada VALOR INICIAL); retenção de voz `null` = não expurga (B19 pendente); `.env.local` limpo das envs de produção da ligação (cron local não pode discar). `assistantOverrides.server` NÃO existe na API da Vapi → o Server Secret vai no assistant (João).
**Pendente com o João:** ver lista final no `CONTINUAR-AQUI.md` (Variables do n8n: 3; Server Secret na Vapi; conferir credencial do nó DISPARO; envs na Hostinger).
**Notas tocadas:** [[Estado Atual]] (reescrita), `docs/integracoes/n8n-ligacao-ia.md` §8.4/§8.5, `n8n/README.md`, `tmp/squad/fase7-brief.md`.

## 13:10 — Fase 7 fechada: rodada 2 (CI, demo, acabamento) publicada em `9bf5ed6` (carimbo `2a27673`)

**Pedido:** (continuação da ordem das 14:30) "100% polido para apresentar; o que só eu faço, lista".
**Feito:** rodada 2 com 4 agentes Opus (CI · DEMO · UX2 · BE2) + pentest r2 (Fable) + correções (DEMO, UX3) + trava r2 (Fable, reprovou 1× por número do roteiro que a tela não mostrava; aprovado na reauditoria). `.github/workflows/ci.yml`; 3 scripts `tsx` → vitest (149/75/39 casos idênticos); `scripts/seed-demo.ts` (4 famílias fictícias, guarda de `origem_dado` antes de todo DELETE, régua cancelada na hora); `docs/APRESENTACAO.md`; migrations **0076** (drift: `registrar_briefing` perdera na 0042 a derivação de `origem_dado` — todo briefing em modo demonstração falhava desde 04/09) e **0077** (`for update` em `resolve_link_escrita`; briefing só com execução da própria jornada) aplicadas e provadas; upload público com pré-checagem por `usos` (uma query a menos); `text-xs`→`text-legenda` (80 arquivos); mobile 390 px em 31 telas (Cartão, alvos 44 px, `sr-only` do croqui fora do containing block); `/p/*` com rodapé de contato + aria; `rotuloOpcao` ("Viúvo(a)", "União estável"); README de deploy corrigido (não é standalone).
**Medido:** `tsc` 0 · `eslint` 0 · vitest **470/470** (16 arquivos) · `npm run build` ok · 62 capturas 1440/390 sem tela em branco · banco: 6 pessoas (5 exemplo + 1 real), 4 jornadas de demo, 8 mensagens de régua todas `cancelada`, 0 ligação `na_fila`. Produção rodou a rodada 1 (`/versao.txt` = `4356e1b`) antes do push da 2.
**Decidido:** banco FICA com a demo (o João apresenta); `ligacao_ia.automatica` fica `true` (decisão dele de 05/09) com aviso no CONTINUAR-AQUI; vault do Obsidian recebe cópia das notas tocadas (Estado Atual + este diário).
**Pendente com o João:** bloco "SÓ O JOÃO" no topo de `CONTINUAR-AQUI.md` (Hostinger, 3 Variables do n8n, Server Secret na Vapi + credencial do nó DISPARO, 1ª ligação real, ruleset do GitHub, decisões com a Dra. Elaine).
**Notas tocadas:** `CONTINUAR-AQUI.md` (bloco Fase 7 no topo), [[Estado Atual]], `docs/APRESENTACAO.md`, `docs/integracoes/n8n-ligacao-ia.md` §8.5, `README.md`.

## 13:25 — CI verde no GitHub e produção na rodada 2

**Feito:** o 1º run do `ci.yml` reprovou 2× em `npm ci` (lockfile do Windows sem `@emnapi/core`/`@emnapi/runtime` 1.11.3, deps do `sharp-wasm32` — entradas acrescentadas à mão com integridade do registro; `npm ci --dry-run` fecha) e 1× em `tsc` (`LayoutProps` é tipo gerado pelo Next — `typecheck` passou a rodar `next typegen` antes). Run `34044647718`: **success em 1m52s** (tipos · lint · 470 testes · build). Commits `0f30b61`, `b41b062`.
**Medido:** `/versao.txt` em produção = `9bf5ed6` (rodada 2 no ar; os commits seguintes são lock/CI/doc, mesmo código).
**Pendente com o João:** ruleset de branch nos dois remotes agora que o check `tipos · lint · testes · build` existe.

## 17:29 — Fable r3: trava de tela (olho de tela) da Fase 7 rodada 3

**Pedido:** validar em navegador real (Chromium via npx cache) os 9 pontos da Fase 7 r3 (admin/formulários/roteiros/titulares/ficha/navegação/link inválido/mobile/tema escuro) sem editar código nem publicar nada em produção.
**Feito:** scripts em `tmp/squad/fable-r3-parte1.mjs`, `-parte2.mjs`, `-parte2b/c.mjs`, `-parte3.mjs` + debug (`fable-r3-debug-mover.mjs`, `-debug-ficha*.mjs`); capturas em `tmp/squad/capturas/fable-r3/`.
**Medido:** todos os 9 itens PASSARAM. Zero erro de console/pageerror e zero 4xx inesperado em todas as telas (só 404 esperado de `/api/publico/abcdef123` no item 7). Dev server subiu e foi derrubado ao final (porta 3000 confirmada livre via netstat).
**Decidido:** nenhuma correção de código — só validação. Nenhum achado bloqueante.
**Pendente com o João:** nenhum.
**Notas tocadas:** —
