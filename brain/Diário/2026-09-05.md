# 2026-09-05

## 00:10 — Fase 4 · retomada ("continue") e fechamento das ondas 2, 3 e 4

**Pedido:** o João mandou "continue" sobre a Fase 4 (brief em `tmp/squad/fase4-brief.md`): sistema completo, identidade do seminário no sistema inteiro, esteira automatizada (Hotmart → ligação por IA → confirmação D-7 → sala → sessão → material PDF → croqui), times de Fable com um agente geral administrando. No meio da sessão, duas ordens novas: **cuidado com tokens** e **"Sonnet só pega contexto; codificação, raciocínio e modelagem são Fable"** (um agente de costura lançado em Sonnet foi parado e relançado em Fable; regra gravada na memória do Claude desta máquina).

**Estado ao entrar:** Onda 1 (agentes A–F, V1, V2) já estava na árvore, sem commit; `tsc` limpo; migrations 0050–0059 escritas. `list_migrations` mostrou que 0050–0058 já tinham sido aplicadas na sessão anterior (05/09 00:25–00:32 UTC) — só a 0059 faltava.

**Feito (por onda):**
- **Onda 2 (3 Fable em paralelo):** G — Comunicação reescrita como "O que vai sair e quando" (prova de vida do cron, grupos por data, "Preparar e copiar", Recebidas do Chatwoot com "Vincular"), rotas `GET /api/mensagens/recebidas` e `POST .../[id]/vincular`, Admin com abas Integrações/Parâmetros/Modelos de material + 8 abas migradas ao design system, `url_checkout` em Produtos, 11 schemas novos em `server/admin/configuracoes.ts`. H — Ficha 360: Sessão (presença, sala, ligação IA, tarefa "enviar link do croqui"), Material (PDF), Relatório → gaveta Cenário Patrimonial, Diagnóstico da SV (montar/editar/visibilidade por bloco/apresentar), Briefing "Como ele fala", Pasta com selos. I — `/p/c/[token]` (confirmação de presença em um toque), `/p/m` com "Baixar PDF", `Apresentacao` genérica (teclado, notas, tela cheia) usada pelo croqui e pelo diagnóstico, gráficos de cenário com legenda de procedência, `PainelOferta` com preço da API, Conduzir Sessão no design system, públicas revisadas.
- **Onda 3:** K (Fable) — `PATCH /api/jornadas/[id]/sessao {link_sala}`, migration **0060** (`vw_cenarios_totais`: total NULL enquanto faltar rubrica padrão — achado do H; `pdf_disponivel` no payload de `/p/m`), tipos em `lib/api.ts`, `GET /api/croquis/[id]` com `graficos.cenario`, `scripts/verificacao-fase4.sql` + `docs/ARQUITETURA-FASE-4-VERIFICACAO.md`, `scripts/limpeza-teste-h.sql`. L (Fable, depois de parar a versão Sonnet) — costura: eslint zerado (`LigacaoAba.tsx`), diagnóstico usando `Apresentacao`, âncoras da Ficha/Admin, `Cartao` com `flex-wrap`, alvos < 44px, `AvisoInline` em `ui/`, grep de `rounded-sm|text-[10px]|font-semibold` = 0, mais 4 achados de layout corrigidos (Esteira 2181px de scroll, tabela do Admin, Admin#prompts a 390, chip da Agenda).
- **Onda 4:** pentester (Fable) — 0 crítico/alto no relatório, 4 BAIXO, 3 INFO + SQL de prova. O orquestrador rodou o SQL no banco e achou **1 ALTO real**: usuário autenticado sem perfil chamava `registrar_diagnostico_sv` e gravava (gates `ve_patrimonio()`/`eh_admin()` da 0001 devolviam NULL sem perfil). Backend (Fable) → migration **0061**: gates com `coalesce(..., false)` (fecha 0027/0030/0033/0055/0056/0058 de uma vez), CHECK https em `link_sala`, `n8n/ligacao` deixa de ter `id_evento` ocupado por tentativa inválida (`server/integracoes/livro-razao.ts`, compartilhado com `n8n/sala`), trigger de vínculo coerente em `mensagens_recebidas` + revoke de colunas, `cenario_rubrica_calcula` exige parâmetro ativo e UF coerente, `presenca_confirmada_em` carimbado pelo servidor, `diagnosticos_sv.atual` só pela RPC e aprovação com o próprio perfil.
- **Banco (via MCP, depois do OAuth do João):** aplicadas 0059, 0060, 0061. Roteiro da Fase 4 rodado: **17/17 OK** (presença idempotente, núcleo de horário, ligação IA → agendamento origem `ia`, sem sobrecarga em `reivindicar_mensagens_pendentes`, hold de `{{link_sala}}`, tarefa idempotente, `ck_pdf_exige_aprovacao`, cenário 23514/NULL, `o_que_falta` nunca visível, `on conflict do nothing`, relacionamento 42501, explain sem Seq Scan, 0 residual). Harness da 0061: **8/8 OK**. Nenhuma linha de teste ficou no banco.
- **n8n:** 2 workflows criados via MCP, não publicados — `SIC-HF · LIGAÇÃO · LANCADOR → Vapi` (`zh5tjDcSoHaPaRRL`) e `SIC-HF · LIGAÇÃO · WEBHOOK Vapi → SIC-HF` (`OXetB37jgJgmif3d`); documentado em `docs/integracoes/n8n-ligacao-ia.md` §7.

**Medido:** `npx tsc --noEmit` = 0 · `npx eslint "src/**/*.{ts,tsx}"` = 0 · `npm run build` = verde (3 vezes: pós-onda 2, 3 e 4) · ~280 arquivos alterados/novos · 138+ capturas em `tmp/squad/capturas/` (não versionadas) · explain: kanban 0,56 ms, sessões do dia 0,06 ms, cenários 0,15 ms.

**Decidido:**
- Sonnet só levanta contexto; Fable codifica, raciocina e valida (ordem do João).
- 0050–0058 não foram reaplicadas (já estavam); 0059→0061 aplicadas sem comentários no texto enviado.
- Dado de teste do H na jornada "(exemplo)" (cenário `custas_cartorio` R$ 15.000, diagnóstico v1–v4) ficou no banco; `scripts/limpeza-teste-h.sql` pronto, decisão do João.
- Workflow de SALA no n8n não criado (depende de escolher provedor de vídeo).
- Prompts v3 (briefing) e v2 (croqui) inseridos INATIVOS; ativação só com sonda + bancada em produção (`docs/ARQUITETURA-FASE-4-VERIFICACAO.md`).

**Pendente com o João:** ver "Configuração pendente" em `CONTINUAR-AQUI.md` §0 (cron do hPanel + `CRON_SECRET`, `RESEND_API_KEY`/`EMAIL_FROM`, Hotmart, envs de n8n/Vapi/Chatwoot, variáveis no n8n, publicar os 2 workflows, `ligacao_ia.provedor='n8n'`), decisão sobre `limpeza-teste-h.sql`, ativar prompts v3/v2 após bancada, trocar a senha do admin de teste.

**Notas tocadas:** [[Seguranca]] (0061), [[Schema]] (0050–0061), [[Deploy na Hostinger]], `docs/integracoes/n8n-ligacao-ia.md`, `CONTINUAR-AQUI.md`.

## 01:50 — Trava final (Fable): APROVADO com ressalva em otimização; ressalvas fechadas

**Veredito:** segurança · escalabilidade · solidificação · UX aprovados; otimização com ressalva (5 restos de código morto + "Próxima ação" da Ficha derivada por outra precedência). Fable mediu 23 telas (2 temas + 390 + 6 cenários de `/p/c`): 0 pageerror, 0 console, 0 fonte < 12px, 0 scrollX, 1 h1; `setInterval` só no cronômetro de `Progresso`; `7200|4500|VALOR_PADRAO_CROQUI` em `src` = 0.
**Feito pelo orquestrador (Fable) em seguida:** `CabecalhoFicha.tsx` passou a usar `derivarProximoPasso(sinaisDaFicha(ficha))` + `hrefDoPasso` (mesma fonte da Esteira/Painel/Agenda; `derivarPasta` deixou de rodar 2× por render); apagados `ui/ChecklistPendencias.tsx`, `croqui/apiCroqui.ts` (fallback de fetch removido de `ModoApresentacao.tsx` — a página já passa `cenario`/`alocacao`), `admin/ConfirmarAcao.tsx` (re-export; `BriefingAba` importa de `ui/`), `MensagemAgendada`/`listarMensagens`/`marcarMensagemEnviada` mortos em `lib/api.ts`. `ui/pendencias.ts` ficou: `lib/pasta/catalogo.ts` importa dele.
**Riscos residuais registrados (não bloqueantes):** R3 remarcação pela equipe e `POST jornadas/[id]/agendamentos` ainda fora do núcleo (C26/Fase 5); R4 token do link de agendamento da ligação IA só em memória (reinício emite link novo); R5 `0061` deriva jornada aberta sem `order by` (pessoa com 2 abertas → arbitrária); R6 cron 50 × 15 s de timeout do Resend = até 12,5 min com provedor fora (pré-existente).

## 02:40 — Análise do João + leitura do Drive + proposta da Fase 5

**Pedido:** o João viu as telas: gostou da UX mas "muito texto, não está pontual/direto"; blocos grudados e fonte "entortada"; coisa de dev na tela inicial; automações com resultado visível na ficha; documentos a enviar "na cara"; **"onde vejo o croqui? o croqui apresenta dados (1/2/3 células, economia)"** — mandou a pasta de um cliente no Drive como referência e pediu solução fora da caixa. Nova divisão de modelos: Sonnet reconhece · Opus codifica · Fable audita/inova (memória atualizada). O arquiteto Fable lançado antes morreu por limite de sessão (429).
**Feito:** lidos no Drive os modelos `1) RELATÓRIO DA SESSÃO DE VIABILIDADE`, `3) RELATÓRIO DO CROQUI`, `4.1) APRESENTAÇÃO DE CROQUI COM OPERACIONAL` (a pasta da cliente não lista filhos pela API). Estrutura + fórmulas inferidas em `brain/06 - Materiais/Modelo real do Croqui e da Sessao (Drive).md` (sem PII). Proposta em `docs/ARQUITETURA-FASE-5.md` §0–§3: **Motor do Croqui determinístico** (17 tabelas, procedência por número, reforma como cenário paralelo, IA só narra), simulador ao vivo na sessão, trilho de 7 passos, automações com resultado na ficha, radar de documentos, painel por papel, dicionário, lei de texto. Arquiteto **Opus** lançado para detalhar §4.
**Medido:** nada rodou; só leitura.
**Decidido:** croqui deixa de ser texto de IA e vira cálculo do método (o Drive é a fonte); `parametros_metodo` é a base dos parâmetros; nada de zero — célula `ausente` com motivo.
**Pendente com o João:** validar as fórmulas inferidas com a Dra. Elaine (cartório 1%, honorários do inventário 5%, deságio 20%, IR 15%, ITBI 3% × (mercado − DIRPF) dos imóveis, reforma 20%); alíquotas de ITCMD por UF; credencial do Drive para exportar.
**Notas tocadas:** [[Modelo real do Croqui e da Sessao (Drive)]], `docs/ARQUITETURA-FASE-5.md`, `tmp/squad/fase5-brief.md`.

## 09:20 — Fase 5 · ondas 1 e 2 fechadas, banco 0062–0067 aplicado, costura em andamento

**Pedido:** executar a Fase 5 sem aprovação ("confio em você"); instalar `find-skills` (vercel-labs/skills) e usar na squad; Sonnet reconhece · Opus codifica · Fable orquestra/audita/inova.
**Feito:**
- Recon (Sonnet) dos 57 arquivos da pasta real do cliente → `brain/06 - Materiais/Processo real do escritorio (Drive).md` (sem PII) + `tmp/squad/drive-cliente-exemplo.md` (local). Corrigiu a nota anterior: hora R$ 1.800, honorários de inventário 7%, incentivo SV 2.400, ITCMD/ITBI/IR por **faixas**, base do ITCMD = mercado em 1/2 células e DIRPF só em 3.
- Skills instaladas (`docs/SKILLS.md`): find-skills, supabase, supabase-postgres-best-practices, vercel-react-best-practices, web-design-guidelines, docx, pdf, webapp-testing.
- Arquiteto Opus: `docs/ARQUITETURA-FASE-5.md` rev. 04:20 (motor com faixas, `croqui_calculos`, trilho 9 passos, automações, radar, execução, vocabulário, painel por papel, `.docx`, minutas na Onda 3).
- **Onda 1 (Opus):** M1 motor do croqui (19 tabelas, 123/123 testes, 0062/0063) · M2 trilho/automações/radar/execução (49/49, 0064/0065/0067) · M3 vocabulário + painel por papel + lei de texto + ritmo tipográfico (Painel 278→108 palavras, Comunicação 293→85).
- **Onda 2 (Opus):** M4 croqui de 19 tabelas + apresentação do método + simulador ao vivo (0 requests ao editar) + schema de narrativa 2.048 bytes + 0066 · M5 `ui/Trilho`, Ficha com Pasta como porta única, Sessão em linha de passos, automações e radar reais, upload com `item_ref` (Ficha 225→158, Sessão 189→105) · M6 `.docx` do relatório do croqui (19 seções, célula ausente sempre `—`, Drive fail-closed).
- **Banco:** 0062, 0063, 0064, 0065, 0067, 0066 aplicadas + correções do orquestrador **0062b** (view de faixas lia objeto como array), **0065b** (revoke all antes de grant por coluna — o `alter default privileges` do Supabase dava ALL a `authenticated`) e **0065c** (varredura: 51 tabelas com DELETE e ~20 com INSERT sem policy → revogados). Verificação final: 0 DELETE/INSERT sem policy, 0 grant para anon, 0 tabela sem RLS. Harness M2 5/7 (2 falhas = fixture + o achado que virou 0065b); intruso em `registrar_croqui_calculo` → 42501.
**Medido:** `tsc` 0 · `eslint` 0 · `build` verde (2×) · 108 arquivos alterados sem commit.
**Decidido:** M7 (minutas + sub-esteira) fica para depois do veredito do Fable; `.docx` no Drive fail-closed até `GOOGLE_SA_JSON`/`DRIVE_PASTA_RAIZ_ID`; nenhum prompt ativado.
**Pendente com o João/Dra. Elaine:** cadastrar alíquotas de MG (10 chaves) e `croqui.horas_por_ato` (sem isso nenhum croqui fecha — por desenho); reconciliar divergências (certidões 2.000×7.000, membership, IBS/CBS 26,5×36,92, junta 500×511); confirmar base do ITCMD por modelo; env do Drive; regra de nome da pasta.
**Notas tocadas:** [[Processo real do escritorio (Drive)]], [[Seguranca]] (privilégio herdado), `docs/ARQUITETURA-FASE-5.md`, `docs/SKILLS.md`, `docs/DESIGN-SYSTEM.md`.

## 10:20 — Fase 5 · costura (C1/C2), 0068 aplicada

**Pedido:** fechar a costura entre as ondas 1 e 2 antes do pentest.
**Feito:** C1 (Opus, backend) — `/p/d` com `item_ref` validado (chave opaca no navegador), `/p/m` filtrado na serialização (15 tabelas públicas; 0 `honorarios/deducoes/parametro`), motor 13→5 consultas, `Celula.unidade`, `lerOverrides` por cenário+rubrica, **bug real na Ficha**: único leitor sem `.eq('ativo', true)` mostrava bens/familiares já baixados (aba Patrimônio da jornada exemplo passa de 10 para 1 — o número verdadeiro). Migration **0068** (`registrar_documento_publico` com `p_item_ref`) aplicada. C2 (Opus, frontend) — vocabulário em `lib/pasta`, "MQL" traduzido na leitura, rotas órfãs apagadas, DESIGN-SYSTEM §3.1, botão "Baixar relatório (.docx)" com `?info=1`.
**Medido:** palavras fora de dado (HEAD → agora): Painel 278→108 · Comunicação 293→85 · Ficha 225→137 · Sessão 189→104 · Esteira 181→130 · Agenda 150→97 · Admin 113→88; harness `verificacao-0063-0068.sql` 7/8 (a falha era regex do próprio harness).
**Decidido:** meta ≤ 50 % não bate em Ficha/Esteira porque o que sobra é nome de artefato, não prosa — aceito. M7 (minutas) fica para a próxima rodada.
**Notas tocadas:** [[Schema]] (0068), `docs/DESIGN-SYSTEM.md`.

## 12:10 — Fase 5 · pentest (Fable) e correção 0069

**Pedido:** onda 4 do pipeline — pentester obrigatório (patrimônio + documento + RPC nova).
**Feito:** pentester (Fable) leu 0062–0068 inteiras + rotas novas, curl no dev server com e sem sessão, PostgREST anon/admin, mock da serialização do `/p/m`, SQL de prova em `tmp/squad/pentest-fase5.sql`. **0 crítico/alto; 1 MÉDIO + 4 BAIXO + 10 INFO.** O MÉDIO: `registrar_croqui_calculo` (0063) era `security definer` com EXECUTE para `authenticated` e aceitava `p_resultado`/`p_entrada`/`p_parametros` do chamador — "o resultado nunca vem do cliente" valia só na rota (422 zod strict), não no banco. Backend (Opus) → migration **0069**: `app.perfil_ve_patrimonio(uuid)` (gate pelo PERFIL, porque sob `service_role` não há `auth.uid()`), `registrar_croqui_calculo`/`fixar_croqui_calculo` recriadas com `p_criado_por` e EXECUTE só de `service_role` (drop da assinatura antiga — armadilha das sobrecargas), `croqui_id` imutável, trigger `trg_documento_item_ref` zera `item_ref` de outra pessoa; `servico.ts` grava via `criarClienteAdmin()` (leitura continua na sessão, pela RLS), sem chave → 503 rotulado.
**Medido:** prova no banco pelo orquestrador — admin autenticado chamando a RPC direto → **42501**; `update croqui_id` → 42501; `item_ref` de outra pessoa → NULL; `has_function_privilege(authenticated)` = false / `service_role` = true; 1 assinatura por RPC; residual 0. `tsc` 0 · `eslint` 0 · `build` verde.
**Decidido:** gate de papel sob `service_role` vive no banco (`perfil_ve_patrimonio`), não na rota — a rota é cinto, o banco é a trava.
**Notas tocadas:** [[Seguranca]] (0069), [[Schema]].

## 14:30 — Fase 5 · trava final do Fable: 2 reprovações, 0070, item cirúrgico

**Pedido:** veredito vinculante dos 5 critérios.
**Feito:**
- **1ª rodada (REPROVADO em solidificação + otimização).** Solidificação: `sinaisDaFicha` inventava `croquiStatus = "pronto"` para QUALQUER evento `tipo='croqui'` sem `dados.status`, e a fase criou dois escritores desse tipo sem status (trigger `app.timeline_croqui_calculo` da 0063 e o `.docx`) → fixar versão ou baixar relatório fazia Pasta/trilho dizerem "croqui pronto — apresentar" com croqui em rascunho. `ApresentarCroqui` não registrava iniciar/encerrar (`croqui_apresentacoes` morta). Otimização: código morto novo (`DeckImpressao`, `buscarCroquiParaApresentar`, `?modo=apresentacao`, `fixar_croqui_calculo` sem rota) e dois "croquis" na mesma aba. Correção F1/F2 (Opus) → migration **0070**: tipos próprios `croqui_calculo`/`croqui_exportacao`/`croqui_narrativa` na timeline (+ backfill, no-op: 0 linhas), `search_path` nos 4 triggers da 0065/0067, grants explícitos de `vw_automacoes_jornada`, tabela `croqui_narrativas` + RPC `registrar_croqui_narrativa` (só `service_role`, gate por perfil); `sinais.ts` só lê evento croqui com `status` legível; apresentação registra iniciar/encerrar (`keepalive`); narrativa v3 ganhou consumidor (rota + `NarrativaCroqui`, fail-closed 409 enquanto o prompt está inativo); aba Croqui com uma verdade (19 tabelas) e o editor v1 recolhido.
- **2ª rodada (REPROVADO em otimização, 1 item):** `POST .../croqui-calculo/[calculoId]/fixar` tinha 0 chamadores — "RPC sem rota" virou "rota sem botão". Orquestrador (Fable, 3 arquivos): botão **"Fixar esta versão"** na gaveta de versões (`CroquiCalculado.tsx`, `fixarVersaoCroqui`), `calcular.ts` devolve `faltas: [{chave:'motor.erro_interno'}]` em vez de vazio silencioso, comentário obsoleto de `GraficoDoSlide.tsx` corrigido. Prova Playwright (`FABLE3-*.png`): botão ausente na versão atual, presente nas outras, 200 `ja_era_atual`.
**Medido:** teste-trilho 55/55 · motor 143/143 · Playwright do fluxo real: `POST .../apresentacao` 201 iniciar / 200 encerrar, `encerrada_em` confirmado no banco · `tsc` 0 · `eslint` 0 · `build` verde.
**Decidido (orquestrador, em nome do João):** o croqui de 19 tabelas É o produto; o editor de 13 slides fica atrás de "Narrativa da IA (versão anterior)", nada apagado de dado; IA v1 continua ativa até a bancada aprovar a narrativa. `LIMITE_ARQUIVOS_POR_LINK` e nome da pasta do Drive continuam com o João.
**Notas tocadas:** [[Schema]] (0070), [[Seguranca]] (0070).

## 19:30 — Fechamento da Fase 5 (a CLI caiu entre a correção e o commit)

**Pedido:** "continue de onde paramos" — a sessão anterior morreu depois do item cirúrgico da 2ª rodada, antes do commit.
**Feito:** roteiro `scripts/verificacao-0070.sql` rodado no banco (era o único passo sem saída colada): 5/7 na primeira; as 2 falhas eram do ROTEIRO, não do banco — (a) regex de PUBLIC `'%=X/%'` casava com `postgres=X/postgres` (falso positivo); (b) fixture inseria `croquis.origem_dado`, coluna que não existe, e exigia zero evento `croqui` numa jornada onde o próprio INSERT em `croquis` gera um (com status). Corrigido no script (PUBLIC = `'{=X/%' or '%,=X/%'`; passo 6 compara eventos croqui antes/depois da RPC + zero sem status) → **7/7 OK**, resíduo 0. Build final do zero (`rm -rf .next`) verde. Brain, `CONTINUAR-AQUI.md` e memória atualizados; commit + push em `origin` e `infra` (deploy automático).
**Medido:** 0070 no banco: evento de cálculo = `croqui_calculo` (0 `croqui` de carona) · 17 eventos croqui, 0 sem status · view: anon fora, authenticated só SELECT · 4/4 funções com `search_path=public, pg_temp` · `croqui_narrativas` RLS forçada, RPC 1 assinatura, EXECUTE só `service_role`, PUBLIC não · gate por perfil recusa `relacionamento` (42501) e grava com advogada (atual, schema 3, real, v1) · `tsc` 0 · `eslint` 0 · `BUILD_EXIT=0`.
**Decidido:** trava final da Fase 5 = **APROVADA** (2ª rodada do Fable + item cirúrgico fechado com prova; nada mais pendente nos 5 critérios).
**Pendente com o João:** lista de configuração em `CONTINUAR-AQUI.md` (topo) — alíquotas por UF + `croqui.horas_por_ato` (sem isso nenhum croqui fecha, por desenho), divergências dos parâmetros, env do Drive, decisão `LIMITE_ARQUIVOS_POR_LINK`, limpeza das 27 `croqui_apresentacoes` abertas de fixture, bancada antes de ativar a narrativa v3.
**Notas tocadas:** [[Schema]], [[Seguranca]], `CONTINUAR-AQUI.md`.

## 20:10 — Drive era referência, não padrão: destino "Google Drive" removido

**Pedido:** o João, ao ler o resumo: "o Drive eu usei pra você pegar como referência, não pra usar como padrão" / "era referência e contexto por conta dos arquivos que existiam ali dentro".
**Feito:** apagado `src/server/exportacao/destino.ts` (adaptador Drive por service account, pasta `HOLDING DRIVE - <cliente>`, JWT RS256, upload multipart); `api/croquis/[id]/docx` ficou só `GET` (download + `?info=1` com `calculo`), `POST ?destino=` removido; `BaixarRelatorio.tsx` sem "Enviar ao Drive"; `NOME_ARQUIVO_DRIVE` fora de `docx-croqui.ts`; comentários de `limite.ts`/`banco.ts`; `docs/ARQUITETURA-FASE-5.md` §10.1 e bloqueio 5; `CONTINUAR-AQUI.md` item 3 da configuração pendente. O que veio do Drive como **referência** fica: fórmulas, faixas, 19 tabelas, 19 marcos, formato do relatório.
**Medido:** `tsc` 0 · `eslint` 0 · grep `GOOGLE_SA_JSON|DRIVE_PASTA_RAIZ_ID|NOME_ARQUIVO_DRIVE|ErroDrive|Enviar ao Drive` em `src`/`scripts` = 0 · build (ver commit).
**Decidido:** material de referência vira regra de negócio e formato de saída; não vira integração, convenção de nome nem pendência de env em cima da ferramenta onde o material vivia. Regra gravada na memória do Claude desta máquina.
**Pendente com o João:** nada novo; a lista de configuração perde o item do Drive.
**Notas tocadas:** `CONTINUAR-AQUI.md`, `docs/ARQUITETURA-FASE-5.md`.
