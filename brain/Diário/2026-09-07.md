# 2026-09-07

## 00:20 — Retomada: configuração manual (n8n Variables / Hostinger) — ponto zero medido

**Pedido:** "continuar de onde paramos" na configuração das variáveis do n8n.
**Feito:** medição do estado real antes de qualquer passo. Valor da 1ª Variable copiado para a área de transferência do João; página `/variables` do n8n aberta no navegador.
**Medido:** produção em `8fce0e6`; `POST /api/cron/regua` com o `CRON_SECRET` do arquivo → **401** (envs NÃO coladas na Hostinger); ping assinado `teste:true` ao LANCADOR → **401 `variavel_LIGACAO_IA_WEBHOOK_SECRET_ausente`** (nenhuma Variable criada no n8n); últimas execuções dos 2 workflows são as sondas de 06/09 13:36 — nenhuma ação do João desde então.
**Decidido:** nada tinha sido configurado; começa do passo 1 da lista "SÓ O JOÃO" (n8n → 3 Variables → Vapi → Hostinger → restart → Admin → Integrações).
**Pendente com o João:** as 3 Variables no n8n, Server Secret na Vapi, envs na Hostinger. Depois: restart da app (posso fazer via MCP Hostinger) e conferir Admin → Integrações.
**Notas tocadas:** `CONTINUAR-AQUI.md` (lido), `docs/integracoes/n8n-ligacao-ia.md` §8.2.

## 00:35 — Ligação por IA: n8n Community não tem Variables → configuração por $env (Easypanel); workflows republicados

**Pedido:** "o n8n é auto-hospedado, não permite Variables; achar outra forma".
**Feito:** `lerConfig(nome)` nos dois nós Code (tenta `$vars`, cai para `$env` em try/catch, fail-closed) — `n8n/ligacao/verificar-hmac.js` e `mapear-vapi.js` (blocos COLA NO NÓ); gerador `n8n/gerar-sdk.mjs` monta o jsCode a partir do repo e emite os scripts SDK; LANCADOR `zh5tjDcSoHaPaRRL` → `da10fdc9…` e WEBHOOK `OXetB37jgJgmif3d` → `371d2429…` republicados via MCP. `Downloads\n8n-easypanel.env` pronto para colar (4 linhas). README do n8n, doc §8.2, CONTINUAR-AQUI e o .env de Downloads atualizados.
**Medido:** sonda de 05/09 (exec 32975): `$env` → "access to env vars denied" (N8N_BLOCK_ENV_ACCESS_IN_NODE padrão); `$vars` existe mas Variables não podem ser criadas no plano. Simulação local do nó: env completo → valido; sem VAPI → `variavel_VAPI_SERVER_SECRET_ausente`; tudo bloqueado → `variavel_LIGACAO_IA_WEBHOOK_SECRET_ausente`. Pós-publicação: ping assinado → 401 `variavel_LIGACAO_IA_WEBHOOK_SECRET_ausente` (esperado até a env existir); webhook forjado → 200 "started" e o nó lança (nada assinado). vitest ligacao-ia 137/137; eslint 0.
**Decidido:** `$env` + `N8N_BLOCK_ENV_ACCESS_IN_NODE=false` em vez de Data Table/hardcode. Trade-off registrado no README: o flag expõe todas as envs do container a qualquer nó Code — aceito porque a instância tem um usuário só. `lerConfig` mantém `$vars` na frente: migrar para plano com Variables não exige mudar nó.
**Pendente com o João:** colar `n8n-easypanel.env` no Easypanel e reiniciar o n8n; confirmar credencial `Vapi API - RSVP (org nova)` no nó DISPARO (a API não devolve credencial); Server Secret na Vapi; envs na Hostinger. Depois: ping assinado tem de responder 200 `{ok:true,teste:true}`.
**Notas tocadas:** `n8n/README.md`, `docs/integracoes/n8n-ligacao-ia.md` §8.2, `CONTINUAR-AQUI.md`.


## 04:31 — n8n organizado (12 ativos com tags, 4 arquivados, 10 apagados) e mapa de integração

**Pedido:** "organizar todas as automações do n8n, apagar as inacabadas, manter só as que têm uso; deixar o terreno preparado para ligação e Chatwoot".
**Feito:** chave da API pública do n8n (`Downloads\n8n-api-key.txt`); inventário e execuções dos 26 workflows pela API; backup JSON em `projetos\n8n-backups\2026-09-07-antes-da-limpeza-26-workflows.json`; 4 sondas SETUP arquivadas (MCP), 9 apagados pela API (RSVP v1 ×2 nunca rodaram, RSVP v2 ×2 substituídos pelo v3, Monitor de presença ×6 de lives passadas — mais 1 no teste de DELETE); tags CNHF/RSVP/SIC-HF/Seminário/Acelera; `availableInMCP` nos 12 ativos; diagnóstico de erros recorrentes (Seminário alerta 25/250 transitório; RSVP DISPARO "teto de 5" tratado como erro — regra de negócio, não tocado; o "regex quebrado" do WEBHOOK v3 era histórico de 01/09); `docs/integracoes/n8n-mapa-de-integracao.md` (ligação publicada · Chatwoot 100% codificado, só 5 envs · sala Meet/Zoom sem workflow, lados do SIC-HF prontos); nota do vault `04 Sistemas/n8n - Inventario de workflows e regras da instancia.md`.
**Medido:** `DELETE /api/v1/workflows/{id}` apaga de vez (testado); PUT exige body completo; tag com acento via curl no Git Bash chega quebrada (corrigida por fetch UTF-8).
**Decidido:** apagar (não arquivar) os 10 — o João pediu "apagar"; backup guarda o JSON.
**Pendente com o João:** Easypanel env + credencial Vapi + Server Secret; Chatwoot (5 envs, decisão de contratar); sala (Meet ou Zoom).
**Notas tocadas:** [[n8n - Inventario de workflows e regras da instancia]], `docs/integracoes/n8n-mapa-de-integracao.md` (commit `30e1753`).

## 04:31 — Fase 8 "Intuitivo para advogado" publicada em `b72a3c7` (dois remotes); trava do Fable APROVADA

**Pedido:** Fase 8 — usabilidade/acessibilidade/UX para usuários de mais idade, referência de sistemas jurídicos, o sistema guia; mobile de verdade; status do croqui visível; 3 produtos Hotmart com ciclo de vida e gate por pagamento; arquivar processo; usar Find Skills.
**Feito:** pesquisa (Sonnet, `tmp/squad/fase8-pesquisa.md`, 17 fontes + skills) ‖ reconhecimento (Sonnet) → arquiteto Opus (`docs/ARQUITETURA-FASE-8.md`, D1–D22, B48–B55, C1–C6) → UX1 rodada 0 (DS §12, componentes, catálogo, jsx-a11y/vitest-axe/a11y.mjs, skill) → PAG ‖ CRQ ‖ UX2 ‖ UX3 → UX1-FIX + CRQ-FIX → pentester (1 HIGH: PATCH `{etapa, desfecho:congelada}` escapava da RPC → 409 `arquivar_com_etapa`) → Fable (reprovou 1× em UX: 2 serious de axe em `/croquis/[id]` → corrigido → APROVADO). Migrations **0083–0087** aplicadas e provadas (13/13 · 9/9 com rollback, estado depois = antes). 91 arquivos, +3.914/−1.027.
**Medido:** vitest 581 → **749** · typecheck 0 · lint 0/0 · build ok · axe 0 serious/critical em 8 telas × 2 temas × 2 escalas × {1440, 360} · densidade Hoje/Clientes/Ficha 900/900/900 (baseline 900/900/917) · /admin#repertorio 959 (= baseline) · 0 scroll horizontal a 360 px em 29+ telas · público ≥ 16 px · JS por rota +0,7–2,4% em dev (features novas; nada acima do baseline de proporção) · queries por tela sem N+1 (/hoje 3 REST + 3 API; /clientes 3 + 4) · 0 jornadas acima do teto vigente antes da 0084.
**Decidido:** estado da compra pelo EVENTO (D1); um webhook roteado por `product.id` (D2/C3); piso histórico + teto vigente (D5); reembolso trava e avisa (D6); fase do croqui por view (D12); arquivar = `congelada` reversível com efeitos por marcador (D15–D17); escala de texto do usuário com default 14 (C2/B55); axe de página fica local porque o CI não tem banco (C1); `anonimizada` entrou no catálogo `processo`. 0087 nasceu das ressalvas do Fable (views só SELECT para `authenticated`; `search_path` na trigger de ordem de evento).
**Pendente com o João:** `hotmart_produto_id` dos 3 produtos + `HOTMART_WEBHOOK_SECRET` + 3 produtos na mesma URL; B48–B55 (hipótese conservadora aplicada; B51: arquivar não revoga links — o trigger revogava em todo desfecho antes); Easypanel env do n8n; Vapi; `.env` da Hostinger; ruleset GitHub; decisões com a Dra. Elaine.
**Notas tocadas:** `CONTINUAR-AQUI.md` (bloco Fase 8 no topo), `docs/ARQUITETURA-FASE-8.md`, `docs/DESIGN-SYSTEM.md` §12, `docs/integracoes/hotmart.md`, `docs/SKILLS.md`, [[Glossario]] (+5 termos), [[Estado Atual]] (pendente de reescrita — próxima sessão).

## 11:46 — Chatwoot na VPS localizado e webhook criado; falta o inbox de WhatsApp

**Pedido:** "me manda a URL do Chatwoot que temos na VPS" → depois Account ID 1 + access token.
**Feito:** URL achada por sonda do padrão Easypanel: `https://infra-csm-chatwoot.nfpbgs.easypanel.host` (conta 1, dono "Marcio Carvalho de Sá"). Pela API: **0 inboxes**, 0 webhooks. Criado o webhook id 1 (`message_created` → `/api/webhooks/chatwoot?token=<segredo novo>`). Bloco de envs em `Downloads\sic-hf-chatwoot.env` (4 de 5; `CHATWOOT_INBOX_ID` vazio até existir inbox).
**Medido:** `GET /api/v1/accounts/1/inboxes` → `{"payload":[]}`.
**Decidido:** não criar inbox — exige credencial de canal WhatsApp (Meta Cloud API/Twilio) e escolha do número; é do João.
**Pendente com o João:** criar o inbox WhatsApp no Chatwoot e me passar o id; colar o `.env` na Hostinger; `regua.canal_whatsapp=chatwoot` em Admin → Configurações.
**Notas tocadas:** [[n8n - Inventario de workflows e regras da instancia]] (URL do Chatwoot).

## 14:40 — Fase 9: agente de WhatsApp de onboarding — trava do Fable APROVADA, publicada nos dois remotes

**Pedido:** "o número do agente só responde números cadastrados no Supabase; faz o onboarding (documentos, links do que falta); esquiva do que foge ao fechamento da holding; direto, cordial, fluxo linear; dúvidas de uso do sistema".
**Feito:** arquiteto Opus (`docs/ARQUITETURA-FASE-9.md`, D1–D28, C1–C10, B56–B64) → BACK (porteiro 15 travas, `derivarProximoPasso()` como máquina de passos, IA com Zod + guarda de saída, `scripts/simular-chatwoot.ts`) ‖ FRONT (Ficha "Conversa no WhatsApp", Recebidas com selo do agente, aba Admin "Agente de WhatsApp", pendências novas) → BACK-FIX (7 chaves no schema do Admin, contrato `agente` na fila, painel por papel) → FRONT-FIX (heurística apagada, `?busca=`) → pentest (0 crítico/alto; MÉDIO conversa não amarrada ao telefone → trava 15 via GET no Chatwoot; BAIXOS: oráculo `correspondencia` gated, guarda de termo na saída da IA) → Fable APROVADO com ressalvas. Orquestrador: 0088–0090 aplicadas via MCP; roteiro 12/12 (fixture `seminario`→`indicacao`); EXPLAIN → Index Scan `idx_pessoas_telefone_e164`; deep-link `#conversa` corrigido por ref no `<details>`.
**Medido:** typecheck 0 · lint 0 · vitest 749 → **885** · build ok · simulador 11/11 · sondas em `next start` (503 sem secret, 401 token errado, corpo de produção sem `correspondencia`) · RLS/grants provados como authenticated e anon · axe 0 violação em 5 telas × 2 temas × {1440, 360} · densidade Mensagens/Clientes/Admin 900; /hoje 1.027 por UMA pendência real (`telefone_fora_do_padrao` da única pessoa real) · banco depois = antes (respostas 0, estado 0, `ativo=false`). Elegíveis para o agente hoje: **0** (a única pessoa real não tem consentimento nem pagamento, telefone sem `+`).
**Decidido:** agente nasce desligado e prompt inativo (só texto fixo até sonda + bancada); número desconhecido nunca recebe texto (D1); link é escolha do sistema, nunca da IA (D21); orçamento próprio fora de `verificar_cooldown_ia` (C3); `emitir_link_sistema` com nome novo (C4); pendência derivada, não segundo livro-razão (D2); telefone fora do padrão vira pendência, nunca backfill (D4).
**Pendente com o João:** inbox WhatsApp no Chatwoot + `CHATWOOT_INBOX_ID`; 5 envs na Hostinger; ligar no Admin; ativar prompt após sonda/bancada; consentimentos e telefone da pessoa real; B56–B64. Ressalvas técnicas para a próxima rodada: unificar os dois casadores de telefone; corrida do teto de link (RPC); 0088 sem `if not exists`.
**Notas tocadas:** `CONTINUAR-AQUI.md` (bloco Fase 9), `docs/ARQUITETURA-FASE-9.md`, `docs/integracoes/agente-whatsapp.md`, [[Estado Atual]] (reescrita pendente).
