/**
 * Ponto único de importação do catálogo de estados (Fase 8).
 * `import { estadoDe, classificarPrazo } from "@/lib/estados";`
 */
export {
  ESTADOS,
  ESTADO_SEM_INFORMACAO,
  classificarIntegracao,
  classificarPrazo,
  diferencaEmDias,
  estadoDe,
  rotuloEstado,
  type ChaveEstado,
  type ClasseIntegracao,
  type ClassePrazo,
  type DefinicaoEstado,
  type DominioEstado,
  type TomEstado,
} from "./catalogo";
export { IconeEstado, NOMES_DE_ICONE, type NomeIcone } from "./icones";
